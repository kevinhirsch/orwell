# Ship gate — the launch-acceptance bar (2026-06-27)

**Purpose.** Stop discovering-and-polishing into infinity. This document defines what
"shippable" means for Orwell, so we can draw a hard line: a finite set of **launch-blockers**,
everything else explicitly **post-launch** or **parked**. It is the overseer's authoritative
launch checklist; pair it with the issue triage at the bottom.

## The launch thesis (read this first)

The product is **feature-complete** (specs 0001–0074 built; 0075–0086 + 0105/0106 built; the
weekly loop runs to a winner across seeds; the audits confirm the **Vault holds, anti-sycophancy
holds, the hidden layer + persistence are sound**). We are **discovering, not breaking.**

The bug *count* surfaced this session is high because we hunted hard (the BB-nerd audit +
real-model live-verify). But the *severity* is **concentrated in one seam**: the player-facing
**casting → premiere narration** — which is also the most visible part of the game (it is the
onboarding). The engine core is not rickety; **one soft surface that happens to be the first ten
minutes** is.

**Root cause of the recurring surprises:** every CI gate **stubs the LLM**. The product's real
behavior lives in the model↔engine seam, and that seam is the one thing CI never exercises — so
tool under-calls, narration desyncs, the inert desync guard, and cast-authoring richness all ship
*green* and only surface when a human drives a real model for ~30 minutes (SOUL lessons 17 & 19).
The strategic fix is to **make the real-model golden path an automated gate** (feature 0108, in
spec) so we stop re-discovering the same class of bug by hand.

> **Update (2026-07-09):** feature 0108 is **built and armed** — the first real-model fixture
> `frontend/tests/golden/golden_path_glm-5.2.jsonl` is committed (regenerated #1251), so the
> `golden-path` CI job replays it key-free (twice, comparing digests) and is **blocking** via
> `ci-gate`. The G1–G9 golden path is now gated automatically (I1–I8 PASS/SKIP), not just
> live-verified. Source: `docs/audits/2026-07-09-ship-gate-reverification.md` (committed in PR #1269; this PR should merge after it, and the link resolves then).

## The acceptance bar — the golden path a real player must walk

> **#1 release standard (owner ruling, 2026-06-27) — the FE must be airtight, above all else.**
> Before the golden path below even matters, the front-end itself must be *airtight* under
> concurrency. The bar, gated separately and above G1–G9:
>
> | # | Gate | Pass criterion | Status |
> |---|------|----------------|--------|
> | F1 | **No missing messages** | A sent message NEVER disappears/drops — not on send, reconcile, reload, or a refused/409 turn. Every authored turn lands + persists. | ✅ **PASS** on a correctly-bound game (two-window audit 2026-06-27); the binding breaks #1085/#1086 are **fixed** (#1092); re-audit pending |
> | F2 | **Right status at the right time** | Every control (send, decision card, gadget toggles, status chips) shows the correct state for the moment — never stale/wrong, never a dead control, never a control that lies about state. | ✅ **PASS** (audit); send button cycled idle→streaming→newchat correctly, no dead/lying control on the bound game |
> | F3 | **Smart queueing, no lost sends** | Send-while-streaming / rapid sends / offline→online queue + flush in order, zero loss, never double-send (the outbox, #985). | ✅ **PASS** (audit); send-while-the-other-window-streams queued + landed, in-flight stream survived, no double-send |
> | F4 | **Multi-window concurrency** | F1–F3 all hold with **multiple windows/tabs open on the same game** — no state bleed, no lost update, no divergence (ADR 0008). | ✅ **PASS** w/ one self-healing transient (#1087 GAP-3); simultaneous A+B sends converged identical |
> | F5 | **Realtime mirrored parity** | Two windows are **pixel-perfect mirrors in realtime** — a turn, status change, decision card, gadget state in one appears identically + promptly in the other (ADR 0012 two-window "Messenger mirror" + 0064 server-push). **Executable gate (ADR 0015 / R2):** `docs/audits/playtest-harness/mirror_live_parity.mjs` via `run_mirror_gate.sh` (key-free, deterministic fake streamed model) — asserts B renders DURING A's stream, through the same incremental renderer, within a bounded lag; `frontend/tests/test_0012_mirror.py` is the fast `xfail` tripwire. | ⛔ **RED on main (2026-07-09)** — the 2026-06-27 status below was a by-inspection PASS; re-verification **executed** the harness on current main and `mirror_live_parity.mjs` **exits 1** on `bUsesIncrementalRenderer` (mirror window B never mounts the streaming container; settled transcripts still identical). Fix in flight on `claude/f5-mirror-parity-fix`. Source: `docs/audits/2026-07-09-ship-gate-reverification.md` (committed in PR #1269; this PR should merge after it, and the link resolves then). *(prior: ✅ PASS (audit); window B mirrored A's live deepseek stream in lockstep, lag ~0s, identical final transcripts)* |
>
> **The FE ships only when F1–F5 are *proven* by a repeatable two-window concurrency harness, not
> by inspection.** F1–F5 are **model-independent** (FE sync/render/queue layer), so the audit AND
> the permanent gate run **key-free against the deterministic narrator** — reproducible and fast.
> Sequence: two-window audit (map the gaps) → fix campaign → the harness becomes a permanent CI
> gate (ADR 0008 "two-tab concurrent-write parity gate" / refactor-roadmap R0).

**Definition of shippable:** a real player, on a fresh install with a real model wired, can go
**casting → first eviction** with *no dead screen, no Vault leak, no stuck beat* — **and** the FE
holds F1–F5 above under multiple concurrent windows. Concretely, every line below must hold in a
**real-model** run (not a stubbed gate):

| # | Gate | Pass criterion | Status (2026-06-27) |
|---|------|----------------|---------------------|
| G1 | **Casting opens itself** | After setup (API key set, access enabled, settings closed), the producers send the opening message **on their own**, promptly, with no player prompt. | ✅ **live-verified** (convergence run 2026-06-27, #1082): opener auto-fired +4.4s, no player msg |
| G2 | **Post-photo resume** | After the headshot upload, the producers acknowledge + begin the interview **automatically**, no manual "continue". | ✅ **live-verified** (#1082): auto-acked on "Skip for now", no manual continue |
| G3 | **Casting finalizes** | One player readiness signal → `createCharacter` → season starts (`started=true, week=1, premiere`). | ✅ live-verified (#1033) |
| G4 | **Cast is richly authored** | ≥13/15 NPCs get a deep authored profile (not the deterministic floor). | ✅ **live-verified** (#1081): authored **14/15, 0 degradation** (up from 9/15); the 1 floor = player-mirror, not a refusal |
| G5 | **The game advances** | No stuck phase: HOH → noms → veto → eviction each reach the player without a manual nudge. | ✅ live-verified (premiere→week-2, beatSeq 73→126) |
| G6 | **No premature outcome leak** | The GM never narrates a result/tally the engine hasn't committed (the desync guard holds — including single-tenant `owner=None`). | ✅ **live-verified** (#1014/#1063): guard HELD a phantom ×7 at `owner=None`; player saw only the committed evictee |
| G7 | **Eviction reads cleanly** | Eviction-vote card surfaces; player goodbye blocks the week roll (E34); reveal is batched, anonymized; week rolls. | ✅ live-verified (#1064): batched single staged reveal; goodbye gated the roll |
| G8 | **No machinery in the bubble** | Player text never contains Vault data, raw `npc:<id>`, operator asides, or reasoning tokens. | ✅ live-verified (#1047): 79 AI bubbles, 0 leaks |
| G9 | **No cross-user / Vault leak** | Player + admin surfaces return zero Vault data; cross-user isolation holds. | ✅ structurally gated (dependency-cruiser + tests) |

**Launch gate status — 2026-06-27 convergence run (real DeepSeek-V4-Pro, full casting→eviction→week-roll):
ALL G1–G9 PASS, and the F1–F5 two-window mirror RE-AUDIT PASSES** (GAP-1 #1085 wipe-all unbinds cleanly,
no window-collapse; GAP-2-b1 #1086 casting reply no longer strips; realtime mirror byte-identical). #827
(first-message-loss / Stop-hang) verified RESOLVED. **The launch-blocker tier is now proven, not just coded.**
Residual: GAP-2-b2 (casting cross-tab convergence, owner decision, #1086), #1087 (low self-healing transient),
and one MINOR non-blocking narration fourth-wall nit (#964 family — "the engine confirms it" under a
repeated-nudge stall loop; not a Vault leak; doesn't surface in normal play). None gate ship.

## What gates a launch vs. not (the triage rubric)

Apply to every open issue:

- **LAUNCH-BLOCKER** — breaks G1–G9 on the real-model golden path: a dead screen, a Vault/leak, a
  stuck beat, a broken casting/eviction step, or a cross-user violation. These, and only these,
  block ship.
- **POST-LAUNCH** — real but does not break the golden path: week-N polish, secondary surfaces,
  responsive nits that still leave the control reachable, enhancements, calibration tuning,
  refactor-roadmap items (R1–R7). Ship without them; fix on a fast-follow.
- **PARKED** — needs a product decision or external input, not more agents: the Glass cluster, the
  HIG-audit set, the PO-review enhancements, and the real-host/deploy items (#0010 Proxmox smoke).
  Surface to the owner; do not spend agents until decided.

**Discipline:** new bugs found after this line is drawn are triaged into one of the three buckets
on sight. Only the LAUNCH-BLOCKER bucket may delay ship. The ~80 open issues are
**overwhelmingly post-launch/parked** — they are not a launch backlog.

## The hardening tracks (in flight as of 2026-06-27)

0. **FE airtight + two-window realtime parity (THE top release blocker, F1–F5).** Diagnose first:
   a repeatable **two-window concurrency audit** (real FE + engine + deterministic narrator, two
   Playwright windows driven concurrently, timestamp-aligned telemetry) maps every gap against
   F1–F5 — message loss, wrong/stale button status, queue drops/double-sends, cross-window state
   bleed, and realtime mirror divergence. Then a focused fix campaign, then the harness lands as a
   **permanent key-free CI gate**. Model-independent — no API key needed at any stage.
1. **Real-model golden-path gate (feature 0108, spec in progress).** Record one real run; replay it
   deterministically in CI to assert G1–G9 on every PR (key-free); nightly real-model smoke to
   catch prompt drift. *This is the move that makes "ship soon" safe* — it converts the manual
   30-minute live-verify into a repeatable gate.
2. **Casting → premiere P0 hardening.** G1 (#967 opener), G2 (#969 resume), G4 (cast authoring:
   the FE retry landed 3/15→9/15; the engine `recordCastProfile` degradation refusal is the
   remaining loss), G6 (F16 guard live re-confirm).
3. **Season-start concurrency.** The background write-backs racing premiere mutations (the
   degradation refusals) is architectural — reconcile-and-retry (the 0081 pattern) or serialize
   against premiere. Kills a whole bug class, not one instance.

## Issue triage (141 open, snapshot 2026-06-27)

Bucketed against the rubric. Composition: **~71 enhancements** (`type:enhancement`), **~18 bugs**,
**30 unlabeled** (mostly `feat`/PO-question), 4 chore, 2 design-review, 2 feature, ops/deploy.

**Headline: NO new hidden launch-blocker.** Every open bug is either already fixed (close-after-verify),
post-launch polish, or parked — *except one verify target (#827)*. The launch-blocker set is exactly the
in-flight F/G tier below.

### LAUNCH-BLOCKER (the only ones that gate ship — all in flight or one verify)
- **F-tier (FE-airtight):** #1085 (GAP-1 stale binding — **fixed**, #1092), #1086 (GAP-2 casting fork/strip
  — b1 **fixed** #1092; b2 owner-decision), #1087 (GAP-3 transient, low).
- **G-tier (casting golden path):** #1014 (F16 wrong-evictee guard — fix merged #1063, **held for live**),
  #967 / #969 (opener/resume — fix merged #1082, **held for live**), #1044 (cast authoring — fixed #1062/#1081,
  **reopened, held for live**).
- **One verify:** **#827** (P1 "first message disappears + Stop hangs") — no merged commit cites it, but the
  two-window audit proved **F1 (no missing messages) PASSES** on a bound game and the #961/#962/#971/#836
  cluster fixed disappearing messages. Almost certainly resolved by sibling work; **fold into the two-window
  re-audit** to confirm, then close or fix.

### CLOSE-AFTER-VERIFY (merged fix exists; confirm in the re-audit/convergence run, then close)
#835 (truncation → 664300cf), #873 (machinery leak on re-render → 424f3499), #968 (seeding indicator →
0eb97f94), #965 (OOC-marker mix → #970/#972 renderer+prompt work). Verify each holds live, then close.

### POST-LAUNCH (real, but does not break F1–F5 / G1–G9 — fast-follow)
The ~71 enhancements; the #620/#621/#626 roundups (narration/FE-Python/UX-a11y tails); responsive/a11y nits
(#934, #935, #938, #934 tap-floors, landscape); cosmetic render (#828 OOC styling-until-refresh, #871 cursor,
#964 fourth-wall vocab P2, #989 "Let me…" scrub P3); settings polish (#659, #661, #890); calibration tuning;
the refactor-roadmap R1–R7. #914 (mid-session 401 re-auth) sits here unless the re-audit shows it loses a
player turn — then it promotes.

### PARKED (needs a product decision or host — not agent work)
The **Glass cluster** (#718–724 + the parked PR #709), the **HIG-audit** set, #916–918 (still awaiting
owner rulings as of this pass), #926 (favicon), #951 (notification unification), and the **ops/deploy**
items (#0010 Proxmox container smoke / deploy verify — need the host). Surface to the owner; do not
spend agents until decided.

*(2026-07-05 correction, doc-drift audit-closure: **#905–909 already have owner rulings**, recorded
the same day as this doc — `docs/decisions/PO-DECISIONS-LOG.md`'s 2026-06-27 entry (R1–R4) — and were
folded into the build-ready spec [0111-day-1-experience](../features/0111-day-1-experience.md)
(renumbered from 0102 to resolve a filename collision). They are **not** "awaiting owner rulings";
they're designed and ready to build, just not yet implemented. Moved out of the "needs a decision"
framing here so a future pass doesn't re-litigate a settled question.)*

> Cross-check: matches the source-verified snapshot (`docs/audits/2026-06-21-open-items-verification.md`) —
> **no launch-blockers remain in the tracker** beyond the F/G tier above.

---

## M0-3 closure — the owed live-LLM two-window re-run + mid-generation-join pin (2026-07-08)

The ADR-0008/0012 residual ("owed verification": a live-model two-window re-run through a full
ceremony sequence, plus the mid-generation-join test pin) is **closed**. Two artifacts:

**1. The deterministic pin (permanent, CI):** `frontend/tests/test_m0_3_midgen_join.py`
(fe-browser-tests lane). Real engine (seeded season) + real FE + a scripted OpenAI-compatible
slow-drip stub (~20s narration). A run kicked over REST (a third device); window A joins at
stream start, window B joins MID-GENERATION — both must render live partial text before settle
(canonical resolve → events subscribe → `run-started` → `resumeStream`) and settle byte-identical
with exactly one bubble each. 3/3 green locally. *(Harness subtlety, documented in the file: the
game-build leak scrub emits the visible stream per SENTENCE — a stub whose prose has no sentence
terminators buffers the whole round and false-fails the live-paint assertion.)*

**2. The live-model evidence run (this closure):** `frontend/scripts/_verify_two_window_live.py`
— production-shaped stack, real OpenRouter narrator (narration `z-ai/glm-5.2`, utility
`qwen/qwen3.6-flash`), one live game driven premiere → HOH competition → nominations → the veto
transition with two Playwright windows open throughout and a third joining mid-generation on an
HOH-phase turn. Result: **VERIFY OK — 14/14 checks** (run 2, 2026-07-08; committed evidence
`docs/audits/evidence/m0-3-live/`: `verdicts.json`, final screenshots of both windows, window C
settled; the run also leaves a full working copy under `frontend/data/_m03_live/`):

| check | result |
|---|---|
| parity after both windows join a warm run | PASS |
| per-turn transcript parity, turns 1–12 (premiere ×4, hoh-competition ×5, nominations ×3) | PASS ×12 |
| mid-generation join (window C, HOH beat): partial before settle → converged after | PASS |
| full ceremony sequence reached | `['premiere','hoh-competition','nominations'] → veto` |

Final screenshots show A and B pixel-equivalent: same transcript, same decision card
(veto comp-intent), same board (HOH/noms), same whereabouts + time-of-day panels. An earlier run
the same day (run 1) passed 8/8 checks through the HOH beat — including the mid-gen join — before
its DRIVER (not the product) died on a 240s socket-read timeout during a long GLM thinking pause;
the driver now reads with a 600s per-read timeout and degrades to status-poll settle (the run is
server-detached), and persists verdicts incrementally.

**F5 note:** the realtime-mirror parity above held at REAL model latency (rounds of 1–3 minutes,
one 193s round) across 26 settled-turn parity checks over the two runs, zero divergences, zero
duplicate bubbles, zero stripped replies.
