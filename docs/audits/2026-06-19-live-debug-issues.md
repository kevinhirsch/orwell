# Live-debug issue ledger — 2026-06-19

Logged from a live play session (real FE + engine, deepseek-v4-pro). Each item is an issue to
resolve. Status: ☐ open · ◐ in progress · ☑ done. Lane: FE (front-end), ENG (engine), OPS (deploy),
COPY (text). This ledger is the authoritative open-items list for this batch.

## Casting / OOBE flow
- **L1 ☑ FE** — The headshot prompt/studio panel renders **under the "orwell" header and gets jumbled** (z-index / layout); it keeps interfering with the logo throughout the headshot step.
- **L2 ☑ FE** — In OOBE, the **"Open settings" button doesn't open settings** — it only closes the modal back to the main chat screen (broken action; looks wrong).
- **L3 ☑ FE** — During the headshot step, the generation **accordion is collapsed by default** after generating your photos. It should not collapse at all, let alone by default.
- **L4 ☑ FE** — After tapping a desired generated headshot it says **"casting headshot set" but the dialog doesn't dismiss or guide you into the game** — only "make another" / "remove" are offered, and it keeps covering the logo; the only escape is collapsing it manually. Selecting a headshot must dismiss the picker and hand off into the game.
- **L5 ☑ FE** — Once the headshot is selected, **the producers must send the first message to open the game** — the player should NOT be responsible for the first word. The game opens with the producer's opener.

## Thinking / immersion / accordions
- **L6 ☑ FE** *(L6a + L6b done)* — Model **thinking/reasoning was visible** in the game build. Thinking must **never** be shown/expanded by default (not even while generating); viewable ONLY via an admin-only opt-in.
  - **L6a ☑** — the `<think>`-block / reasoning-stream variant: suppressed in the game build across the live + reload paths (PR #321).
  - **L6b ☑ FE** — the **plain-content** variant: at 07:48 the planning ("`The game state shows…` `npc:1 - Faith Willis` … `Let me stay in character`") did **NOT** render in a thinking block — the model emitted its reasoning as a regular assistant turn (an intermediate AGENT ROUND), so think-block suppression does not catch it. Fix: in the game build, suppress the free text of INTERMEDIATE agent rounds (any round that then calls a tool / is followed by another assistant round) — show only the FINAL narration round — and/or broaden plain-reasoning detection ("Let me…", "Looking at the roster", "The game state shows", raw `npc:<n>` ids). The narrator only ever speaks the final in-character text.
- **L7 ☑ FE** — Every inter-message "action" carries an **accordion** that expands to hidden/empty content. Remove all worthless accordions: if there is nothing to expand, render **no** expand affordance.

## Settings / models
- **L8 ☑ FE** — Selecting only deepseek-v4-pro shows **both "1/341 models enabled" and "341/341 models enabled"** — conflicting counters.
- **L9 ☑ FE** *(hidden in the game build)* — The **agent/chat switch** in the chat bar has unclear purpose in this game (play is always a hybrid). Decide whether to remove/hide it in the game build. *Recommendation: hide in the game build.*

## Windows / gadget rail / layout
- **L10 ☑ COPY** — The top-right pane reads **"The house"** → should be **"The House"** (capital H).
- **L11 ☑ FE** — The **cast window is too large and not responsively resizeable**. Every window must be resizeable from the **side and corner** on desktop.
- **L12 ☑ FE (feature)** — Allow **pinning the cast window into the right sidebar** (e.g. two small portraits side by side).
- **L13 ☑ FE (feature)** — Allow **drag-reordering the gadgets** in the sidebar rail.
- **L14 ☑ FE** *(subsumed by L26)* — The **"Where you are" panel should show first names only**, disambiguating duplicates as "First L." (e.g. `Kevin H` only when two Kevins exist; otherwise `Kevin`). Full names are too wordy.

## Cast generation / portraits
- **L15 ☑ FE/ENG (shipped #329)** — While watching the cast generate, the **final photo generated, then all photos vanished and the FE lost the backend connection** (required a full page refresh). The backend must stay responsive during generation and report progress; it must never drop the FE.
- **L16 ☑ FE** — Some cast photos generated in **B&W**. All portraits must be **full color until a houseguest is evicted**, then rendered **B&W/monotone** (the eviction state is the only monochrome).
- **L17 ☑ ENG/FE (feature) (shipped #329)** — After the full cast generates, run an **automated pass for look-alikes / mistakes** and **regenerate** offending portraits until the set is distinct and correct.

## Engine / ops
- **L18 ◐ ENG/OPS** — The engine **hangs and requires an FE reload** at `E22 guard: game turn narrated with no engine write - recording a fallback digest. process count = 2`, then 502s for a few seconds. Investigate the E22 fallback-digest path for the hang; determine if it is a specs/perf issue. **Update deploy defaults to recommend better container specs and document recommendations regardless.**

  **Diagnosis (2026-06-19).** Not a deadlock — a **latency/queue pileup**, worsened by an under-resourced host. The chain:
  1. The narration LLM **under-calls** the engine tools (the known gap the FE error-corrects). On every such turn the FE fires the E22 fallback `recordInteraction` **fire-and-forget** at stream `[DONE]` — `asyncio.create_task(ensure_turn_recorded(...))` in `frontend/routes/chat_routes.py` (~L1248), with no tracking. "`process count = 2`" is simply the FE's `_unrecorded_turn_fallbacks` counter reaching 2 (two under-called turns), **not** an OS process count.
  2. The engine **serializes all calls for one user** into a per-user promise queue (B60/audit E10, `src/adapters/mcp/HttpMcpServer.ts` `enqueue`). This is correct and must stay — it closes the sandbox-swap race. But it means the stacked fire-and-forget fallbacks **plus the next live turn's framing reads** all run strictly in order for that user.
  3. Each `recordInteraction` commit pays the **O(events) snapshot export + integrity checkpoint** (`registry.snapshot` → `exportSnapshot` runs `events.query()`; `Orchestrator.commitPlayerTurn` → `checkpoint` runs `toGameState`/`isSuperset` over the full event set; plus, in turn-driven mode, one off-screen tick with soul folds). The codebase already flags this as the unresolved **R3** cost ("late-season latency grows with the O(events) snapshot export"). On a 2 vCPU / 2 GB box (the old deploy default) several of these back-to-back, late season, blow past the FE's 30s engine timeout and the reverse-proxy window → the FE turn 502s and the UI looks hung until the queue drains. (The synchronous fastembed `Atomics.wait` embed bridge is **already** mitigated by the G8/G12 breathing lane — `recordToSoul` defers indexing one embed per macrotask — so soul writes are not the pin; `recall` is the only sync-embed-in-tool path and it is not used in any live tool.)

  **Fix shipped (safe, no game-logic / Vault-Wall change):**
  - **OPS / L18b** — raised the LXC deploy baseline to **4 vCPU / 8 GB RAM / 12 GB disk** (`deploy/orwell.sh` `CORES`/`RAM_MB`/`DISK_GB`; still env-overridable) and documented a **Recommended specs** section in `deploy/README.md` (LLM is remote ⇒ CPU is for FE + engine + local fastembed embeddings; more RAM if also running image generation).
  - **FE / L18a mitigation** — made the E22 fallback **single-flight per user** (`frontend/routes/chat_helpers.py` `ensure_turn_recorded` + `_fallback_in_flight`): a fallback fired while one is still committing for the same user is **skipped** (the under-call is still counted; the next real engine write or the next, now-unblocked fallback still records the consequence). This stops the fire-and-forget guard from stacking a backlog of O(events) commits behind the next live turn — the proximate trigger of the observed stall. Tests: `frontend/tests/test_lane6_turn_integrity.py` (concurrent same-user skip, per-user isolation, failure releases the slot).

  **Deferred (larger, not forced):** the **R3 incremental-snapshot** work — `exportSnapshot`/`checkpoint` are O(events) per mutation, so even with the single-flight guard and a bigger host, a very long season's per-turn commit cost keeps growing. The real cure is an append-only / incremental snapshot + checkpoint so a commit is O(Δ), not O(events). That is an engine-spine change (touches `src/composition/orchestrator.ts`, `registry.ts`, `sessionSnapshot.ts`) and should be its own queue item; it is intentionally **not** attempted in full here. *(PARTIAL SHIPPED: `toGameState` is now memoized by snapshot IDENTITY (a WeakMap; provably safe — pure function, immutable snapshots), eliminating the redundant baseline re-projection the checkpoint + the supplementary tick paid every commit. The full O(Δ) `isSuperset`/leak-check rewrite — which guards mandates #1/#4 — stays the deferred follow-up.)*

## Characters / content
- **L19 ☑ ENG** *(subsumed by 0058 Phase 1 + L41 — the §3-depth profiles + the diverse demeanor/voice register replace the one-word persona)* — Expand **archetypes & personas** to be richer than a single reductive word.

## Model output
- **L20 ☑ FE** *(regression-guarded; no live strip path found)* — **Trailing question marks on deepseek responses are systematically stripped** from the end of messages (recurring — likely systemic in the stream/sanitize path).

## Carried from the prior transcript (related)
- **L21 ☑ ENG/FE** *(engine: whereabouts+duration in context, player held, moveTo lever; FE: the L26 "Where you are" gadget + the auto-move belt now ship. NPC-personality movement weighting was prototyped engine-side but DROPPED — bending the per-NPC move rate by social aptitude shifts the seeded `juryReach` calibration gate past its `F2_WIN_MAX` cap, so it is not worth a calibration regression; NPC positions are already grounded + persistent via the existing affinity-clustered, adjacency-constrained, stay-probability assignment. The remaining immersion fix was the PLAYER auto-move belt.)* — **Whereabouts cohesion**: NPC + general whereabouts contradict across turns (Garrett "on the couch" → "still to arrive"; player moved to the living room → "still in the kitchen"). The model invents positions instead of grounding to the engine's `whereabouts`, and player movement isn't persisted. Surface whereabouts in the per-turn GAME CONTEXT (the C8-04 ceremony-context pattern) and persist player room moves so the picture stays consistent. **FE fix: `_auto_move_player` (`frontend/src/agent_loop.py`) — when the player walked to a room but the model never called `moveTo`, a constrained extraction relays the destination and the FE calls `move_to` itself, PERSISTING the move so next turn's whereabouts stays consistent (no snap-back). Mirrors the 0055 `_auto_record_scene` belt. Test: `frontend/tests/test_l21_l24_auto_move.py`.**
- **L22 ☑ ENG** — **Setting must stay fixed to the LA house.** Narration relocated the world to the **player's hometown** (it scraped the player's stated origin and set the scene there). Every season happens in the Big Brother **house in Los Angeles**, full stop — the model must NEVER move the setting to anyone's hometown/backstory (no off-site scenes, no hometown weather/landmarks). Origin colors who a houseguest IS, never WHERE the game happens.
- **L23 ☑ ENG** — **Stop re-describing bodily features.** Physical description (hair, build, etc.) is helpful to establish a houseguest in the first few moments, but with cast **headshots** present it is redundant thereafter — past intros the model should describe **demeanor / personality / behavior**, not physical appearance.

- **L24 ☑ ENG/FE** *(same cohesion fix as L21 — engine grounding shipped; the FE player auto-move belt now persists player room moves so the picture stays consistent turn-to-turn)* — **Cross-message context loss / scene discontinuity.** Turn to turn, the world resets: houseguests who were in one room doing one thing are elsewhere doing something else the next message. The model invents scene state each turn instead of grounding to PERSISTENT engine state. Same family as L21 — ground the per-turn context in the engine's `whereabouts` + the recent-record so the scene is continuous, and persist player/NPC positions. The single biggest immersion-killer.
- **L25 ☑ ENG (Vault-adjacent leak)** *(sealed via base-prompt pin + npcVoice sentinel test; traced — no engine data-flow leak, the model was improvising from chat)* — **Casting / producer-interview content leaks into NPC dialogue.** An NPC "clocked '*camp counselor energy*'" — the exact phrase the player used to describe himself to **producers** in the casting interview. The casting interview is a player-level / OOC channel (like the Diary Room): the player's private self-description and strategy have **no in-game pathway to any NPC**. NPCs may form their OWN read of the player from WITNESSED behavior, but must never echo the player's private casting answers verbatim. Filter casting-sheet / private-strategy text out of anything that feeds NPC voicing (it should never reach an NPC's mouth).

- **L26 ☑ FE** — **"Where you are" gadget redesign.** HEADER = the room you're in + who's with you, first names: `Kitchen — Keith, John, Joe`. BODY = the **visible** rooms/houseguests from the player's perspective only (the adjacent rooms and who's in them) — omit rooms and houseguests out of view entirely. The engine `whereabouts()` view already returns this exact shape (`{ room, present, nearby: [{room, present}] }`, scoped to the player's room + adjacent rooms), so this is a pure FE render of that projection. **Subsumes L14** (first-names-only, `First L.` to disambiguate duplicates).

- **L27 ☑ ENG/FE** *(recall-fold shipped + test; L27b extends)* — **All summarized social play AND all messages must be stored in the engine (MCP) for later retrieval, to build ongoing story & narrative.** This is the project's spine (non-degradation #4 + ADR 0003: long-term memory is the STORE recalled, never the chat remembered). Every turn — engaged social scene, narration, OOC — must leave a durable, retrievable SUMMARY in the engine stores (EventStore / soul memory / the vector index), not just the hidden-weight fold. Today: `recordInteraction` (0002) stores `{withIds,kind,content}` summaries for player-witnessed scenes; `_auto_record_scene` (0055) guarantees ENGAGED player↔NPC turns fold + record; the soul vector (0024) backs semantic recall; the record (0048) holds public events. GAP to close: ensure EVERY turn produces a stored summary (not only engaged player↔NPC), and that the narrative-recall seam (retrieve relevant past summaries into the per-turn context) is wired so continuity is built from the store, never the chat window. Verify coverage end-to-end; add the missing per-turn summary capture + retrieval, and tests proving a scene summarized N turns ago is recallable.

- **L27b ☐ ENG/FE (extends L27/L28)** — **LLM-authored detail must persist + recall in FULL detail, and the engine must be aware of weight impact.** If the LLM generates an NPC backstory (or any detail) in extreme detail, it must be RECALLABLE in that same extreme detail later (not flattened to a one-line summary), AND the engine must know whether/how that detail moves any relationship/soul weight (the consequence fold, 0023). So: (a) recording must capture enough detail (not over-summarize) for full-fidelity recall; (b) every established detail is durably stored + semantically recallable (L27 fold — done for scenes; verify backstory granularity); (c) weight-bearing details fold their hidden impact. Airtight like a video game: a detail, once established, never drifts and is always recallable.
- **L28b ◐ ENG/FE (the hybrid — owner ruling 2026-06-19)** *(ENGINE half SHIPPED: `recordCastProfile` is LIVE — validates non-player-mirroring, splits PUBLIC↔HIDDEN across the wall, keeps the calibrated seeded leans (LLM authors flavor only), re-derives threads, re-indexes for full recall, and re-seals idempotently via the new `VaultStore.replaceHidden`. REMAINING: the FE producer-LLM authoring caller + the backstory-first→portrait pipeline order.)* — **Backstories are LLM-generated (endless variety) and engine-persisted (airtight).** The cast's backstories must be *endlessly* randomly generated — so the LLM authors them, not a finite engine pool. But every generated detail is then **persisted to the engine as the source of truth** (Character/soul), recalled in FULL detail forever (L27/L27b), never re-invented/drifting. At cast creation a producer-framed LLM writes each NPC a rich, diverse, **non-player-mirroring** backstory; the FE persists it back through a new engine seam (mirror the 0051 portrait-prompt pattern: engine emits the Vault-free cast skeleton → FE LLM generates → FE writes the backstory back to the engine, which stores + indexes it for recall + folds any weight impact). The engine's role: the airtight store, the diversity caps + non-player-mirroring guarantee (validate/repair the LLM output), recall, and weight-awareness. The L28 seeded-pool work is the deterministic FLOOR/fallback (tests/offline), not the primary source.
  - **PIPELINE ORDER (owner ruling 2026-06-19) — backstories are spawned FIRST, before image generation.** The detailed backstories must be authored + persisted at the VERY START of cast creation, **before** the portrait/image-gen step — because **the backstory feeds the portrait prompt** (the face must MATCH the person: a 52-year-old blunt welder from Detroit looks like that), and image generation fires very early. So the order is: (1) engine seeds the deterministic skeleton → (2) **L28b LLM authors + the engine seals the rich backstory for every NPC** → (3) the sealed backstory + appearance facets feed the **portrait-gen prompt (L29)** → (4) faces are generated → (5) only LATER does the **premiere (L31) VOICE** the already-sealed persona (it never generates it). The premiere consumes pre-seeded backstories; it does not create them. Implementation: the createCharacter→portrait handshake must block portrait-prompt assembly until the backstory write-back lands (or the engine builds the portrait prompt FROM the stored backstory, so there is one source of truth).
  - **THE PROFILE SCHEMA — match the v0 Producer's Vault §3 DEPTH** (`docs/legacy/BB_ProducersVault.md`, vendored as the depth reference; new spec **0058**). Each spawned-at-start, persisted-airtight profile carries the same sections the v0 doc proved out, split across the Vault Wall: **PUBLIC `CHARACTER`** (voiced, shown, feeds portraits) = biography/backstory (presentable parts), personality & observable traits, **speech patterns & voice register** (the distinct cadence/catchphrases — the L28 voice fix), occupation, hometown, age, archetype, **physical characteristics** (see below); **HIDDEN Vault/Soul** (never crosses; folds into weights) = the 2–3 **secrets**, the **true** strategic goals, the named **weakness/blind spot**, the real **stat scores**, and the **Perception-of-the-player Day-1** read (which **seeds the NPC→player relationship edge** — cf. v0 §10 perception tracker). Recall must return these in FULL detail (L27b), never flattened. (Take the v0 *structure/depth* only — never its named cast, the player "Ryne", or the dropped Luck stat; repo testing rule.)
  - **SEPARATE, EXPLICIT PHYSICAL CHARACTERISTICS — one source of truth for text↔image cohesion (owner ruling 2026-06-19).** Spawn each NPC's physical description as its OWN structured, persisted facet (height/build, skin tone/ethnicity cues, hair, facial features, distinguishing marks, age-appropriate look, style) — distinct from the prose "appearance"/"presentation" blurb. This single stored physical descriptor is the ONE source consumed by BOTH the **portrait/image-gen prompt** AND the **text narration**, so the words and the picture always agree (no drift between how a houseguest is described and how they look). It is sealed before portraits are generated (pipeline order above) and recalled in full. This is the concrete hook L29 builds the diverse-faces prompt from.
- **L28 ☑ ENG (CRITICAL)** *(seeded multi-axis diversity shipped: vocation/hometown caps + non-player-mirroring (c2b609e); distinct **demeanor/voice register** facet + body-type spread + voicing pins (ae0f247). The LLM-authored ENDLESS variety is tracked as L28b.)* — **NPC cast is NOT diverse — multi-dimensionally, and it MIRRORS the player.** Two faces of the same problem: (a) NPCs echo Player 1's attributes (three "from Phoenix"; the player's vocation repeated); and (b) — clearer in the wander transcript — even with varied *job titles*, **every houseguest has the SAME voice/register**: warm, witty, quick-bantering, emotionally-available professional. No villain, no hothead, no blunt/cynical one, no quiet/awkward one who stays quiet, no older or blue-collar or low-status-energy person, no real range of age/body type/education/disposition. Surface variety (occupations) hiding a homogeneous personality. The cast must be a **completely diverse crew, producer-filtered** across EVERY axis: archetype + **disposition** (force a spread — clash/villain/neutral/bond, not all warm bonders), **voice/register** (each houseguest voiced distinctly — abrasive, deadpan, anxious, grandiose, terse, etc.), age, body type, geography, background, education/class. And **independent of the player's profile** (casting answers must NOT seed NPC generation; same seed ⇒ same cast regardless of player). Fix in `CharacterFactory` (the disposition/archetype/age/body spread + non-player derivation) AND the voicing prompt (`npcVoice`/momentPrompts: voice each NPC in their OWN register — the house is NOT a room of identical witty professionals). Test the SPREAD (archetype/disposition/age distribution), the non-mirroring, and that no single vocation/region/disposition dominates.
- **L29 ☑ ENG (Tier 2)** *(the sealed `physicalCharacteristics` facet now AUTHORS the portrait prompt — `physicalFacetToAppearance` is the one source of truth shared with the narration; falls back to prose on pre-0058 saves. The LLM-authored facet rides in via L28b. FE image-gen consumes the enriched prompt.)* — **Cast portraits look too similar.** Faces (and body types) are too alike — not "truly cast." Portrait prompts must produce **vastly different** people: distinct faces, ages, ethnicities, hair, builds/body types, styles. Enrich `portraitPrompts` (and the per-cast variety/anchor logic) so the generated set reads as a diverse real-TV cast. **The portrait prompt must be BUILT FROM the single, sealed `physical characteristics` facet** (L28b — height/build, skin/ethnicity, hair, features, age, style; plus vocation/hometown/demeanor) so the face MATCHES the person AND the text and image never drift (one source of truth for both). See the L28b pipeline-order note (the physical facet is sealed BEFORE portraits are generated). Two tiers: Tier 1 (now, on the seeded facets) already lifts variety; Tier 2 consumes the LLM-authored physical facet. Pairs with L16 (color until evicted) and L17 (look-alike dedup pass).

- **L30 ☑ ENG (prompt/pacing)** *(base-prompt wander-pacing pin shipped — survey, then ONE cluster, then WAIT; ae0f247)* — **The "wander the house" beat fly-through poses questions the player can't answer.** When the player explores/mills around, the GM narrated ALL seven rooms in ONE turn and had ~5 NPCs pose direct questions to the player (a cool-down offer, "scouting the territory?", "best TV pilot, go") then ended with a single global "where do you want to land?" — so the player could not connect or answer *in* any grouping. The **survey is wanted** (clusters + vibes is great orientation), but it must not REPLACE the scene. Pacing fix (momentPrompts lingering/wander beat): give the brief orienting survey, then DROP the player into ONE cluster to actually connect — play it as a real interactive scene where the NPC openers are LIVE invitations and the GM WAITS for the player's reply; never fire multiple unanswerable NPC questions across rooms in a single turn. One grouping at a time for real connection; the survey orients, the scene happens. (Ties to "lingering is play" + "seize the lull, let substantive play run.")

- **L31 ◐ ENG/FE (feature)** *(ENGINE half SHIPPED: the `premiere` moment now drives a BUILT-IN round of introductions (production gathers the house, each voices their PUBLIC persona from their card; the player introduces themselves), a once-introduced intro is fixed, and a hand-held tutorial cadence orients each first beat — then still drives to the first HOH. REMAINING: the FE tutorial-overlay framing.)* — **structured premiere with a guided introductions round (a tutorial).** The premiere should be STRUCTURED and hand-held like a real IRL premiere — and the player-prompted "house meeting / round of introductions" should be a BUILT-IN beat: production gathers the house, and each houseguest introduces their PUBLIC persona (name, where they're from, vocation, "one real thing"). It's the natural place the player meets all 15 NPCs and — ties to L28/L28b — where each NPC's diverse backstory is voiced AND **persisted airtight** (once introduced, that public intro never drifts). Make the premiere a light tutorial: guide the new player through the first beats (meet the house → the first HOH → noms → veto → eviction) with a more hand-held cadence than mid-season. Engine: a `premiere`/introductions sub-beat the moment prompt drives; FE: tutorial framing. Don't make the player prompt for it.
- **L18c ☑ ENG — age floor.** NPC ages are engine-generated `21 + …` (≥21, BB-eligible); locked with a test so it can't regress. (The player authors their own age at OOBE.)

- **L32 ☑ FE — `telescreen` is the default theme out of the box.** *(Shipped: `DEFAULT_THEME = 'telescreen'` in `theme.js` + the first-paint head-script default in `index.html`; an explicit saved choice still wins. Tests: `frontend/tests/test_l32_l33_l34_theme_defaults.py`.)* The first-run / unset-preference theme must be `telescreen` (the on-brand _1984_ surveillance look), not whatever the current default is. Set it as the default in the theme model + the persisted-preference fallback so a brand-new player (and a factory-reset / no-stored-theme session) lands on telescreen, while an explicit saved choice still wins. Update the FE theme default + its boot/restore path; cover with a pytest that an absent stored theme resolves to telescreen.
- **L33 ☑ FE — `frosted` is ON by default on every theme.** *(Shipped: `defaultFrostedFor()` returns ON for every theme (empty opt-out map) in `theme.js`; the head-script defaults frosted ON when unset; an explicit toggle-off persists as `frosted:false` and wins. Tests: same file.)* The "frosted" glass treatment (under Themes → Customize) must default to ENABLED for all themes, not off. New players get the frosted look without opting in; an explicit toggle-off still persists and wins. Flip the default in the customize/frosted preference model + its restore fallback; pytest that an unset frosted-preference resolves to on for each theme.
- **L34 ☑ FE — the frosted title bar is a big OPAQUE rectangle; it must actually look frosted (every window).** *(Shipped at the KIT level: `body.theme-frosted .ow-window` frosts the frame and `body.theme-frosted .ow-titlebar` shares the `.modal-header` translucent+blur rule (transparent fill, no solid `--panel`/`--win-bg`), so every Lane-F window — theme/cast/finale/retrospective/gadget-rail — inherits cohesive glass. The A6 `overflow:hidden` clip that broke L11 corner-resize stays REVERTED (NB comment in `orwellWindow.js`). Tests: `frontend/tests/test_l32_l33_l34_theme_defaults.py` + browser-smoke/responsive-matrix.)* When frosted is on, the window TITLE BAR renders as a solid opaque block at the top instead of the translucent/blurred frosted treatment the body gets — so it reads as a heavy opaque rectangle, not glass. The frosted styling (backdrop-blur + translucent background, not a solid fill) must apply to the **title bar** too, and consistently across **every window/panel** (theme window, cast, finale, retrospective, gadget rail, any Lane-F `OrwellWindow`/`.ow-*` kit window) — not just the theme window. Audit each window chrome's header against the frosted tokens; fix the title-bar background to use the same translucent+blur as the body so the whole window is cohesively frosted. (Ties to the Lane-F windowing kit — fix it at the kit level so new windows inherit it.)

- **L36 ☑ ENG/FE (interaction design — CRITICAL immersion)** *(engine OOC pin `f651fad`; FE input-bubble + `((...))` hint `22dbea8`; and NOW the model-output half — the GM marks its own OOC answers by wrapping the whole reply in `((...))` (a real engine prompt contract) and the FE renders them as a distinct `.msg-ooc-producer` aside. DONE end to end.)* **— channel ambiguity: OOC meta-queries get voiced into the room.** The chat collapses the three channels (in-character diegetic speech to the houseguest in front of you · player-level **OOC** asides to the game/producers · admin) into one box, and the GM **defaults everything to in-character/diegetic** — so an OOC logistics question ("what time is it in-game?") was treated as something the player said **aloud**, and the whole room reacted in that frame. Other moments share this "incorrectly assumed context" failure. **Patch (two parts):** (1) **Model-side intent classification** (momentPrompts / BASE prompt) — a bare query about game **STATE / TIME / RULES / "what are my options" / "who's HOH" / day-count** etc. is an OOC request to the producers/HUD: answer it as a **quiet OOC producer aside or HUD fact, and the HOUSE DOES NOT HEAR OR REACT.** Only clear in-room dialogue or action **directed at a present houseguest** is diegetic. Ambiguous ⇒ a logistics question defaults OOC; speech aimed at a houseguest defaults in-character. (2) An explicit, low-friction **OOC MARKER** the model ALWAYS honors — `((...))` (RP convention) or a leading `ooc:` = always OOC, never voiced — the unambiguous override. Ties to the three-channel model (CLAUDE.md) + the Diary-Room OOC precedent. Implement in `momentPrompts` (sequence AFTER 0058 integration to avoid a file conflict with the 0058 worktree) + a small FE hint surfacing the convention. *(Want the failed example chat to calibrate the classifier boundary.)*
  - **DECIDED (owner, 2026-06-19): smart inference + `((...))` override.** The GM **auto-treats** obvious meta/logistics/state/rules queries as OOC (the house never hears or reacts), AND `((double parens))` / a leading `ooc:` is the **always-OOC override** for anything ambiguous — the marker is a safety valve, not a requirement. So: classifier leans toward OOC for bare logistics questions; in-character is the default for speech/action aimed at a present houseguest; the marker forces OOC unconditionally. FE: a quiet, dismissible hint surfacing the `((...))` convention; render OOC answers as a visually-distinct producer/HUD aside (not a spoken-in-room line).

- **L37 ◐ FE/OPS (incident follow-up 2026-06-19) — `/admin/status` crashed the browser; engine-down log firehose.** During the prod outage (the fastembed warm-up wedged the engine — fixed in `f380657`), the FE polled `/state`,`/status`,`/finale` every ~2s and logged a WARNING on EVERY failed poll, so `ops-panel.log` ballooned and an `/admin/status` tab left open eventually crashed. **Fixed (this batch):** the poll-failure warnings are now **throttled** (`routes/orwell_routes.py` `_warn_throttled`/`_clear_warn`: first failure logs immediately, then ≤1 per 30s per key; a successful poll clears the key so a fresh outage is never silent) — `tests/test_l37_poll_warn_throttle.py`. The viewer DOM is already capped at 2500 lines (`admin_health_routes.py:473`) and the file tail at 64KB/500 lines, so the page is bounded going forward. **Remaining (◐):** rotate/size-cap the on-disk `ops-panel.log` itself, and quiet the per-poll httpx `INFO GET …/models 200` lines that also fill the log.
- **L38 ☑ ENG/FE (feature — admin/dev) — a God-Mode "fast-forward to finale" so the post-season Vault debrief is reachable in dev.** RULING (reaffirms mandate #2): **God Mode must NEVER read the Vault** — `AdminPort` is structurally Vault-free by construction (no `VaultStore` dependency, dependency-cruiser-enforced), and the wall holds for the admin/developer precisely so they can't spoil their own game. The **post-season retrospective (0048 `seasonRetrospective`) is the ONE sanctioned Vault reveal**, gated on a FINISHED season. So the dev need ("inspect MY game's Vault while building") is met not by a Vault-bypass but by a fast, LEGITIMATE way to finish: an **admin/God-Mode `advanceToFinale` / fast-forward lever** that drives the existing deterministic engine (the `advanceGame` loop, auto-resolving the player's pending decisions with legal defaults) to a crowned winner — reading **no Vault** — after which the existing `seasonRetrospective` unseals normally. Preserves integrity absolutely (the season genuinely finished); gives the Vault in seconds; also relieves the "game won't advance" pain. Lives on the **admin channel** (not player). Vault-free ⇒ dependency-cruiser must stay green; sentinel-test that the fast-forward returns no Vault content and that the unseal still only fires post-finale. *(Bonus: a headless dev CLI that plays a full UAT-style season and prints the retrospective — for throwaway fresh-season inspection.)* Own spec (next free number).
- **L39 ☐ ENG/FE (bugs surfaced by the God-Mode/finale transcript).** Three real defects from the same session: **(a) the player "walk out the front door" / quit was NARRATED as done but the engine never processed it** — game state stayed Week 1 with the player ACTIVE, then later turns correctly said "you never actually left" (the narrated-but-not-recorded gap, this time for a player exit). Need a real player walk-out / self-eviction path (ties to 0046 player eviction) OR the GM must refuse and not fabricate an exit. **(b) The season would not ADVANCE** — the model couldn't drive `advanceGame` to make progress ("not a single beat advanced"), then hit step limits trying to speed-run (the progression-stall / tool-under-call family, L18-adjacent; the FE stall-nudge should reliably move the live game, and a dev fast-forward (L38) gives a deterministic path). **(c) "Can I enter God Mode, I am admin" typed in the player chat got ROLE-PLAYED** — houseguests (Faith/Joey/Cole/Bridget) reacted in-character to an OOC/admin request. That request is OUT OF CHARACTER (L36 channel: the house must not hear/react) AND God Mode is an **admin-channel capability**, not a chat phrase — the GM should answer it as a producer/HUD aside ("God Mode is the admin surface, not something you type in here"), never voice the room reacting.
- **L40 ◐ ENG** *(SHIPPED: the moment-prompt restraint (ordinary closeness = friendship/strategy, never romance unless explicitly a pair) + the seeded showmance MODEL core in 0059 (sparse, Vault-sealed, staged spark→…). REMAINING: the organic stage-advancement surfacing scheduler — see 0059.)* — **(behavioral fidelity — surfaced by the 2026-06-19 finale Vault reveal) — showmance OVERLOAD: everyone romanced everyone.** The post-season debrief read like a soap opera with no restraint — "Jalen cycled through Uma, Troy, Megan, Anthony, Ella, Justin, Cole, and Beatriz"; "Cole got close to everyone." Root cause: **there is no actual showmance MODEL**, so the narrator reads every high-affinity relationship edge as a romance → near-total saturation (implausible; a real BB season has 1–3 showmances). Fix = the **deferred §6 sparse, SEEDED showmance**: a *small* number of genuine romantic arcs seeded at cast time (Vault-held, organic, never telegraphed — like L35 pre-game ties, twist-governed), and the GM voices romance ONLY for those, never for ordinary high-affinity bonds. Until that model exists, the moment prompt should also stop the narrator from labeling every close bond a "showmance." (Pairs with L35; both are §4/§6 of the v0 Producer's Vault.)
- **L41 ☑ ENG** *(SHIPPED: the 0058 hidden pools widened (secrets 10→24, goals 8→18, weakness 8→16) + cast-wide `MAX_PER_*` spread caps (mirroring L28's `spreadFacet`) so no secret/goal/weakness/read clusters; 60-seed test.)* — **(diversity — surfaced by the same reveal) — the hidden layer clusters ONE trope.** Cole, Ella, Troy, AND Garrett were each described identically — "seemed naive but was secretly clocking every single move." The hidden layer reuses the same "secret quiet-observer mastermind" archetype across the cast (the hidden-trait version of the L28 voice homogeneity). Fix lives in **0058's deep-profile pools** (`deepProfile.ts` SECRET_POOL / WEAKNESS_POOL / TRUE_GOAL_POOL / the perception pool): make them large + varied AND **cap how many of the cast can share the same secret/weakness/archetype** (mirror the L28 `MAX_PER_*` spread caps), so no single hidden trope dominates a season. The 0058 integration starts this (structured, pooled secrets/goals/weakness); L41 is the spread-cap + pool-breadth pass on top. (NB: the finale game was a PRE-0058 season, so it had the old undiversified hidden layer.)
- **L42 ☑ FE** *(SHIPPED: `orwellBeatOutcome` derives the Vault-free public outcome from the tool result — "🗳️ Troy is evicted (7-1)", "🏆 Maya wins HOH", "🔨 …nominates… " — rendered in both the live (chat.js) and reload (chatRenderer.js) beat paths; behavioral + structural tests.)* — **the inter-message beat markers are too OPAQUE ("PRODUCTION done" / "YOUR MOVE done"): make solidified moments informative.** Live feedback (2026-06-19): the chat's tool-beat markers (`orwellToolBeats.js`) read as a stack of identical "PRODUCTION done" / "YOUR MOVE done" rows that convey nothing about WHAT happened. (Confirmed good: thinking is hidden — L6 holds; the UI is now hiding too much of the SUBSTANCE.) Each solidified beat should carry the actual PUBLIC outcome derived from the tool RESULT / `gameStatus` (all player-witnessed, Vault-free — never hidden state): `advanceGame` that resolved an eviction → "🗳️ Eviction — <name> left 7-1"; an HOH comp → "🏆 HOH — <name>"; nominations → "🔨 Noms — <a> & <b>"; veto used/saved → "💎 Veto — used on <name>"; a recorded social scene → its kind + who. Enrich `orwellToolBeats.js` + the beat render to read the result, not just the tool name; keep it terse + diegetic. (The STACK of repeats specifically was the speed-run; **L38**'s server-side fast-forward removes that stack from the chat — but the genericness applies to normal play too.) SEQUENCE after the L36-FE chat-render agent lands (shared render path).
- **L35 ◐ ENG** *(CORE SHIPPED in 0059 — `seededRelationships.ts` seeds 0–2 sparse, Vault-sealed pre-game ties off the 0025 reserve governance, folds a small standing affinity bias, and persists them; wall + sparseness + persistence tested. REMAINING: the organic pathway-surfacing — discovery only through gameplay.)* — **(feature — approved 2026-06-19, GATED BY THE TWIST RULES) — pre-game relationships.** Seed a few HIDDEN pre-existing connections between houseguests (the v0 Producer's Vault §4 pattern — e.g. two who met at a casting callback and silently agree not to acknowledge each other; two with a mutual friend who bond "like destiny"). **Approved to build, but ONLY under the reserve-twist governance (feature 0025):** (1) **Vault-sealed from the player AND the admin** — the engine generates + seals them; neither the player nor God Mode learns them in advance; (2) **held in reserve / sparing** — not every season has them, a small engine-decided count like the 1–2 twist budget, never a fixed always-on mechanic; (3) **non-structural / format-preserving / never game-breaking** — a pre-game tie is a subtle behavioral seed (warmth, unconscious protectiveness, a destiny-bond), NEVER a deterministic advantage and it never overrides legitimate gameplay or the hard rules (0005); (4) **engine-timed, seeded, organic surfacing** — the tie exists at cast time but is **discovered only through gameplay pathways** (a careful observer notices the warmth; someone slips), **never telegraphed** and **never confirmed by exposition** (reveal-as-event only, like a fired twist). Build it ON / mirroring the 0025 reserve seam so it inherits those guardrails. Its own spec (next free number after 0058). *(Owner deferred the §6 organic-showmance candidate — not approved here.)*

## Windows / gadget rail / layout (cont.)
- **L43 ☑ FE (fixed #345; found by the 2026-06-19 verification pass)** — **the producers'-vault (retrospective) window and the "New season" window COLLIDE/STACK, and the second is pushed off the top of the viewport.** Both register in the SAME slot: `orwellRetrospective.js:67` uses `slot: "bottom-right", slotKey: "retro"` and `orwellNewSeason.js:154-155` uses `slot: "bottom-right", slotKey: "new-season"`. When both are visible (a finished season shows the retrospective AND the "New season" surface), the slot engine stacks them in one column (`restackSlot`, `orwellSlots.js:115-154`). The root defect: the **base-anchor stacking branch is NOT clamped to the viewport.** `restackSlot` accumulates `cursor += h + GAP` per visible panel and writes `bottom = cursor` raw (the no-drag-offset branch, `orwellSlots.js:145-152`); the viewport clamp (`Math.max(4, Math.min(...))`, `orwellSlots.js:138-139`) runs ONLY in the drag-OFFSET branch, never on the base stack. So the second bottom-right panel's accumulated `bottom` offset (≈ first panel height + GAP, with two LARGE windows) pushes its top above the viewport, and because the right-anchor sets `right:14px` on the base path the horizontal calc reads fine but the vertical does not. **Fix direction:** (1) give the two windows DIFFERENT slots (e.g. retrospective → its own slot or default it DOCKED — `orwellRetrospective.js:71` is already `dockable:true, defaultDocked:false`, so flipping `defaultDocked:true` is the cheapest fix and matches the feature-doc lean) AND (2) run the slot stacking offset through the viewport clamp in the base-anchor branch too (clamp `bottom`/derived `top` the same way the drag branch does, `orwellSlots.js:138-139`), so NO stacked panel can ever be pushed off-screen regardless of how many share a slot or how tall they are. Either alone reduces the collision; do both so any future slot-sharing windows are safe.
- **L44 ☑ FE/COPY (ledger-status correction — found by the 2026-06-19 verification pass)** — **L15 and L17 are SHIPPED but the ledger still marked them ☐ open.** Both landed in commit `c763a22` (PR #329, "fix(fe): L15/L17 — cast generation stays responsive + a look-alike dedup pass"). L15: the live per-user generation-progress record + stale-roster guard so a transient engine hiccup never blanks the cast or closes the panel (`src/orwell_portraits.py:61-189` progress record + spaced writes; `static/js/orwellCast.js:76-92` never hides on a non-ok `/state`, `:346-366` consumes `data.generation`/`data.stale` + fast-polls during a run). L17: the post-generation distinctness pass (`src/orwell_portraits.py:83-92, 1197-1262, 1288 find_lookalikes/dedupe_lookalikes` — Jaccard ≥ 0.62 over the Vault-free physical-feature tokens, regenerate offenders with an enforced-variety re-prompt, retry-capped). Status flipped to ☑; update lines 32/34 above on the next ledger pass.
- **L45 ◐ FE/COPY (verification finding — found by the 2026-06-19 pass; refines L20)** — **L20's "no live strip path" verdict is CONFIRMED, but the regression guard only covers `?`.** A full trace of the deepseek stream + sanitize paths (`routes/chat_helpers.py` `_extract_thinking_meta`/`_normalize_thinking` — `.strip()` is whitespace-only; `src/text_helpers.py` `strip_think` — only `<think>`/channel/prompt-echo blocks; `src/agent_loop.py` `_split_complete_sentences`/`_scrub_game_leak` — both PRESERVE the sentence delimiter via lookbehind/lookahead, never consume the trailing char; `static/js/orwellOocAside.js` `detectOocAside` — fires only when the WHOLE message is `((...))`-wrapped; `static/js/markdown.js:625-631` URL autolink — RE-APPENDS the captured trailing punctuation via `${trail}`; the `stream_rewrite` path `routes/chat_routes.py:1518` — `.strip()` whitespace) found **NO** path that strips a trailing `?`/`!`/`.`/`…` from normal narration. The only protection is the regression guard `frontend/tests/test_l6b_l7_l9_l20_chat_render.py:161-217`, which exercises the real live-game scrub + `strip_tool_blocks` round-trip — **but only asserts on a trailing `?`** (`assert scrubbed.endswith("?")`). GAP: extend the guard's fixtures to also assert preservation of a trailing `!`, `.`, and `…` (and a closing quote/paren after them), so the same systemic class the owner re-reported can't regress in via any of the four punctuation marks, not just `?`. (No code defect found to fix; this is a coverage hardening on the only line of defense.)

---
### Triage (suggested order)
1. **Critical immersion/loss:** L6 (thinking, in flight), L15 (generation drops FE), L18 (engine hang), L20 (stripped `?`), L4/L5 (casting hand-off), L1 (logo overlap).
2. **High-value polish:** L7 (empty accordions), L8 (model counter), L3 (accordion default), L14 (first names), L10 (capital H), L21 (whereabouts).
3. **Features/larger:** L11 (resizeable windows), L12 (pin cast), L13 (drag-reorder), L17 (look-alike pass), L16 (color/B&W), L19 (richer personas), L9 (agent/chat switch decision), L2 (OOBE settings button).
