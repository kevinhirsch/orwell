# Feature specs

Drafted, stack-agnostic feature definitions for the `orwell` build. Each feature is authored
here **before** implementation and handed to an implementer (a separate Claude Code
instance). The author drafts; the implementer builds to green and refactors.

## Convention

Each feature is two files, numbered by **build priority** (lower = built first):

| File | Role |
|---|---|
| `NNNN-<slug>.md` | **Design note** — context, scope, the port/tool contracts involved, test strategy, acceptance criteria, and an implementer handoff with open questions. |
| `NNNN-<slug>.feature` | **Executable spec** — Gherkin scenarios. Failing-first; the implementer makes them pass. |

## Rules these specs must hold to

- **BDD/TDD-first.** The `.feature` file is written to fail, then implemented to green
  (`docs/CLAUDE_CODE_INSTRUCTIONS.md` §9, `docs/bb-sim-spec.md` §12).
- **Roles only, never names.** No houseguest or player names appear in scenarios — only
  roles (player, admin, NPC, HOH, nominee, evictee, veto winner). Fixtures are **generated**;
  sample-save content is format-only and is never used as seed/test data.
- **Definition of done** (per `CLAUDE_CODE_INSTRUCTIONS.md` §11): a passing name-agnostic
  feature test; Vault isolation verified on the relevant player **and** admin surfaces;
  domain logic unit-tested with a **seeded** randomness source; no Vault read reachable from
  any player- or admin-facing adapter/tool.

## Build priority order

From `docs/CLAUDE_CODE_INSTRUCTIONS.md` §9. Draft and build in this order:

1. **Vault isolation (incl. God Mode)** — `0001-vault-wall-isolation`
2. Event visibility & propagation
3. Behavioral fidelity
4. Replayability / naming
5. Competition eligibility
6. Outcomes by stats + temperature
7. Persistence non-degradation
8. Daily-event invariant

## Status legend

The **[index](#index)** below is the single source of truth for per-feature status — the prose in
`CLAUDE.md` and the audit ledgers can drift, so the index is reconciled against the actual code.

| Token | Meaning |
|---|---|
| ✅ **Built** | Shipped and gated (see the **Gate** column). |
| 🚧 **Built · follow-on owed** | Shipped, with one named remaining item (host smoke, browser render, an FE chunk…). |
| 📝 **Spec only** | Authored, not yet built. |
| 🟢 **Build-ready** | PO-reviewed and resolved, not yet implemented (a stronger commitment than plain "spec only" — cleared for the next build slot). |
| ❄️ **Frozen** | Reviewed and explicitly parked (owner ruling), preserved not deleted, reopenable. |
| ⏸ **Deferred** | Intentionally not built. |

**Gate** — how the feature is verified: `BDD` (Cucumber, listed in `cucumber.cjs`) · `unit` (Vitest) ·
`FE` (front-end pytest) · `engine` (Vitest, no `.feature`) · `scripts` (deploy) · `—` (none yet).

> **Audit (2026-06-20), scope 0001–0063 only.** Every feature **0001–0063** was cross-checked against
> its source artifact (not its prose) as of that date; **no orphaned or untracked unbuilt specs** in
> that range. *(0022 was **removed** in the 2026-06-28 PO review — see the index row; its goals were
> superseded by 0020/0051/0054 under ADR 0003.)* **0064** (live multi-device game sync) shipped its
> **stopgap** (the canonical game session — every device on one chat) 2026-06-20; the Messenger-style
> sync + window/HUD layout sync are the owed follow-on. **0062** (the move-in zeitgeist snapshot)
> **shipped 2026-06-20** — BDD-gated, frozen + seed-reproducible + flavor-only (outcome-invariant).
> **0064–0112 (added since 2026-06-20) have had spot reconciliation passes (most recently the
> 2026-07-06 closure session, which built the last three spec-only files — 0094 scoped/0095/0096 —
> and shipped 0101/0102, following the 2026-07-05 doc-drift audit-closure — see
> [the doc-drift lane](../audits/2026-07-03-final-pre-ship-audit/lanes/doc-drift.md) and
> [the 2026-07-06 closure-session summary](../audits/2026-07-06-closure-session.md))
> but not this same exhaustive per-row sweep** — the individual row prose is the source of truth for
> that range (0094–0096/0097/0098/0101/0102/0103/0108's own rows are current as of 2026-07-06).
> Tracked here and in the authoritative
> [open-items ledger](../audits/2026-06-10-full-product-audit.md).

## Index

| # | Feature | Gate | Status |
|---|---|---|---|
| 0001 | [Vault Wall isolation](./0001-vault-wall-isolation.md) | BDD | ✅ Built |
| 0002 | [Event visibility & propagation](./0002-event-visibility-and-propagation.md) | BDD | ✅ Built |
| 0003 | [Behavioral fidelity](./0003-behavioral-fidelity.md) | BDD | ✅ Built |
| 0004 | [Replayability & naming](./0004-replayability-and-naming.md) | BDD | ✅ Built · amended (E38/D8: real-name corpora + entropy seed) |
| 0005 | [Competition eligibility](./0005-competition-eligibility.md) | BDD | ✅ Built |
| 0006 | [Outcomes by stats + temperature](./0006-outcomes-by-stats-and-temperature.md) | BDD | ✅ Built |
| 0007 | [Persistence non-degradation](./0007-persistence-non-degradation.md) | BDD | ✅ Built |
| 0008 | [Daily-event invariant](./0008-daily-event-invariant.md) | BDD | ✅ Built |
| 0009 | [MCP tool boundary](./0009-mcp-tool-boundary.md) | BDD | ✅ Built |
| 0010 | [One-liner deployment & update](./0010-deployment-one-liner.md) | scripts | 🚧 Built · real-Proxmox-host smoke owed |
| 0011 | [Weekly loop orchestration](./0011-weekly-loop-orchestration.md) | BDD | ✅ Built |
| 0012 | [Conversation & scene system](./0012-conversation-and-scene-system.md) | BDD | ✅ Built |
| 0013 | [The Diary Room](./0013-diary-room.md) | BDD | ✅ Built |
| 0014 | [Jury & endgame](./0014-jury-and-endgame.md) | BDD | ✅ Built |
| 0015 | [Character creation (OOBE)](./0015-character-creation-oobe.md) | BDD | ✅ Built |
| 0016 | [God Mode (admin port)](./0016-god-mode-admin.md) | BDD | ✅ Built |
| 0017 | [Relationship model](./0017-relationship-model.md) | BDD | ✅ Built |
| 0018 | [Narrative & moment orchestration](./0018-narrative-moment-orchestration.md) | BDD | ✅ Built |
| 0019 | [Agent-driven play loop](./0019-agent-driven-play-loop.md) | BDD | ✅ Built |
| 0020 | [Player experience (status, decisions, portraits)](./0020-player-experience.md) | BDD | ✅ Built |
| 0021 | [Game session & save lifecycle (per-user sandboxes)](./0021-game-session-and-save-lifecycle.md) | BDD | ✅ Built |
| 0022 | Player experience MVP-2 (rich game UI) | — | 🗑️ **Removed** (PO review 2026-06-28) — superseded by 0020/0051/0054 under ADR 0003; the dashboard direction was deliberately not taken |
| 0023 | [Consequence & memory (the MVP-1 backbone)](./0023-consequence-and-memory.md) | BDD | ✅ Built |
| 0024 | [Soul storage & memory recall (md + vector)](./0024-soul-storage-and-memory-recall.md) | BDD | ✅ Built |
| 0025 | [Reserve twists (Vault-sealed, engine-timed)](./0025-reserve-twists.md) | BDD | ✅ Built · **reactive redesign 2026-07-06** (all 3 twists live, standing pool, dynamic triggers; `ORWELL_REACTIVE_TWISTS`) |
| 0026 | [Relationship math](./0026-relationship-math.md) | BDD | ✅ Built |
| 0027 | [NarrativePort LLM adapter](./0027-narrative-port-llm-adapter.md) | BDD | ✅ Built |
| 0028 | [Temperature & emotional-modifier constants](./0028-temperature-and-emotional-constants.md) | BDD | ✅ Built |
| 0029 | [App administrator role & user management](./0029-app-admin-and-user-management.md) | FE | ✅ Built |
| 0030 | [Durable game persistence (survive restart)](./0030-durable-game-persistence-survive-restart.md) | BDD | ✅ Built |
| 0031 | [Game orchestrator & integrity checkpoint](./0031-game-orchestrator-and-integrity-watcher.md) | BDD | ✅ Built · turn-driven only (real-time purge 2026-07-10) |
| 0032 | [Front-end surface reduction (the "game build")](./0032-frontend-surface-reduction-game-build.md) | FE | ✅ Built |
| 0033 | [Dynamic player tagline](./0033-dynamic-player-tagline.md) | unit | ✅ Built · recorded deviation E87a |
| 0034 | [Live weekly progression & decision seam](./0034-live-weekly-progression-and-decision-seam.md) | BDD | ✅ Built |
| 0035 | Live off-screen life (running watcher) | — | 🗑️ **Removed** 2026-07-10 (real-time purge, PO ruling) — the wall-clock watcher is deleted; the house lives only on the player's play-clock (0031). |
| 0036 | [Live social surface (approaches + Diary Room)](./0036-live-social-surface-approaches-and-diary-room.md) | unit | ✅ Built · recorded deviation E87a · **DR extended by 0115** (strategic confessional) |
| 0037 | [Live jury-vote choreography (the finale)](./0037-live-jury-vote-choreography.md) | BDD | ✅ Built · + finale UI |
| 0038 | [Live off-screen society](./0038-live-offscreen-society.md) | BDD | ✅ Built |
| 0039 | [Promise & deal tracking](./0039-promise-and-deal-tracking.md) | BDD | ✅ Built |
| 0040 | [NPC confessionals](./0040-npc-confessionals.md) | BDD | ✅ Built |
| 0041 | [Character evolution & arc (the linchpin)](./0041-character-evolution-and-arc.md) | BDD | ✅ Built |
| 0042 | [Competition library](./0042-competition-library.md) | BDD | ✅ Built |
| 0043 | [Emergent bloc behavior](./0043-emergent-bloc-behavior.md) | BDD | ✅ Built · amended (PO 2026-07-13: removed the 5-person size cap — clique requirement is the only limiter; juryReach re-confirmed in band) |
| 0044 | [Strategic nomination & vote refinements](./0044-strategic-nomination-and-vote-refinements.md) | BDD | ✅ Built |
| 0045 | [Endgame structure (Final 5 → Final 2)](./0045-endgame-structure.md) | BDD | ✅ Built |
| 0046 | [Player eviction & the juror's seat](./0046-player-eviction-and-jury.md) | BDD | ✅ Built |
| 0047 | [Eviction night live (reveal + goodbyes)](./0047-eviction-night-live.md) | BDD | ✅ Built |
| 0048 | [Season retrospective & the Vault unsealing](./0048-season-retrospective-and-unsealing.md) | BDD | ✅ Built |
| 0049 | [House presence & lingering play](./0049-house-presence-and-lingering.md) | BDD | ✅ Built |
| 0050 | [The casting interview](./0050-casting-interview.md) | BDD | ✅ Built |
| 0051 | [In-character images](./0051-in-character-images.md) | unit + FE | ✅ Built · browser-smoke render landed (`browser_smoke.py:1166` asserts a real sized portrait `<img>` + zero placeholders) |
| 0052 | House themes (five seasons) | FE | ✅ Built · shipped FE-side, no standalone spec file |
| 0053 | [Admin transcript retrieval](./0053-admin-transcripts.md) | FE | ✅ Built |
| 0054 | [Control-room gadget rail](./0054-gadget-rail.md) | FE | ✅ Built · Phase 1 (HUD rail) + Phase 2 (dock windows) |
| 0055 | [Social play moves the weights](./0055-social-play-weights.md) | FE | ✅ Built · the `_auto_record_scene` guarantee |
| 0056 | [Season-to-season character continuity](./0056-season-character-continuity.md) | unit + FE | ✅ Built |
| 0057 | [Seasons as levels](./0057-seasons-as-levels.md) | FE | ✅ Built · live browser validation landed (`browser_smoke.py:1197` validates the season-progress bar + "Season N" chip + new-season window) |
| 0058 | [Deep character profiles](./0058-deep-character-profiles.md) | BDD | ✅ Built · Phase 1 + Phase 2 |
| 0059 | [Hidden seeded relationships](./0059-hidden-seeded-relationships.md) | engine | ✅ Built · orientation-aware via 0063 · §5 organic stage-advancement surfacing scheduler now built (issue #605): showmance arc spark→bond→visible (`advanceShowmances`) + the pre-game-TIE discovery scheduler (`advanceSeededTies`, opt-in `ORWELL_SEEDED_TIE_SURFACING`, default OFF) — pathway-only (0002) + the 0023 discovery fold, sealed off player AND admin, monotonic/persistent. Calibration-neutral when off (proved byte-identical vs. main; `tests/unit/seededTieSurfacing.test.ts`) |
| 0060 | [Story-thread scheduler (0058 Phase 2)](./0060-story-thread-scheduler.md) | BDD | ✅ Built |
| 0061 | [Player self-eviction](./0061-player-self-eviction.md) | BDD | ✅ Built |
| 0062 | [Move-in zeitgeist snapshot](./0062-world-snapshot-zeitgeist.md) | BDD + FE | ✅ Built · `src/engine/zeitgeist.ts` (frozen + seed-reproducible + flavor-only) **+ the FE `web_search` capture lane** (`frontend/src/orwell_zeitgeist.py`) writing the real move-in zeitgeist back via `recordWorldSnapshot` |
| 0063 | [Casting diversity floor](./0063-casting-diversity-floor.md) | BDD | ✅ Built |
| 0064 | [Live multi-device game sync](./0064-live-multi-device-game-sync.md) | FE | ✅ Built · canonical session (A) + Messenger serialization (C) + window/HUD layout sync (F) + `game-updated`/cast-photo/once-only (B/D) |
| 0065 | [Cast pre-warm: deep-author before portraits](./0065-cast-prewarm.md) | BDD + FE | ✅ Built · wired the `recordCastProfile` write-back onto the player channel (was rejected at the MCP boundary) + `preSeedCast` pre-game cast + `createCharacter` adoption + FE author-warm-early / portrait-warm-gated |
| 0066 | [In-game time of day & the nightly sleep economy](./0066-in-game-time-and-sleep.md) | unit + integration | ✅ Built (ADR 0006) · the five-phase clock + character-driven bedtimes + the diegetic bound + the player's `turnIn` bedtime lever + a hidden bounded sleep→comp penalty + the HUD clock/rest cue. **Opt-in** (`ORWELL_TIME_OF_DAY`, default off) so the seeded calibration spine is byte-identical. Gate: `tests/unit/timeOfDay*.test.ts` + `sleepCompetition.test.ts` (recorded deviation, like 0033/0036/0055) |
| 0067 | [Public internet exposure & internet-grade hardening (hiorwell.com)](./0067-public-internet-exposure.md) | FE + scripts | 🚧 Built · follow-on owed (host smoke) (ADR 0007) · the hardening floor — FE `ORWELL_BIND_HOST` loopback default + `--proxy-headers`, the fail-closed public-profile boot guard, `TrustedHostMiddleware`, the real-client-IP login-throttle fix behind the tunnel — + a `deploy/expose/` kit (**Cloudflare Tunnel + Access** chosen; Pangolin / Caddy documented) + the INSTALL public-deployment runbook. Engine **unchanged**; Vault Wall + 0021 isolation untouched. Gate: `frontend/tests/test_public_profile_guard.py` · `test_trusted_host.py` · `test_login_throttle.py` · `test_public_deploy_config.py`. Owed: real-host smoke (folds into 0010) |
| 0068 | [Admin "Public deployment / Connect to the internet" (UI-driven exposure)](./0068-admin-public-deployment.md) | FE + scripts | 🚧 Built · follow-on owed (host smoke) (ADR 0007; drives 0067) · an admin Settings→System panel + a token-paste **Connect** wizard using a Cloudflare **remotely-managed** tunnel, applied through the existing privileged **ops** mechanism (flag→root oneshot→`orwell-ops-public-deployment.sh`: `.env` upsert + cloudflared install/run + FE restart + token shred) with live tunnel/URL status. Routes/UI/deploy built; request-time fail-closed validation reuses `assert_public_profile_safe`; token write-only. Gate: `test_public_deployment_{routes,ui,ops}.py`. Engine **unchanged**; Cloudflare-account steps stay dashboard-side. Owed: real-host smoke (folds into 0010) |
| 0069 | [Token economy: metered LLM boundary, reasoning budget & non-degrading context tiering](./0069-token-economy-and-context-budget.md) | FE | ✅ Built (ADR 0010) · A: meter the one LLM boundary (full usage envelope incl. cached/reasoning/cost + context-%, Vault-free, admin-only `/api/admin/token-economy` + soft spend-alert) · B: token **policy per call class**, `reasoning` budget now SENT provider-aware (admin-editable `reasoning_budget`) · C: per-session stickiness + opt-in high-token provider-pin · D: opt-in non-degrading context tiering (escalate before lossy compaction). FE/adapter-only; engine/closed set untouched; player never sees a number. Gate: `frontend/tests/test_adr0010_*.py`, full FE suite green |
| 0070 | [Off-screen society texture enrichment](./0070-offscreen-texture-enrichment.md) | Engine + FE | ✅ Built (#507 engine seam + #511 FE live-wiring) · enrich the **prose** of already-recorded **hidden** off-screen scenes with model-voiced texture via a new FE-driven write-back `recordOffscreenSceneTexture`, fanned out in parallel (the hermes `delegate_tool.py` subagent **pattern**, MIT © Nous Research — pattern only). Enrichment lands *downstream* of the Vault boundary (event already hidden); reaches the player only via existing overhear/gossip pathways. Engine keeps the entire closed set (ADR 0005); no texture ⇒ byte-identical fold (`expressiveNonCollapse`). Mandate #1 fidelity win. Gates: `tests/unit/offscreenTextureBoundary.test.ts` (engine four-place) + `frontend/tests/test_offscreen_texture_live.py` (FE live-fire) + BDD `0070-offscreen-texture-enrichment.feature` |
| 0071 | [Defensive hardening: log redaction & URL/path guards](./0071-defensive-hardening-redaction-url-guards.md) | FE | ✅ Built (#505) · adapt `agent/redact.py` (broader secret masking on the FE log path) + the gateway SSRF / path-traversal URL guards (fail-closed). Mandate-neutral defense-in-depth; no Vault surface. Attribution: Nous Research MIT. Gates: `frontend/tests/test_secret_redaction.py` + `test_url_safety.py` + BDD `0071-defensive-hardening-redaction-url-guards.feature` |
| 0072 | [Multi-platform gateway: play your game from messaging platforms](./0072-multi-platform-gateway.md) | FE | ✅ Built (#504) · a new non-Vault player adapter (`frontend/gateway/`) over the same Vault-free MCP player channel: per-platform adapter+registry + `pairing.py` identity binding (one human ⇒ one game across platforms, 0021) + a **server-side reasoning/operator-aside scrub chokepoint** (the accordion-less-transport safety) + decision-card text degradation + `ORWELL_GAME_BUILD` tool-gating. Adapts hermes `gateway/` (MIT © Nous Research). Coordinates with ADR 0007 / 0064. Gates: `frontend/tests/test_gateway_{pairing,scrub,isolation,build_gate}.py` (63 tests) + BDD `0072-multi-platform-gateway.feature` |
| 0073 | [Structural anti-sycophancy wall: game-build boundary is a CI gate](./0073-structural-anti-sycophancy-wall.md) | FE tests + CI | ✅ Built (#506; BDD compose-fix #510) · makes the `ORWELL_GAME_BUILD` game-build wall **structurally proven in CI**, closing the asymmetry with the Vault Wall (compile-time via dependency-cruiser). A new `frontend/tests/test_game_build_wall.py` gate — env-pinned (forces game build on internally), covering set purity, context-injection zeroing, JS-strip completeness, and HTTP-level 404 for every drop-set route. Wired into the CI frontend job as a required step. Gates: `frontend/tests/test_game_build_wall.py` + BDD `0073-structural-anti-sycophancy-wall.feature` |
| 0074 | [Local & tunable HTTPS (trusted on the LAN, with or without a domain)](./0074-local-and-tunable-https.md) | FE + scripts | ✅ Built (2026-06-22; ADR 0014) · a Caddy TLS terminator in front of the FE, driven by tunable `ORWELL_TLS_*` vars from the `orwell https` control-panel action **and** the in-app Admin "Local HTTPS" card (the 0068 ops-flag seam: `orwell-ops-tls.*` → `orwell-https.sh`). Layered trust — offline internal-CA floor (`/orwell-local-ca.crt` download → no warning after one install) + optional publicly-trusted DNS-01 upgrade (zero per-device install). Engine never named. Gates `frontend/tests/test_{tls_routes,local_tls_guard,local_ca_download}.py` + `tests/unit/tlsArtifacts.test.ts` |
| 0075 | [Trust-gated confidences](./0075-trust-gated-confidences.md) | BDD + unit | 🟢 Built (2026-06-24; BDD-wired + passive lie-catch 2026-06-27) · the player-driven `confide` lever ships end-to-end — a houseguest confides a real secret only when they have a *reason* (bond/favor/debt), with a graduated disclosure ladder, and a manipulative houseguest may plant a false confidence (capped) — **plus the passive lie-catch** (a later truthful pathway flips the player's belief to the truth + folds a betrayal-grade blow on the player→liar edge). Vault Wall held structurally (the engine decides + records every disclosure; the model only voices). Gate: `confidence.test.ts` + `confideBoundary.test.ts` (MCP-boundary + Vault-wall + lie-catch) + BDD `0075-trust-gated-confidences.feature` (in `cucumber.cjs`, `confidence.steps.ts`). Opt-in by construction ⇒ calibration byte-identical. Fast-follow SHIPPED: the FE confide-under-call guardrail (PR #1088, `frontend/tests/test_0075_confide_guardrail.py`). |
| 0076 | [Presence grounding & motivated movement](./0076-presence-grounding-and-motivated-movement.md) | unit + FE | 🚧 Built (PR #515) · the narrator stops inventing the room: engine "present company holds the scene" churn fix (`PRESENCE.companionMoveProb`, calibration-safe on the weighted player-facing pass only), the per-turn occupancy feed, FE narrated-departures cue, and the presence/identity desync guard. Gates: `tests/unit/presence.test.ts` + `frontend/tests/test_0076_presence_{movement,desync}.py`. The 0077 floor-plan it was waiting on has since landed (BDD-wired in `cucumber.cjs`); 0076 itself is still not wired into `cucumber.cjs` — its coverage is unit + FE only. |
| 0077 | [House map, privacy & eyeshot](./0077-house-map-privacy-and-eyeshot.md) | unit + BDD + FE | 🟢 **Built (phased).** Phase 1 (the recent-BB 13-room floor plan + movement adjacency) **merged** (PR #772). Phase 2a (the **SIGHTLINE graph** — eyeshot < adjacency; `HOUSE_SIGHTLINE`/`areVisible`/`isPrivateRoom`) **merged** (PR #786). Phase 2b **built 2026-06-25**: `whereabouts()` returns `{present, nearby(=visible), tracked}` — the **tracked** closed-door belief layer (acquired by watching a doorway; pathway + confidence + age-derived `stale`; persisted 0007), the **conspicuousness** read ("two alone too long"; derived per-read; who/where/how-long, never content; diffuses NPC-to-NPC), **sub-zone** earshot (`ROOM_ZONES`; co-presence witnessing zone-scoped, eyeshot room-wide) + an opt-in closed-door overhear muffle. Every new behavior is opt-in/read-side ⇒ the `juryReach`/gradient spine is byte-identical (`privacyEyeshot0077.test.ts` draw-count guard). **NPC-side increment (2026-06-25):** the house now **whispers** — a conspicuous NPC pairing in a private room starts a Vault-free position rumor (`src/engine/houseSuspicion.ts`) that diffuses NPC-to-NPC and can reach the player, on a **dedicated rng with no relationship fold** ⇒ still calibration byte-identical (gated by `houseSuspicion.test.ts` + the green calibration sims). Gates: `sightline0077`/`privacyEyeshot0077`/`houseSuspicion` + BDD `0077-…feature` (wired into `cucumber.cjs`) + the L26 FE gadget. Sibling of 0076. |
| 0078 | [Motivated off-screen society & intentional movement](./0078-motivated-society-and-intentional-movement.md) | unit + calibration + BDD | 🟢 **Built 2026-06-25 (Phase 1 + Phase 2 — feature complete).** **Phase 1 (intentional movement):** co-presence is **goal-driven** — NPCs move toward what their motivation points at (`MOVEMENT_INTENT` + `tanh`-bounded room steering + unsatisfied-pursuit move-gate in `assignRooms`, wired off the relationship model), so "who's in a room together" reflects agenda not luck. **Phase 2 (nature clarity):** the scene's nature sets its fold — `natureFoldImpact` folds a **friendly** scene (`{bonding, showmance}`) **affinity-only** (no strategic move, no vote-affecting change) and a **game** scene byte-identically (the spine; same jitter draws). Intent draws no rng of its own but is DELIBERATELY calibration-load-bearing (owner ruling: location must affect play); combined **calibration re-pass** re-tuned `moveIntentStrength 0.2 → 0.15` (the friendly fold reduced society trust), at which **`juryReach` + gradient hold green** (active ≥ passive). Gate: `intentionalMovement0078.test.ts` + `natureClarity0078.test.ts` + the calibration sims + BDD `0078-…feature` (8 scenarios, wired into `cucumber.cjs`; the anti-sycophancy band is enforced authoritatively in the heavy sims, proved wired here per the 0073 structural pattern). Sibling of 0077. |
| 0079 | [Runtime loop overseer & diagnostic log](./0079-runtime-overseer-and-diagnostic-log.md) | FE | ✅ Built (PRs #717, #741) · the OVERSEER ring + admin "Overseer (live)" source + gap-repair/pacing belt wiring; `OverseerPort` + `DeterministicOverseer` + `LlmOverseer` + the sparse `should_assess` gate; the live hook (opt-in, default OFF, fail-soft) drives the **reasoning tier** (utility-model diagnosis → deterministic fallback); admin Settings toggle. Diagnose-and-LOG only (the inline guardrails are the hands). Gate: `frontend/tests/test_0079_overseer*.py`. |
| 0080 | [Active overseer (reasoning tier as the primary actor)](./0080-active-overseer.md) | FE | ✅ Built · follow-on to 0079 — the overseer **acts on its verdict** (verdict-driven corrections routed through the existing tuned action paths) with the deterministic guardrails demoted to the **fail-soft floor**. A 3-state mode (off/shadow/active — `overseer_mode` in `frontend/src/settings.py`), inline relocation, **trigger-only** (the engine still owns every outcome), **live-only** (seeded lanes unchanged, no re-baseline). Live in `frontend/src/agent_loop.py`'s "0080 ACTIVE OVERSEER" block (Model D: primary + floor), gated on `overseer_mode()=="active"`; default off. Gates: `frontend/tests/test_0080_active_overseer.py` + `test_0080_overseer_mode_dispatch.py`; FE pytest, not in `cucumber.cjs`. |
| 0081 | [Narration-faithfulness gate (the overseer's 2nd role)](./0081-narration-faithfulness-gate.md) | FE | ✅ Built (PR #778, P1–P6) · checks the narration is faithful to engine truth (board / persona / leak / omission). **Hybrid** detection: the deterministic 0065 guard flags **claim-bearing turns**; an LLM `FaithfulnessJudge` runs only on those, post-stream. Correction **split on ADR 0005** — an **open-set** slip is *adopted* canonical (via the 0055 record machinery, tagged O3), a **closed-set** slip is *reframed* / re-grounded next turn (the outcome is **never** bent; the wall is `adopt ⇒ open-set`, enforced at parse **and** integration); un-reframable ⇒ configurable `{reground,log-only,visible}` (default reground). Covers the in-game loop **and** the casting/premiere/preview junctions (subsumes 0082 — premiere/preview ride the in-game hook, casting has its own seam + context-aware judge). Its **own** off/shadow/active dial (`faithfulness_mode`), independent of the 0080 pacing dial; live-only, fail-soft, default **OFF**. Owner-ruled 2026-06-24 (boundary held — `adopt` open / `reframe` closed, never bend a consequential outcome). Gates: `frontend/tests/test_0081_*.py` (58); FE pytest, not in `cucumber.cjs`. |
| 0084 | [Character voice & grounded mood](./0084-character-voice-and-mood.md) | BDD | ✅ Built (2026-06-25) · sixteen mouths, not one — a byte-stable PUBLIC `voice` fingerprint on `CHARACTER` (register/rhythm/energy/directness/humor/stress-tell + a prose signature), generated per NPC and voiced every turn, plus a Vault-safe `mood` derived from the live soul (`emotionalState`/`volatility`) so affect is **queried from truth, not invented** (fixes the "invent moods freely" anti-sycophancy soft-leak). Voice is **static identity** (never drifts); mood carries the dynamism on **two timescales** — the acute spike *and* a **marinated baseline** house dynamics shift over the ~3-month season (0041). Mandates: behavioral fidelity #1, non-degradation (voice never drifts), anti-sycophancy (grounded mood), Vault-safe (observable affect only, never the cause/number). Design note + `.feature`; not yet in `cucumber.cjs`. Sibling of 0085. |
| 0085 | [NPC campaigns & the scramble](./0085-npc-campaigns-and-the-scramble.md) | BDD | ✅ Built (2026-06-25, phased A/B1/B2/C) · strategy executed over time — a hidden, persistent `Campaign` (goal/target/plan/progress/horizon/status) per NPC or bloc, advancing one motivated move per off-screen tick (lobby/plant/deal/position/throw), **adapting** when the board changes, and tilting the **seeded** noms/veto/votes (0044) so campaigning actually changes who goes home (engine tallies; narration voices — anti-sycophancy). The tilt is **character-mediated** — `persuasiveness(owner) × susceptibility(listener) × trust`, new static CHARACTER aptitudes — not a flat knob. Vault-sealed with **symmetric perspective** — a campaign is its owner's private knowledge; every OTHER houseguest *and* the player learns of one only through the same pathway (lobbying, gossip, conspicuousness 0077, contradictions), and NPCs are never omniscient about each other's plans (they act on their own belief, the engine reads the master ledger only to tally). The **scramble** is campaigns colliding (hidden true count vs. stated). **Player offense, Phase 2 (2026-07-05):** declaring a campaign target stays pure player-knowledge/never mind-control, but the player's own LANDED third-party pitches (`recordInteraction`'s `aboutEdges`, Phase 1 of "the player can play offense") now earn a real, bounded campaign the player owns — `advancePlayerCampaign` in `src/engine/campaigns.ts`, folded via `GameSessionAdapter.foldPlayerCampaignMove` — feeding the SAME `campaignTiltFor` mechanism an NPC's campaign uses, throttled to one progress-earning pitch per beat (NPC-cadence parity) and bounded to the identical `CAMPAIGN.base` ceiling. Fixes the audit finding that `declarePlayerCampaign` was dead code with no mechanical effect. Persists + accumulates (non-degradation). Design note + `.feature`; not yet in `cucumber.cjs`. Sibling of 0084. |
| 0086 | [Houseguest drives](./0086-houseguest-drives.md) | BDD | ✅ Built (2026-06-25) · **everyone always plays, intensity varies** — every houseguest always carries a hidden `Drive` (motivation `self-preserve`/`target`/`build`/`lay-low`/`win-power` + an intensity scaled by the board + archetype aggression + soul). `deriveDrive` is **sticky** (holds a target across a stable board, re-aims on a real board change) and **perspective-bound** (from the owner's own reads). Three tiers of one spectrum: a **low `target` lean** adds a small bounded term to the **owner's OWN ballot only** (`ownBallotLean`, never the electorate); the **mid/high** tiers are the 0085 `Campaign` lobbying/tilt — exactly one tier, never additive. Only `target` drives touch the vote (`self-preserve`/`lay-low`/`build`/`win-power` are behavioral). Behind `ORWELL_CAMPAIGNS` (default off) on the dedicated rng ⇒ `juryReach`/UAT byte-identical when off; persisted in `sessionSnapshot.drives`; never a number to the player. Anchors the diagnostic's thin mood/suspicion layers (a drive gives a houseguest something to be on-edge *about* and someone to suspect). Engine + BDD `0086-houseguest-drives.feature` (wired in `cucumber.cjs`). Sibling of 0085. |
| 0087 | [Relationship trajectories (warming/cooling arcs)](./0087-relationship-trajectories.md) | BDD | ✅ Built (2026-06-27) · each directed relationship carries a hidden, engine-owned **momentum** (`{phase: warming/cooling/souring/steady, momentum}`) derived from its recent folded history (`deriveTrajectory`, pure/no-rng; a betrayal spikes souring, an unfed arc `decayTrajectory`s toward steady at the 0026 neglect cadence). `tiltNatureWeights` RE-WEIGHTS the existing off-screen nature `weightedPick` toward continuing the arc — bounded by a tunable cap so off-arc beats stay possible and the **0078 betrayal gate is never bypassed** (a gated-to-zero nature stays zero). A friendship visibly **curdles** over weeks instead of bond→clash→bond whiplash. Dedicated `ORWELL_TRAJECTORIES` flag (default OFF) ⇒ the tilt is never wired, `natureWeights` is the identity, and `juryReach`/UAT are **byte-identical**; ON, the tilt adds **zero rng** (re-weights the SAME single draw) so the off-screen stream's draw count/order is unchanged and the seeded competition/vote spine reading it stays in phase (`trajectoryOutcomeNeutral` is the gate). Vault-class hidden state — `phase`/`momentum` appear on NO player- or admin-facing projection (proven by byte-identity of every projection on vs off), reaching the player only as the KINDS of scenes the arc produces. Perspective-bound (directed `A→B` from A's own folds) and persisted in `sessionSnapshot.trajectories`/`trajectoryFolds` (resumes mid-curdle; a pre-0087 save loads at steady). `src/engine/trajectory.ts` + `trajectoryConstants.ts`; engine + BDD `0087-relationship-trajectories.feature` (wired in `cucumber.cjs`). Sibling of 0086. |
| 0088 | [Living "current read of you"](./0088-living-current-read.md) | BDD | ✅ Built (Vitest + cucumber + arch) · a living per-NPC current read of the player derived from the already-evolving hidden NPC→player edge, distinct from the frozen `dayOnePerception`; surfaced behavior-only as a Vault-safe carriage cue (sibling of 0084 `mood`), never a number. Pure read (no rng) ⇒ calibration byte-identical; absent field back-compatible. Design note + `.feature` (in `cucumber.cjs`). |
| 0089 | [Reactive confessionals](./0089-reactive-confessionals.md) | BDD | ✅ Built (2026-06-27) · confessionals react to concrete recent events the confessor witnessed ("after the veto ceremony…") instead of static target/ally readouts — the engine selects the facts (from the confessor's own witness set), the model only voices them (anti-sycophancy). Additive & byte-identical to 0040 when no events supplied; stays Vault-only NPC content. Engine + BDD `0089-reactive-confessionals.feature` (wired in `cucumber.cjs`). |
| 0090 | [Per-archetype voice (0084 extension)](./0090-per-archetype-voice.md) | unit | ✅ Built (2026-06-27; Tracks #868) · per-archetype `signature`/`lexicon` pools + harder dial anchoring (`BIAS_STRENGTH` 0.55→0.8) in `src/engine/voice.ts` make `generateVoice` genuinely distinctive per archetype, and the shipped 0084 `VoiceProfile` is THREADED through the engine-authored confessional prose (`src/engine/confessionals.ts` voice-keyed CURT/EXPANSIVE/MEASURED parallel target/ally pools + a deterministic habitual-filler weave for warm voices, selected by the public dials; wired in `GameSessionAdapter` `ctxFor`). Voice changes phrasing, never a fact (same engine-grounded target/ally) — closing the sameness the audit found (coordinates #850/#854). Vault-safe (voice is public, encodes no hidden state); deterministic from seed; ABSENT voice ⇒ byte-identical to the 0040 templated fallback ⇒ `juryReach`/UAT calibration byte-identical (voice rides the isolated side rng, the phrasing consumes no extra draw). `momentPrompts` voicing lever tightened (diction + cadence). Gate: `tests/unit/voice0090.test.ts`. |
| 0091 | [Trigger secrets → house events](./0091-trigger-secrets-house-events.md) | BDD | ✅ Built (2026-06-27; Tracks #869) · a volatile sealed hidden element gains a **`trigger`** kind carrying an engine-only `volatility` + eruption `kind` (`armTriggers` reclassifies the existing volatile B50 wordings under a per-NPC cap + population sparseness, on a dedicated `:trigger:` side-rng ⇒ the B50 + main house streams stay byte-stable). Under accumulated **strain** (live soul, 0041) × a fresh **precipitant** (a conflict/betrayal scene this tick) × a seeded **fireRate** roll, `runTriggerEruptions` (in `GameSessionAdapter`, ridden by the orchestrator's bounded off-screen tick AFTER the society/confessional folds) emits an emergent, Vault-safe **public `house-event`** the player witnesses (a generic eruption-pool line — a blow-up / showmance-detonation / mask-slip / meltdown), + folds the erupter's soul (0041) and co-present witnesses' reads (0026). The sealed `detail`/`volatility`/`kind` **never cross** to player OR admin, before or after a fire (proven by a sentinel sweep over every projection + the assembled prompt + the recorded event). **No cold open** (no precipitant ⇒ no fire); **foreseeable in hindsight** (a calm soul with the same trigger never fires — strain is the tell); per-season **cap** + per-kind one-shot/re-arm policy, all persisted (`eruptionCount`/`triggerTickCount` + the per-trigger `fired`/`lastFiredWeek`, monotonic). Dedicated session side-rng + `ORWELL_TRIGGERS` flag (default OFF) ⇒ ZERO draws when off ⇒ `juryReach`/UAT byte-identical (`triggerOutcomeNeutral` is the gate). `src/engine/triggers.ts` + `triggerConstants.ts`; engine + BDD `0091-trigger-secrets-house-events.feature` (wired in `cucumber.cjs`). Sibling of 0075. |
| 0092 | [Secret-pacing drip](./0092-secret-pacing-drip.md) | BDD | ✅ Built (2026-06-27; Tracks #861) · a thin pacing/eligibility layer over the existing **0060 story-thread scheduler** — a sealed `ripeness` (proximity + tension + recency − already-told, `src/engine/secretPacing.ts`/`secretPacingConstants.ts`) + a ~1-2/week drip budget shapes the scarce reveals into a paced cadence so dormant secrets actually reach the player (the "I knew it" payoff). The pass (`GameSessionAdapter.pacingDrip`, run before 0060's surface decision in the bounded tick) ranks ripe player-bound threads, checks the weekly budget + 0060's season cap, and routes the top eligible candidate into 0060's EXISTING anchored surface (`surfaceThread` `force`: proximity-ripe ⇒ the 0075 confidant slip, tension-ripe ⇒ the gossip chain) — **no new pathway**. Crossings still go through real pathways; what crosses is a belief/paraphrase, never the premise, never a number. Opt-in behind a dedicated `ORWELL_SECRET_PACING` flag on a DEDICATED side-rng ⇒ **zero pacing draws + byte-identical seeded comp/vote/jury spine when off** (the load-bearing guarantee; `tests/unit/secretPacingNeutral.test.ts` is the recording-rng byte-identity gate). Vault-safe on player AND admin, before and after a drip (`secretPacing.test.ts` sentinel). Per-week drip counter + per-thread `lastDrippedWeek` persisted (the cadence resumes across a restart). Engine + BDD `0092-secret-pacing-drip.feature` (wired in `cucumber.cjs`). Builds on 0060 + 0075. |
| 0093 | [Secrets as strategic levers](./0093-secrets-as-levers.md) | BDD | ✅ Built (2026-06-27) · ships as ONE *secrets-as-power* spec with 0099 (#862); deception / bluff first-class · a *learned* secret unlocks a concrete option — leverage in a deal (0039) or a first-class `exposeSecret` (seeded standing hit + betrayal backlash); secrets are spent/capped. The player can only wield a `factId` they already legitimately hold (no Vault read unlocks a lever); outing reaches the house only via `surfaceInformationTo`. PO rulings R1–R4 enumerated (closed-set ceiling, expose-as-action, pathway-only, deals composition). Live player tool `exposeSecret` (`src/surfaces/tools/registry.ts`). Builds on 0039 + 0075. Engine + BDD `0093-secrets-as-levers.feature` (wired in `cucumber.cjs`; step defs `secrets_as_power.steps.ts`). |
| 0094 | [Distorted gossip has consequences](./0094-distorted-gossip-consequences.md) | BDD | ✅ Built (2026-07-06; Tracks #865) — **narrowed scope, flagged to the owner**: the spec's "closed-set move" is built as a NEW dedicated lever, **`confront(npcId, factId)`** (the `confide`/`accuseTie` sibling), not as a retrofit of `nominate`/`vote` — those hold a hard, calibration-load-bearing invariant ("whoever the player names IS the nominee/vote") that a bounded feature build should not touch; a nomination/vote divergence remains a genuinely separate, owner-scoped change. `confront` validates the cited `factId` against the player's own held knowledge (`playerKnowledgeReader`, the 0093/0099 Vault bright line, additively widened with the already-existing `distortion`/`confidence` fields), classifies it via the new pure `isMateriallyDistorted` (`src/engine/beliefReliability.ts`, gated on BOTH a distortion floor and a confidence ceiling so a one-hop intact rumor never misfires), and resolves: faithful ⇒ lands with no divergence; materially distorted ⇒ misfires — the confronted houseguest's own read of the player takes the SAME betrayal-grade blow (0026 `IMPACT.betrayal`), seeded per (npc, week, phase). The dramatic-irony reveal and the behavioral-uncertainty texture ride the EXISTING 0002/0038 factId-lineage/hedge-phrase machinery verbatim — no new code for either. Gated `ORWELL_GOSSIP_CONSEQUENCE` (default OFF ⇒ `confront` always lands, no fold — a rollout lever; the calibration guarantee holds regardless, since the lever is reachable only via the player MCP channel, never the seeded off-screen/vote/competition spine). Engine + unit (`beliefReliability0094.test.ts` / `confrontWiring0094.test.ts`) + BDD `0094-distorted-gossip-consequences.feature` (wired in `cucumber.cjs`). |
| 0095 | [Pre-show ties → time-bombs](./0095-pre-show-ties-reveal.md) | BDD | ✅ Built (2026-07-06; Tracks #867) · turns **0059**'s seeded `[Hidden tie]`s into time-bombs — a monotonic `exposure` state (`sealed → surfaced-to-house → public`, `src/engine/seededRelationships.ts`) + two reveal pathways: the off-screen **overhear** (`src/engine/tieReveal.ts`, a distinct pathway from — and never aliased with — 0059 §5's ambient-suspicion-only `seededTieSurfacing.ts`), and the player-reachable **`accuseTie(aId, bId)`** lever (the 0075 `confide` sibling; a landed accusation is a witnessed public confrontation and jumps straight to `public`). On exposure the DIRECT witness takes the FULL confidence-scaled `BETRAYAL_SHOCK` (0026); further diffusion rides the existing, deliberately mild `GOSSIP_HEARD.betrayal` hearsay nudge (kept inside the `gossipFolds.test.ts` bounded-nudge gate — no new `GOSSIP_HEARD` entry). Sealed off player AND admin until pathway-surfaced; capped per season (`TIE_REVEAL.maxExposuresPerSeason`); its own dedicated `ORWELL_TIE_REVEAL` flag/rng stream (default OFF) ⇒ byte-identical when off. Builds on 0059 + 0077's diffusion pattern + 0075's lever shape. Engine + unit (`tieReveal0095.test.ts` / `tieRevealWiring0095.test.ts`) + BDD `0095-pre-show-ties-reveal.feature` (wired in `cucumber.cjs`). |
| 0096 | [Emergent nemesis](./0096-emergent-nemesis.md) | BDD | ✅ Built (2026-07-06; Tracks #881) · elevates the NPC with the highest *sustained* threat-toward-player into a felt recurring antagonist — a pure `selectNemesis` (`src/engine/campaigns.ts`, no rng of its own) reading only the existing threat edge + the sticky **0086** `target` drive over a consecutive-tick sustain window (with hysteresis to hold/hand off), NOT a new targeting system. `GameSessionAdapter.campaignTick` biases the held nemesis's drive to the top of its intensity band and prioritizes/retargets their **0085** campaign onto the player under the UNCHANGED `maxConcurrent` cap. Behavior-only (no identity/number/tier on any player OR admin projection — only a Vault-safe `rivalry` tone-word hint on `npcVoice`, inside a live scene only), organic (can lapse/hand off/never form), opt-in behind the existing `ORWELL_CAMPAIGNS` (⇒ byte-identical when off, no new vote/decision magnitude). Builds on 0085 + 0086. Engine + unit (`nemesis0096.test.ts` / `nemesisWiring0096.test.ts`) + BDD `0096-emergent-nemesis.feature` (wired in `cucumber.cjs`). |
| 0097 | [Suspicion ledger](./0097-suspicion-ledger.md) | core built | 🟩 **REOPENED + ENGINE CORE BUILT (2026-07-12)** — the pure ledger is built & unit-green: `src/engine/suspicionLedger.ts` (`logHunch`/`editHunch`/`withdrawHunch`, the sanctioned-reveal matcher `resolveAgainstReveal` — model-proposed verdict COMMITTED or the deterministic keyword/about/topic floor, frozen-on-resolve, and the 0048 `scorecard`) + `src/engine/suspicionConstants.ts`, `tests/unit/suspicionLedger.test.ts` (17 cases: verbatim/lossless logging, stays-open-until-reveal, called-it/wrong/partial, freeze, out-of-scope drop, determinism, the Vault-sentinel sweep). **GOLDEN-NEUTRAL** (no narrator-prompt/tool-schema change). Vault-safe by construction (the scorer holds no Vault handle — handed only a Vault-free `RevealedFact`). **Follow-up lane (not yet built):** the FE-driven write-back wiring (`logSuspicion`/`getSuspicionLedger` — the 4-place port/adapter/registry-`INFRA_LEVERS`/McpServer + a `callTool` boundary test), snapshot persistence (`suspicionLedger`, distinct from `knowledge.suspicions`), the resolution-at-reveal hooks (eviction tally / veto / 0075 / 0048 unseal), and the BDD wiring into `cucumber.cjs`. · a player-PRIVATE ledger of hunches the game later marks `called-it`/`wrong` against the truth — a detective scorecard, scored ONLY at a sanctioned reveal (an 0002 pathway or the 0048 unseal), never a live Vault read; inherits 0013's `NO_NPC_PATHWAY`. PO rulings R1–R3. Builds on 0013. |
| 0098 | [Confidence-calibrated reads](./0098-confidence-calibrated-reads.md) | core built (INERT — owner-gated) | 🟨 **REOPENED + DOMAIN TERM BUILT, DEFAULT-INERT (2026-07-12)** — the domain mechanic is built & property-green: `Competitor.conviction?` (default `1`) in `src/domain/competitionOutcome.ts` widens ONLY that competitor's seeded temperature span symmetrically about its unchanged center by a bounded `convictionVarianceGain·(1−conviction)` clamped to `convictionVarianceCap` (`src/domain/temperatureConstants.ts`); `tests/unit/confidenceCalibration.test.ts` (10 cases) proves BYTE-IDENTICAL at conviction 1/undefined, variance-monotonic + MEAN-PRESERVING as conviction falls (var(0)≈4×var(1)), bounded by the ceiling, and a non-finite refusal. **⚠️ STANDING-PRINCIPLE CONFLICT — held for owner re-ratification:** the freeze recorded a *standing principle* ("a player input must never modulate a seeded outcome distribution — not the direction, and NOT even the variance"), which this mechanic contradicts. It is therefore built **DEFAULT-INERT**: NO live caller passes a `conviction`, so the live game + every heavy-sim seed stay byte-identical. The live adapter pass-through (deriving `conviction` from the 0002 belief `confidence`) is DELIBERATELY WITHHELD pending explicit owner re-ratification of R1. **GOLDEN-NEUTRAL.** PO rulings R1–R4. Builds on 0002 + competition outcome. |
| 0099 | [Secrets as currency](./0099-secrets-as-currency.md) | BDD | ✅ Built (2026-06-27) · ships TOGETHER with 0093 as one mechanic (#880); deception + player bluff first-class · the *trade* use of a held secret (value is the recipient's — info becomes liquid) + off-screen NPC barter; the player only trades a `factId` in `knownTo(player)`. Shipped as ONE build spec with 0093 (leverage/trade/expose = three uses of a held fact, not three systems). Live player tool `tradeSecret` (`src/surfaces/tools/registry.ts`). Builds on 0039 + 0093 + 0002 diffusion. Engine + BDD `0099-secrets-as-currency.feature` (wired in `cucumber.cjs`; step defs `secrets_as_currency.steps.ts`). |
| 0100 | [Jury grudge book](./0100-jury-grudge-book.md) | BDD | ✅ Built (2026-06-27; Tracks #882) · the sequestered jury house is now a hidden second society — `src/engine/juryHouse.ts` (`runJuryHouseStretch`) reuses 0038 `richOffscreenStretch` + 0002 `diffuseGossip` over the **juror set** (player a SUBJECT only, never a graph terminus) so a grievance one juror carried out diffuses to others, and the adapter `juryHouseTick` folds a **bounded, saturated, monotonic** per-(juror, finalist) grudge into the persisted `live.juryGrudge`. `edgeAsJuryRel` consumes it ALONGSIDE the recorded manner — the ONLY change to the vote path (a richer hidden lean *input*; tally/tie-break/reveal reused verbatim, anonymity unchanged). Sized UNDER `MANNER_LEAN` (`JURY_HOUSE.adjustmentCap`) so it deepens, never swamps, jury management. Runs on a DEDICATED isolated rng behind opt-in `ORWELL_JURY_HOUSE` ⇒ byte-identical when off (SHA256 draw-stream gate); a fresh `juryReach` measurement with it on keeps the EARNED-wins guard. Vault-sealed over player AND admin (hidden events, witnessSet = jurors). Builds on 0014/0037/0045 + 0038/0002. Engine + unit (`juryHouse.test.ts` / `juryHouseAdapter.test.ts`) + BDD `0100-jury-grudge-book.feature` (wired in `cucumber.cjs`). |
| 0101 | [NPC myth-making](./0101-npc-myth-making.md) | BDD | ✅ Built (2026-07-05; Tracks #883) · the player's notable acts seed gossip **legends** about themselves that spread + distort and circle back ("people are saying you…") — a new *origin* for the existing 0002 diffusion, the outward complement to 0088's inward read. `src/engine/legends.ts`'s `notablePlayerActs` is a pure selector over the player's already-recorded PUBLIC ceremony beats (a small curated set: HOH win, veto win, veto save, nominations, finale win); `src/engine/gossip.ts`'s `LEGEND`/`LEGEND_GLOSS`/`legendFrom` supply the per-season tunables + the vague public gloss (never the verbatim act); `GameSessionAdapter.legendTick` (called once per off-screen tick from `orchestrator.ts`, alongside `juryHouseTick`) seeds the legend from a co-present earwitness and hands it to the UNCHANGED `diffuseGossip` (reuses `factId`/decay/distortion/player-terminal surfacing wholesale — no new gossip system). Runs on a DEDICATED isolated rng behind opt-in `ORWELL_MYTH_MAKING` (also a `setBehavioralFlags({mythMaking})` God-Mode dial) ⇒ byte-identical when off; passes **NO** `rel`/`subjects` to `diffuseGossip`, so a legend folds **no edge at all** (not the player's own reads, not any NPC's read of the player, v1) — calibration-neutral by construction even while ON. Per-season cap + a notable-act watermark persist (`legendCount`/`legendTickCount`/`legendLastActTick`, wired into the `SessionCoreCounts` non-degradation checkpoint). Vault-sealed over player AND admin (no admin field added; the legend is an ordinary hidden-layer belief, surfaced only via a terminating pathway exactly like NPC↔NPC gossip). Builds on 0002 + 0038 + 0049 + 0088. Engine + unit (`tests/unit/legends0101.test.ts`) + BDD `0101-npc-myth-making.feature` (wired in `cucumber.cjs`; step defs `npc_myth_making.steps.ts`). |
| 0102 | [Daily recap + cliffhanger](./0102-weekly-recap-cliffhanger.md) | engine | ✅ Built (2026-07-05; Tracks #884) · the REDESIGNED daily "day in review" digest (superseding the originally-drafted weekly recap below it in the filename/spec) fires at the player's own in-game **bedtime** (`turnIn`, ADR 0006) — the *daily* analog of the shipped 0048 `seasonRecap()`: a short Vault-safe digest stitched from witnessed ceremony/scene/deal events + gossip already surfaced to the player (`playerKnowledgeReader`, excluding the OOC Diary Room), scoped to the day that just closed via persisted event/knowledge cursors (`src/engine/liveSeason.ts` `LiveSeasonState.dayNumber`/cursors/`lastDailyRecap`). Ends on an optional non-committal cliffhanger `hook` (a strained/vague deal or "today's surfaced fact isn't finished playing out"), picked via a DEDICATED `daily-recap:<day>` side rng when >1 candidate — never the season/vote/jury stream, never a fold, never a future roll; a quiet day closes with no hook. Delivered inline on `turnIn`'s `AdvanceView.dailyRecap` (re-fetchable via the new `dailyRecap` player tool/MCP dispatch) and voiced per a `momentPrompts.ts` glossary rule (facts to voice, the hook framed only as a possibility). Reads only Vault-free projections (no `VaultStore` import); progresses no game state; reproducible (materialized once, served back verbatim). **Not built:** a dedicated FE pop-up/window surface — shipped as an in-fiction chat beat per the design's own R1 (ADR 0003: augment, never a dashboard), which the original PO redesign note's "window kit" phrasing did not preclude. Engine + unit `tests/unit/dailyRecap0102.test.ts` (13 cases: dormancy, witnessed+surfaced-only, the Vault sweep, the non-committal hook, reproducibility, the MCP boundary dispatch); heavy sims (`juryReach`/UAT/gradient) re-confirmed byte-identical. Builds on 0048 + 0002 + 0066/ADR 0006 + narration. PO rulings R1–R2 honored (an in-fiction recap beat, not a dashboard; Vault-safe non-committing teaser). |
| 0103 | [Edit-bay foreshadowing](./0103-edit-bay-foreshadowing.md) | core built | 🟩 **REOPENED + ENGINE CORE BUILT (2026-07-12)** — the pure fit/eligibility/budget/hint core is built & unit-green: `src/engine/foreshadow.ts` (`foreshadowFit`, `rankForeshadowCandidates` — drops `dormant` + no-public-surface + below-floor, `hintBudget` weekly+season caps, the Vault-safe `buildHint`, `leadTimeFromWeeksOut`) + `src/engine/foreshadowConstants.ts`, `tests/unit/foreshadow.test.ts` (12 cases: in-motion-only, needs-a-public-observation, cadence caps, the Vault-premise sentinel sweep, determinism). **GOLDEN-NEUTRAL** (the `editBayHint` context field is deferred with the adapter wiring, so the base narrator prompt is untouched). Vault-safe by construction (handed only reduced [0,1] signals + the public observation — never a sealed premise). **Follow-up lane (LARGE — not yet built):** the `GameSessionAdapter` off-screen-tick wiring on a dedicated side-rng behind `ORWELL_FORESHADOW` (byte-identical when off), the read-only `editBayHint` `momentPrompts` field, persistence of the per-week counter + per-pathway `lastHintedWeek` + hint→pathway link, the 0092/0102 coordination (R3), and BDD wiring. · a Vault-safe production-edit hint that nods at a pathway ALREADY in motion so a later payoff lands as "I knew it." PO rulings R1–R3. Builds on 0092 + 0049 + 0060. |
| 0104 | [Season-over-season notoriety](./0104-season-notoriety.md) | BDD | ✅ Built (2026-06-27; Tracks #886) · a reputation that **precedes the returning player into a new cast**. At the season-end terminal (inside `registry.resetUser` — the SINGLE restart door, no new path) the engine derives a bounded, fixed-shape **`NotorietySummary`** (`src/engine/notoriety.ts`) from the finished season's **open-set outcome** only (placement / comp wins / how each evictee read the player's eviction role / jury reach + finale vote share — **never** a Vault record, soul, or hidden edge, R1) and **monotonically accumulates** it per-USER in a `UserNotorietyStore` (`src/ports/UserNotorietyStore.ts` + `FileUserNotorietyStore`/`InMemoryUserNotorietyStore`), **separate** from the per-season save so the restart rotation never touches it (account reputation survives the cutover). The opt-in is **DIEGETIC** (R4): the player returns as the **same character** (`keepCharacter` ⇒ the internal `carriesNotoriety` flag) to carry it, or a **new character** for a clean slate. When carried, `seedFirstImpressions` folds **one bounded, seeded, archetype-shaded bias term** beside the existing `dayOnePerception` lean — **modulated per-NPC by a seeded recognition level** (not everyone has heard of you; a minority hold a **distorted**/inverted read, the 0094 idea), tilting the new cast's NPC→player **direction** only, inside the existing `MOVE_IN.spread` envelope. The bias rides a **dedicated `:notoriety` sub-rng** (never the shared `:relationships`/competition/vote streams) ⇒ the engine still owns every outcome magnitude (R2). Cross-user isolated (own prior games only, R3). Vault-sealed over player AND admin (no number / no facets on any projection or the day-one prompt). **Opt-in ⇒ byte-identical** when off (the dedicated rng is never drawn): the NPC↔NPC move-in layer is byte-identical even WITH a notoriety, and the seeded `juryReach`/gradient floor is unmoved. `notorietyConstants.ts` is the single tunable. Engine + unit (`notoriety.test.ts` / `notorietyOutcomeNeutral.test.ts` — the calibration-neutrality proof / `notorietyVault.test.ts` — the Vault + cross-user gate) + BDD `0104-season-notoriety.feature` (wired in `cucumber.cjs`). Builds on the single restart door + 0058/0063 + 0021. PO rulings R1–R4. |
| 0105 | [Drive-anchored suspicion](./0105-suspicion-and-mood-anchoring.md) | BDD | ✅ Built (2026-06-25) · **the wary read behind the sealed plan** — consumes 0086's drive to fill the diagnostic's empty `suspects` layer and anchor 0084's mood. A drive's PLAN (intensity, vote tilt, campaign) stays Vault-sealed, but the **threat-READ that motivates it** is the houseguest's own subjective hunch, so `driveSuspicion` surfaces it on `npcVoice.suspects` as a **behavioral hunch naming the subject** (`target` ⇒ "keeping a close eye on <name>"/"sizing up <name>"; `self-preserve` ⇒ "wary feeling about <name>"/"watching their back around <name>"; `build`/`lay-low`/`win-power` ⇒ none). The narrator now has, per houseguest, a `mood` (0084) **and** a specific person it's about — connected in behavior, never named as a motive (a mood-consumer lever note). Pure leaf (no number, no Vault handle); present only for an active houseguest with a live drive ⇒ **byte-identical projection when `ORWELL_CAMPAIGNS` is off**. Engine + BDD `0105-suspicion-and-mood-anchoring.feature` (wired in `cucumber.cjs`). Consumes 0086. (Renumbered from 0087 — the parallel Producer's-Vault spec batch claimed 0087–0104.) |
| 0106 | [Whole-house events are exclusive set-pieces](./0106-exclusive-house-events.md) | engine | ✅ Built (2026-06-26) · **a comp/ceremony is the ONLY thing happening** — from playtest feedback (a comp ran during a side conversation, then the scene resumed as if uninterrupted). `houseEventInSession()` detects a live competition (comp-intent/round/staging) OR ceremony (nominations / veto / eviction pending); while one runs, `whereabouts` reports the **whole house gathered** (a `houseEvent` block — for a comp the `competing`/`spectating` split with the **outgoing HOH a spectator** + `youAreCompeting`; a ceremony just attends), `nearby` is empty, and the player **can't wander off** (`movePlayer` no-ops). A `momentPrompts` section makes the event exclusive (no side scenes), allows the card to **interrupt mid-scene**, and forbids resuming the prior scene **as if uninterrupted** (acknowledge the gathering, move forward). Pure read-projection + a player-action gate — the seeded sim is untouched ⇒ `juryReach`/UAT **byte-identical**. Spectator *seeded* opinion-folds deferred. Engine-gated (`exclusiveComps0106` + `leverManifest`). |
| 0107 | [Named alliances](./0107-named-alliances.md) | engine | ✅ Built Phase A+B (2026-06-26) · **naming cements an alliance "in reality," real or not** — the explicit, NAMED, multi-party layer between emergent blocs (0043) and 2-party deals (0039). `formAlliance({name, members})` (a player tool, 4-place MCP) is **bond-gated**: members not close enough **decline** (anti-watering-down). Naming then (1) **cements** — a bounded, saturation-diluted tie boost into `detectBlocs` so a borderline cluster forms + tightens (the bloc bends noms/votes via `blocTerm`), capped so a *hollow* alliance never fabricates a bloc; (2) banks **favor** — bounded goodwill toward co-allies (feeds 0075 confidences). Vault-safe projection (`gameStatus.alliances` = name + members, never a number); persists. **No hard cap** — saturation dilution is the only limit (owner ruling). Every effect is 0/identity when no alliance is shared ⇒ `juryReach` **byte-identical** (re-verified). **Phase B:** NPCs **name alliances off-screen** (`formNpcAlliances`, gated `campaignTick`, build-drive founder over real bonds, player never auto-enrolled) and **pitch the player** (`gameStatus.alliancePitches` + the `joinAlliance` seam); **betraying a named ally cuts deeper** (`reconcileAllianceBetrayals` — threat ▲, a witnessed betrayal, the alliance fractures). Engine-gated (`alliances0107` + `alliancesNpc0107b` + MCP boundary). |
| 0108 | Real-model golden-path gate | FE + CI | ❌ **Decommissioned (2026-07-21, owner directive)** — the golden-path fixture apparatus (the committed real-model fixture, the record/replay scripts + the `src/golden_path.py` seam, the `golden-path` CI job, `golden-nightly.yml`, and the `test_0108_golden_path.py` gates) was removed entirely. It was advisory / non-gating (excluded from `ci-gate.needs`), perpetually stale (a fixture treadmill on every prompt/schema change), and hang-prone (#1713). Design note + `.feature` removed. |
| 0109 | [Negotiated deal duration](./0109-deal-duration.md) | BDD | ✅ Built (2026-06-27), amends 0039 · a deal gains an optional negotiated **duration** — an explicit term-count (`expiresWeek`) or simply labeled **`vague`** (no per-party belief tracking — "just labeled as vague is enough"). Betrayal-shock (0026) **scales with remaining deal life**: turning early on an ally costs the full price, turning at/after an explicit expiry is fair game (no shock), and a `vague` deal's break reads softer/fuzzier. ADR 0005 split (the fuzzy "how long" rides the open-set `terms` prose; the engine keeps the bounded interpretation + the seeded magnitude — no number crosses). No duration ⇒ byte-identical to 0039; `expiresWeek`/`vague` persist (`src/engine/deals.ts`, `src/domain/deal.ts`). Builds on 0039 + 0026 + 0014. Engine + BDD `0109-deal-duration.feature` (wired in `cucumber.cjs`; step defs `deal_duration.steps.ts`). |
| 0110 | [Vote deduction (process of elimination)](./0110-vote-deduction.md) | BDD | ✅ **Built (2026-06-28)** · **PO review** (realizes option B of the 0023 secret-vote-grudge ruling) · eviction votes are secret ballots (0047: count public, attribution sealed), but the jury grudge cheated — it folded against the *actual* secret voters (`recordEvictionManner(s, evictee, [hoh, ...votesToEvict])`). 0110 routes the SECRET-vote half through **process of elimination**: the evictee DEDUCES the defectors from the public count + eligible pool + their own reads (0002 belief + confidence, **can be wrong**), and the grudge folds on that belief — a deducible defector earns it, an **undeducible** secret vote earns **none** (secrecy honored), a **wrong** deduction misattributes it (dramatic irony). The public responsibility (HOH/noms) is unchanged; the true tally still unseals at 0048. Vault-sealed (player AND admin); opt-in `ORWELL_VOTE_DEDUCTION` ⇒ byte-identical when off (juryReach re-confirmed). `src/engine/voteDeduction.ts` + `liveSeason.ts`; Engine + BDD `0110-vote-deduction.feature` (wired in `cucumber.cjs`; `vote_deduction.steps.ts`). Builds on 0047 + 0002 + 0037/0014 + 0026. |
| 0111 | [The Day-1 experience](./0111-day-1-experience.md) | BDD | ✅ **Built (2026-07-10)** · composes #905–909 into one coherent first-session design (umbrella #875). **Pillar 1** (#905) — casting probes the SELF (self-belief + tells), drops the declared kill-list; the finalize floor is character (name/backstory/motivation), never a target list. **Pillar 2** (#909) — the producer plants exactly ONE Vault-free **curiosity needle** (`src/engine/curiosityNeedle.ts`, seeded off a dedicated `:curiosity-needle` side stream, generic promise-of-depth only — never a specific secret/relationship/scheme; sentinel-swept on player AND admin surfaces). **Pillar 3** (#906) — the premiere gate is reframed "no one invisible, not everyone equal": a `powerReachable` flag on `PremiereIntrosView` unlocks the first HOH once a couple of HOT reads are formed and everyone is at least met in motion, WITHOUT the full roll-call (`complete` unchanged); the HOH stays a real seeded competition (never gifted). **Pillar 4** (#908) — a Day-1 beat folds a real read through the existing `recordInteraction`/0023 path (guaranteed by the 0055 `_auto_record_scene` belt even on model under-call), recalled later only as behavior; byte-identical folds when absent. **Pillar 5** (#907) — the Diary Room is enterable Day-1 and stays OOC (no NPC pathway). Reframes are calibration-neutral (byte-identical `advanceToFinale` season). Engine (`momentPrompts.ts` casting/premiere framing, `curiosityNeedle.ts`, `GameSessionAdapter`/`GameSession.ts` `powerReachable`/`hotReads`) + FE premiere tutorial touch (`orwellPremiereTutorial.js`) + BDD `0111-day-1-experience.feature` (wired in `cucumber.cjs`; `day1_experience.steps.ts`). Builds on 0050 + 0049 + 0013 + 0023 + 0055; points at (never reveals) 0038/0059/0043. |
| 0112 | [LLM-call observability](./0112-llm-call-observability.md) | FE-pytest | ✅ **Built (2026-07-10)** · **renumbered from 0107** (doc-drift audit, 2026-07-05 — collided with the built `0107-named-alliances`; content unchanged). FE-tier: Vault-free `TraceRecord` (token-ledger facts + the 0065 `beatSeq`/`phase` correlation keys) built at the single `src/llm_core.py` emit point, plus **opt-in** external forwarding — OpenRouter request `metadata` tagging (Broadcast handshake) + a fail-soft `TraceSink` port (`src/trace_sink.py`: `NoopTraceSink` default, `OtlpTraceSink` for OTLP/Langfuse). Settings `observability_{enabled,endpoint,privacy_mode,sampling}` (default off / metrics-only). **Byte-identical when unconfigured**; the trace path reads no Vault/soul; content gated behind `full` opt-in; sampling deterministic by session. Opt-in self-hosted Langfuse `deploy/observability/docker-compose.yml`. Builds on 0069 + 0065 + 0079. Gate: `frontend/tests/test_0112_llm_observability.py` (Vault-free record + fail-soft + correlation + privacy/sampling + boundary + F16-divergence). Design note + `.feature` (FE-tier; not in `cucumber.cjs`). |
| 0113 | Visual regression harness | FE + CI | ❌ **Decommissioned (2026-07-21, owner directive)** — rode the SAME 0108 golden-replay fixture, which was removed. The `visual-regression` CI job, `scripts/visual_regression.py` + `scripts/visual_bless.py`, the `src/visual_geometry.py` / `visual_manifest.py` / `visual_pixeldiff.py` modules, and the `test_0113_*` gates were removed with it. Design note + `.feature` removed. |
| 0114 | Theme surface consistency | FE + CI | ❌ **Decommissioned (2026-07-21, owner directive)** — rode the SAME 0108 golden-replay fixture, which was removed. The `theme-consistency` CI job, `scripts/theme_consistency.py` + `src/theme_probe.py`, and the `test_0114_*` gates were removed with it. Design note + `.feature` removed. |
| 0115 | [Diary Room as a strategic confessional](./0115-diary-room-strategic-confessional.md) | BDD | ✅ **Built (2026-07-10)** · PO expansion of 0036's Diary Room. Gives the walled DR mechanical teeth, all **player-facing**: (1) the player's DR intent **grounds the GM's narration** (`playerDiaryRoom` on `GameStateView` → a fenced PRIVATE steer in `renderGameContext`), so the game narrates the player's *real* strategy — the irony of a lie — instead of sycophantically believing their public mask (mandate #3, pointed the other way); (2) `producerPrompt(beat)` returns a **pointed beat-specific question** (`DRAMATIC_PROMPTS`) not the old label; (3) the player's confessionals resurface in the **0048 retrospective** (`RetrospectiveView.playerConfessionals`). **The wall stays STRUCTURAL where it counts:** DR never enters any NPC's knowledge (`deriveNpcKnowledge` unchanged) or per-NPC voicing projection, and never drives NPC behavior — the house stays deceived; only the GM's player-facing narration is grounded (prompt-guided + a targeted no-leak test). Vault-free; **byte-identical** heavy sims (DR never touches the seeded spine). `diaryRoom.ts` + `momentPrompts.renderGameContext` + `GameSessionAdapter.view()`/`buildVaultUnseal`; Engine + BDD `0115-diary-room-strategic-confessional.feature` (wired in `cucumber.cjs` — done right, not 0036's E87a deviation) + `diaryRoomStrategic.test.ts`. Builds on 0036 + 0013 + 0027 + 0048. |
| 0116 | [Model-authored cast genesis (the validation envelope)](./0116-model-authored-cast-genesis.md) | Engine + FE-pytest | ✅ **Built (2026-07-12, #1403)** · owner-decided design (seven questions locked 2026-07-11) · Phase **2** of the casting upgrade (phase 1 — deep authoring at temperature 1.1 — in flight on `claude/cast-authoring-upgrade`): the model proposes the ENTIRE 15-NPC skeleton — free-form identities (the archetype enum becomes a DERIVED tag, not a generative constraint), validated-not-pooled names, personas, hidden elements, the pre-show tie graph (0059/0095), orientation fields, voices, and **banded variable stat totals** — and the engine validates/clamps/commits inside an envelope it owns (point-buy band + per-stat clamps + a cast-wide variance floor; **no model number escapes the clamp**). Player-BLIND by construction, steered by a **seeded season brief**; async during the casting interview with a **loud pre-finalize gate** on strict-policy failure (never a silent floor, #1313 precedent); the deterministic factory stays as the stubbed/keyless/golden floor (byte-neutral), and the committed genesis is the recorded world-gen artifact (replays read the store; golden fixture re-record owed at build). Implements the **ruling-#1 amendment (§5)** — "no fixed cast" now enforced by validation + per-season generation instead of corpus pooling (legacy deny-list + roles-only testing unchanged; per owner-locked design decision #4 "full name freedom with validators" — the formal ruling-doc amendment in the audit ledger is flagged for owner confirmation). **Gate: the `castGenesis` vitest suite** (`castGenesis.test.ts` 32 tests · `castGenesisBoundary.test.ts` · `castGenesisByteNeutral.test.ts`) + `frontend/tests/test_0116_cast_genesis.py` — the 32 `.feature` scenarios are covered 1:1 by vitest (per-feature vitest gate, reconciled index); the `.feature` stays a design contract, not wired into `cucumber.cjs`. Golden fixture re-record owed (#1539). |
| 0130 | [Exit interviews](./0130-exit-interviews.md) | BDD | ✅ **Built (2026-07-13)** · **PO expansion of the 0047 review — specced AND built same session.** Appends an **exit-interview** stage to 0047's `EvictionProgress` (after the goodbye stage), firing on **every staged eviction**: the producers interview the evictee — they see their goodbye messages and react, and tell their side. **NPC** → a grounded `ExitStance` (`npcExitStance`, DERIVED from the recorded eviction **manner** — betrayed/disrespected ⇒ bitter, blindsided ⇒ defiant, respected ⇒ gracious; narrator-voiced, anti-sycophancy); **player** → a real **pending decision** (`exit-interview`, with a structured `stance` choice + optional words — the engine never speaks for them). Recorded (`LiveSeasonState.exitInterviews`) and **resurfaced in the 0048 retrospective** (`RetrospectiveView.exitInterviews`, scrubbed, first-person). A battle-back returnee (0025) keeps the memory of who buried them (goodbye messages are witnessed → grudge source, not a leak). **Calibration-neutral — NO flag:** the beat draws no rng and mutates no seeded state, so the seeded spine is **byte-identical** — proven against a pre-feature eviction-order baseline (`exitInterview.test.ts`) **and** `juryReach` re-run green. Vault-safe (own knowledge + own goodbyes only; the `exitInterview` Vault canary; player's interview reaches no active houseguest). Distinct from 0129 (moment-of-exit vs the weeks after). `liveSeason.ts` (`EvictionStage`/`ExitStance`/`npcExitStance` + the stage + the player pending) + `GameSession.ts` (pending/submit + `RetrospectiveView.exitInterviews`) + `GameSessionAdapter.ts` + `momentPrompts.ts`; Engine + BDD `0130-exit-interviews.feature` (6 scenarios, in `cucumber.cjs`) + `exit_interview.steps.ts` + `exitInterview.test.ts`. Expands 0047; reuses 0037/0014 + 0034 + 0013 + 0002; feeds 0048; composes with 0025/0046; under 0001 + 0030. |
| 0132 | [Full live per-NPC cognition](./0132-full-live-per-npc-cognition.md) | 📝 Spec only | 📝 **Spec only (2026-07-21)** · **the ambitious version of 0131 (owner opted IN); design record [ADR 0022](../decisions/0022-authored-cognition-and-narration-voice.md), issue #1739 — authored, NOT yet built.** Re-simulate **every awake houseguest's** cognition live each turn (each NPC's `want/read/attempt` refreshed from a per-NPC scoped call before the beat is narrated) — the full-fidelity extension of 0131-S1's salience-gated mode, keeping ADR 0022's authority split. Cost engineering: **parallel cheap-tier** calls over the **awake set** (0066), **delta-fed** prompts (0065 `stateDelta`), a per-turn **budget guard** that **degrades gracefully to 0131-S1** under pressure, and **failure-surface containment** (N validated fail-soft JSON calls, belt-fire success-gated telemetry). Ships with a **cost model** + a **flagged, A/B feel-vs-cost** evaluation. **Integrity gates (build with the feature, per ADR 0022): cognition-only, per-call knowledge-scope + aggregate-leak, funnel-only, soul-anchored** — no closed-set authority moves to the model even at full fidelity; `expressiveNonCollapse` + Vault sentinels stay the proof for the descriptor path they cover. Gate (placeholder, when built): `tests/unit/fullLivePerNpcCognition.test.ts` (parallel + budget + graceful degrade + belt success-gating) + `expressiveNonCollapse.test.ts` + FE `test_…_full_cognition.py`; NOT in `cucumber.cjs`. Extends 0131; builds on 0065 + 0066 + 0041 + ADR 0019/0021/0022; blocked-by the faithful-seam cluster (#1726/#1735). |
| 0131 | [Per-NPC authored cognition](./0131-per-npc-authored-cognition.md) | 📝 Spec only | 📝 **Spec only (2026-07-21)** · **"fan out cognition, funnel narration"; design record [ADR 0022](../decisions/0022-authored-cognition-and-narration-voice.md), issue #1736 — authored, NOT yet built.** Enrich per-NPC cognition via **cheap, scoped, data-only** calls while the engine keeps its closed-set authority: the model authors a `want/attempt/read` cognition **INPUT** (`AuthoredCognition`, open set — **not** the edge-only descriptor), the engine owns `resolve/state` magnitude and only **then** MAY emit the #355 `recordInteraction` `consequence` descriptor as its OUTPUT fold **when an edge actually moves** (the ADR 0005 shape/magnitude split). Cognition with no relationship-edge change must still reach the engine — never forced through the edge-only descriptor. Three sub-designs (the F-fwd-1/2/3 feature-issue split): **S1** salience-gated authored intent (1–2 moved NPCs/turn, cached, cheap-tier, fail-soft), **S2** subjective divergent memory (per-NPC distorted recollection, soul-persisted), **S3** paraphrase-residual scoped call (ADR 0019's residual killed for the one high-risk present-NPC secret-adjacent beat — **returns structured data; the sole narrator writes any line**). **Integrity gates (build with the feature, per ADR 0022): cognition-only, bounded-input, per-call knowledge-scope (ENGINE presence, ADR 0019/#1726), soul-anchored, funnel-only** — no closed-set authority to the model. Gate (placeholder, when built): `tests/unit/perNpcCognition.test.ts` (salience gates + intent-folds-not-overrides + cache + S2 persist + S3 divergence) + `expressiveNonCollapse.test.ts` + FE `test_…_cognition.py`; NOT in `cucumber.cjs`. 0132 is the full-fidelity extension; builds on 0041 + 0065 + ADR 0005/0019/0021/0022; blocked-by the faithful-seam cluster (#1726/#1735). |
| 0129 | [The jury house](./0129-the-jury-house.md) | 📝 Spec only | 📝 **Spec only (2026-07-13)** · **PO expansion of the 0046 review — authored, NOT yet built** (PO: "write the spec for this and build later again"). Pairs with 0046's ceremonies-as-broadcast: while the player holds `player.status === "jury"`, the other evicted houseguests are in the **jury house** with them. The player can hear the season **from each juror's (biased, partial) perspective**, **ask them questions** (answered in that juror's voice from that juror's own witness-bounded knowledge — never the Vault), get a rolling **house update** as new evictees arrive, and carry that read into their **0037 finale vote** (mechanic unchanged). **Vault-safe by construction:** each juror's shareable view is their own 0002 witness-bounded `KnowledgeState` + the 0046 broadcast + jury gossip — sequestered, so live scheming/confessionals never reach them (canary-extended); the player's jury-house talk reaches no active houseguest (mirror of the DR one-way rule). Anti-sycophancy: juror slant is **computed** from soul + recorded eviction manner (bitter/blindsided/respected), narrator-voiced; two jurors can contradict (0002/0038). **Calibration-neutral core** (Vault-free projection + narration, no seeded mutation ⇒ byte-identical spine); the optional NPC-juror-vote **roundtable** effect is a **default-off `ORWELL_JURY_ROUNDTABLE`** sub-scope (calibration-sensitive, juryReach re-check) or deferred. Design note + spec-only `.feature` (NOT in `cucumber.cjs`). Expands 0046; reuses 0002 + 0036/0049 + 0037/0014 + 0038; under 0001 + 0030. |
| 0128 | [Three-part Final HOH](./0128-three-part-final-hoh.md) | 📝 Spec only | 📝 **Spec only (2026-07-13)** · **PO expansion of the 0045 review — authored, NOT yet built** (PO: "write the spec and put it in future features; we don't need to write this right now"). Replaces 0045's single Final-3 HOH competition with the real _BB_ **three-part** tournament: **Part 1** endurance (physical, all three) → winner skips to Part 3; **Part 2** a physical-and-mental hybrid (the two Part-1 losers) → winner to Part 3; **Part 3** a quiz (mental, the two part-winners) → **Final HOH**. The Final HOH wins two parts ("win two to win, lose two and you're out of the HOH race"); the two-loss finalist is **not** auto-evicted — the Final HOH still personally evicts one of two (0045, unchanged). The player competes in each eligible part and rests through Part 2 if they win Part 1. Engine-decided (`resolveCompetition`), Vault-free, mid-tournament restart resumes (0030). **Calibration-safe:** three stat-typed comps change which finalist becomes Final HOH for a seed, so **default-OFF** `ORWELL_FINAL_HOH_THREE_PART` — off ⇒ byte-identical single-comp 0045 path (juryReach re-check on both flag states when built); reuses the 0127 hybrid blend for Part 2 (passed directly, no 0127-flag dependency). Design note + spec-only `.feature` (NOT in `cucumber.cjs`). Expands 0045; builds on 0006/0028 + 0127 + 0030 + 0001. |
| 0127 | [Mixed-type competitions](./0127-mixed-type-competitions.md) | BDD | 🏗️ **Built (2026-07-12)** · **PO expansion of the 0042 review (completes 0125/0126)** — real _BB_ comps are hybrids ("crawl through mud, then solve a puzzle"); every comp resolved on a SINGLE stat, and the `secondary` field was narration-only. 0127 turns it into a **real weighted blend** in `resolveCompetition`: for a comp with a `secondary`, the score base = `primary·(1 − w) + secondary·w` (`w = mixedSecondaryWeight = 0.35`), so a **well-rounded** houseguest edges a one-dimensional one — while the **primary still dominates** (a physical monster still beats a mental-only rival on a physical hybrid; the blend tilts, never inverts). ~13 naturally-hybrid comps carry a secondary (skill/crapshoot/social/maze); pure endurance/strength/puzzle stay single-stat. **Riskiest of the comp series** (it edits `competitionOutcome.ts`, the calibration core), so **default-OFF** (`ORWELL_COMP_MIXED`, deploy opt-in): off ⇒ pure single-stat ⇒ **every seeded gate byte-identical** — proven by `juryReach` re-run green **both** flag-off (byte-identity) **and flag-on** (the band still holds — balance preserved). Composes with 0125/0126. `temperatureConstants.ts` (`mixedSecondaryWeight`) + `competitionOutcome.ts` (`secondaryStat` blend) + `competitionLibrary.ts` (13 `secondary` tags) + `liveSeason.ts` (`SeasonCtx.mixedComps`, `resolveComp` passes it) + `GameSessionAdapter` (`ORWELL_COMP_MIXED` → `ctx().mixedComps`); Engine + BDD `0127-mixed-type-competitions.feature` (5 scenarios, in `cucumber.cjs`) + `mixedCompetition.test.ts` + `mixedCompetitionLive.test.ts`; golden driver pinned off; deploy opt-in. Builds on 0042 + 0125 + 0126 + 0006/0028 + 0001. |
| 0126 | [Expanded competition mechanics](./0126-expanded-competition-mechanics.md) | BDD | 🏗️ **Built (2026-07-12)** · **PO expansion of the 0042 review (completes 0125)** — 0125's themes reskin the *same* ~12 mechanics, so a season still repeats each **mechanic** ~2×; 0126 grows the pool **12 → 30** (15 HOH + 15 veto, `COMPETITION_LIBRARY_PLUS`) and widens the draw into a **rolling shuffle** (avoid the whole recent history, not just the immediate prior) so a full season (~14 HOH + ~13 veto) runs with **no repeated mechanic** — real *gameplay* variety, not just skins. The new mechanics **preserve the base governing-stat mix per phase** (physical 5 / mental 8 / social 2 — the base's mental-dominant ratio) so balance holds; resolution is the **unchanged** `resolveCompetition` (engine still decides). Unlike the 0125 projection, growing the pool changes seeded winners, so it is **default-OFF** (`ORWELL_COMP_MECHANICS_PLUS`, deploy opt-in): with it off the draw is the base 12 and **every seeded gate is byte-identical** — proven by `juryReach` re-run green **both** flag-off (byte-identity) **and flag-on** (the band still holds, empirical balance proof). Composes with 0125 (30 mechanics × 24 themes). `competitionLibrary.ts` (`COMPETITION_LIBRARY_PLUS` + pool-aware `drawCompetition`/`competitionById`) + `liveSeason.ts` (`SeasonCtx.expandedComps`, `drawFor` threads ctx) + `GameSessionAdapter` (`ORWELL_COMP_MECHANICS_PLUS` flag → `ctx().expandedComps`); Engine + BDD `0126-expanded-competition-mechanics.feature` (5 scenarios, in `cucumber.cjs`) + `competitionMechanicsPlus.test.ts` + `competitionMechanicsPlusLive.test.ts`; golden driver pinned off; deploy opt-in in `smoke.sh` + `orwell-install.sh`. **Deferred:** default-on *everywhere* (incl. the seeded gates) needs a full calibration re-baseline + golden re-record (a live key). Builds on 0042 + 0125 + 0006/0028 + 0001 + ADR 0003. |
| 0125 | [Competition theme variety](./0125-competition-theme-variety.md) | BDD | 🏗️ **Built (2026-07-12)** · **PO expansion of the 0042 review** — 0042's ~12 mechanic defs meant a season saw each comp ~twice; 0125 lays a large seeded **THEME/skin pool** (24 Vault-free themes) over the mechanics so the *displayed* comp is `mechanic × theme` (~240 combos ⇒ a repeat-free season). The theme is a pure **projection**: a name transform (`prefix + format noun`, e.g. "Haunted Trivia") + a scene-setter woven into the premise — the mechanic, governing stat, and **winner are unchanged**. Chosen on a **dedicated deterministic hash (never the beat rng)** via one per-seed permutation read through nonzero per-beat rotations (no in-phase repeat for 24 weeks; same-week HOH/veto always differ; a double-eviction second cycle reskins), so the seeded spine (juryReach/gradient/UAT/golden) is **byte-identical on OR off** — proven by a live on-vs-off eviction-order guard. Default-**ON** for real play (`ORWELL_COMP_THEMES`, `=0` to disable); pinned off in the golden driver ⇒ committed fixture replays with **no re-record**. #1400 model-authored fiction still overrides the seeded theme. Deliberately does **not** grow the mechanic pool (that re-rolls calibration — a separate follow-on). `competitionThemes.ts` (`COMPETITION_THEMES`, `themeForWeek`, `applyTheme`) + `GameSessionAdapter` (`themedScaffold` wired into `runCompetition` + `competitionStagingView`, `ORWELL_COMP_THEMES` flag) + `CompetitionResultView.theme`; Engine + BDD `0125-competition-theme-variety.feature` (5 scenarios, in `cucumber.cjs`) + `competitionThemes.test.ts` + `competitionThemesLive.test.ts`. juryReach byte-identical; golden driver pinned off. Builds on 0042 + 0006/0028 + 0018/#1400 + 0001 + ADR 0003. |
| 0124 | [Deeper character evolution](./0124-deeper-character-evolution.md) | BDD | 🏗️ **Built (2026-07-12)** · **PO expansion of the 0041 review** — three deepening axes on the character-evolution linchpin, one default-off flag `ORWELL_SOUL_DEPTH`: (1) **multi-axis affect** — split the single `emotionalState` slider into independent `distress` + `confidence` axes (a houseguest can be cocky AND rattled at once; the distress axis drags a comp even when confident); (2) **strategic-temperament drift** — a bounded, mean-reverting `temperamentDrift` bends a houseguest's *effective* disposition under repeated pressure (a burned loyalist hardens toward paranoia) while the static CHARACTER disposition stays byte-stable; (3) **personality-tuned reactivity** (the PO's catch) — wire each NPC's starting `volatility` to their disposition (`VOL_OF`, as the player already does) + a disposition-scaled settle rate, replacing the flat `rng.next()`, **draw-preserving** so the seeded spine is byte-identical. All hidden (no number to the player, 0001), CHARACTER byte-stable (0007), persisted (0030), deterministic. Calibration-neutral off (juryReach + golden untouched). `emotionalArc.ts` (`evolveEmotion` soulDepth opt + `composedEmotion`/`effectiveDisposition`/`settleScaleOf`) + `GameSessionAdapter` (`emotionalReadOf`/`dispositionReadOf`/`applyDispositionReactivity` + the `ORWELL_SOUL_DEPTH` flag); Engine + BDD `0124-deeper-character-evolution.feature` (7 scenarios, in `cucumber.cjs`) + `deeperEvolution.test.ts` + `deeperEvolutionLive.test.ts`. juryReach byte-identical (flag off); deploy opt-in. Builds on 0041 + 0024 + 0028 + 0026 + 0044. |
| 0123 | [NPC-initiated deal offers](./0123-npc-initiated-deal-offers.md) | BDD + FE | 🏗️ **Built (2026-07-12)** · **PO ask** — closes the last gap in the deal system: player→NPC (`makeDeal`) and NPC↔NPC (`mintNpcDeal`, hidden) existed, but **no houseguest ever came to the player**. Now a motivated NPC **floats the player a deal at a lull** (the NPC→player counterpart of `makeDeal`), surfaced as a `deal-offer` pending resolved via the existing `submitDecision({ kind:"deal-offer", vote:"accept"|"decline" })` — **no new tool** (golden-safe, the FE generic decision card renders it). The offer's **kind is grounded** in the NPC's real read (strong bond ⇒ `final-two`; else `safety`); **accept** creates a real player↔NPC deal via the same `deals.make` spine (reconciles/folds like any deal, composes with 0121/0109/jury reliability); **decline** creates no deal + a small hidden directed cooling of the rebuffed NPC. **Player-witnessed** (not hidden — the NPC came to them, 0002), no number/sealed state in the offer. **Bounded** (one open offer, ≤1/in-game-week, probability-gated) + seeded. **Calibration-safe:** default-off `ORWELL_NPC_DEAL_OFFERS` + dedicated rng + lull-only ⇒ **juryReach byte-identical** (re-run green), golden fixture untouched. `GameSessionAdapter` (`maybeOfferPlayerDeal`/`resolveDealOffer` + `deal-offer` pending), `liveSeason.dealOffer`, `DECISION.playerOffer`; FE `orwellDecision.js` (`SINGLE_PICK_FIELD`/`titleFor`); Engine + BDD `0123-npc-initiated-deal-offers.feature` (6 scenarios, in `cucumber.cjs`) + `npcDealOffers.test.ts`; deploy opt-in. Builds on 0039 + 0121 + 0109 + 0017/0026 + 0034. |
| 0122 | [Deeper, daily NPC confessionals](./0122-deeper-daily-npc-confessionals.md) | BDD | 🏗️ **Built (2026-07-12)** · **PO expansion of the 0040 review**: NPC confessionals today are **thin** (biggest-threat + trust only) and **rare** (only the ~3–5 ceremony-standers confess, plus one random off-screen confessor per tick). This makes them **deeper** — five new engine-grounded facets (**plan** from their target+role, **standing** safe-vs-exposed from public role state, a **grudge** distinct from the current target via 0026 betrayal-shock, the **aftermath of a big conversation**, and **adjacent-move** reactions read through their bonds: "my ally won HOH", "they put my showmance up") — **triggered, not a checklist**: each facet renders only when the moment earns it, so the **HOH / nominees** get the deepest confessionals and a coasting houseguest gets a short one — and **daily** — a once-per-in-game-day sweep where **most** living houseguests confess, **skipping only a bare game** (`isBareGame`: no recent salient witnessed event AND no clear target/ally). Vault-only (sealed from player AND admin, unchanged from 0040), folded to soul (recall-able), seed-deterministic. **Calibration-safe by construction:** default-off `ORWELL_CONFESSIONAL_DEPTH` + a dedicated `confessional:` rng + anchored to the 0117–0119 in-game clock (runs only when `perConversationClockLive()`, pinned off in the golden driver ⇒ byte-identical off, no golden re-record). `confessionals.ts` (`confessionalFor` deep facets + `isBareGame`) + `GameSessionAdapter` (`sweepDailyConfessionals` at `turnIn`, `confessionalDepthFor`/`adjacentMoveFor`, the `ORWELL_CONFESSIONAL_DEPTH` flag); Engine + BDD `0122-deeper-daily-npc-confessionals.feature` (8 scenarios, in `cucumber.cjs`) + `deeperConfessionals.test.ts` + `dailyConfessionalSweep.test.ts`. juryReach byte-identical (flag off); deploy opt-in in `smoke.sh` + `orwell-install.sh`. Builds on 0040 + 0089 + 0090 + 0017/0026 + 0024 + 0066/0117–0119. |
| 0121 | [Deal depth: active-obligation kinds + reliability rewards](./0121-deal-depth-kinds-and-reliability-rewards.md) | BDD | ✅ **Built (2026-07-12); §7 R1–R3 shipped** · **PO expansion of the 0039 review**. All behind `ORWELL_DEAL_DEPTH` (default OFF ⇒ byte-identical; deploy opts in). **Live end-to-end:** both ACTIVE-obligation deal kinds (comp-throw resolves at the comp crown — broken iff the promisor wins; veto-save resolves at the veto ceremony — broken iff the promisee is left on the block) + the **loyalty-streak** reward (consecutive kept deals compound the honored fold, `DEAL_STREAK`; break resets) + the **reliable-ally protection** reward (existing reliability→nomination-safety, amplified by the streak). **§7 shipped:** R1 the diffusing "keeps their word" reputation reward (`spreadReliableReputation` + `GOSSIP_HEARD.reliable`, `dealReputationSink`; `dealReputation.test.ts`), R2 FE chat-extraction of the new kinds (**flag-gated off `/health.flags.dealDepth`** — no golden re-record; `test_0121_deal_kinds.py`), R3 deploy opt-in (`ORWELL_DEAL_DEPTH=1` in the installer + smoke). **Part 1 detail:** two ACTIVE-obligation deal kinds — `comp-throw` ("throw a comp for you", broken if the promisor competes/wins, kept if they throw) + `veto-save` ("use the veto on you", broken if the veto-holder leaves the promisee on the block, kept if they save them, no duty if they weren't nominated) — the first *positive*-obligation promises (a break is a FAILURE TO ACT); engine-adjudicated from the structured action, prose-independent, reusing the 0039 betrayal-shock + jury-demerit + reveal fallout. `src/domain/deal.ts` (`isPositiveObligation`/`positiveVerdict` + extended `DealKind`/`BindingAction`), `src/engine/deals.ts` reconcile resolves them at the act, `GameSessionAdapter` flag + `makeDeal` gate (off ⇒ refused). **Part 2 (to follow):** live-loop reconciliation (a `compete` action per competitor after a comp; a `veto-use` action after the veto ceremony) so they auto-fire; the three reliability rewards (reputation-spread + reliable-ally-protects + loyalty-streak); FE deal-extraction; deploy opt-in. Engine + BDD `0121-...feature` + `dealDepth.test.ts`. Builds on 0039 + 0109 + 0026 + 0014 + 0006b. |
| 0120 | [Strategic-drive off-screen cadence](./0120-strategic-drive-offscreen-cadence.md) | BDD | 🏗️ **Built (2026-07-12)** · **PO expansion of the 0038 review**: sharper / more-strategic houseguests INITIATE the hidden off-screen scheming a touch more often (the owner's "slight variance to personality / strategic intelligence"). `strategicDriveWeight(mental, style)` (bounded, positive, gently-sloped — sharpest schemes < ~3× most passive) + `STRATEGIC_CADENCE` (offscreen.ts); `GameSessionAdapter.initiatorDrive(id)` + the `ORWELL_STRATEGIC_CADENCE` flag (default OFF); `richOffscreenStretch` draws the initiator by a single-draw `weightedPick` when `initiatorDriveOf` is supplied (the orchestrator passes it only when the flag is on). **Calibration-safe like the sibling living-house flags** (0087/0085): off ⇒ the uniform `rng.pick` exactly ⇒ byte-identical (`juryReach` re-confirmed); on ⇒ same one `rng.next()` so the seeded stream stays in phase, colors outcomes within the band. Vault-free (engine-internal weighting; off-screen scenes stay hidden). Enabled in the deploy (`orwell-install.sh` + `smoke.sh`). Engine + BDD `0120-strategic-drive-offscreen-cadence.feature` + `strategicCadence.test.ts`. Builds on 0038. |
| 0119 | [Different events cost different amounts of the in-game day](./0119-per-event-felt-durations.md) | BDD | 🏗️ **Built (2026-07-12)** · **Phase 3 (final) of the in-game-time pivot** (owner directive 2026-07-11): a resolved beat advances the per-beat clock by its OWN felt duration — a quick nomination/veto ceremony ~1h, a competition ~3h, an eviction/finale ~2h (the existing `EVENT_DURATION`/`eventSpanHours` library, previously used only for narration placement) — instead of a flat +3h, so the day fills at a lived rate. `advanceClock(s, hours?)` gains an optional felt-duration (flat `CLOCK.perBeatHours` default = byte-identical); `beatFeltHours(beat)` (in `daySchedule.ts`) maps the beat→hours (null for inert/twist/terminal ⇒ flat default); `advanceGame` applies it ONLY when `perConversationClockLive()`. **Golden nuance:** unlike 0117/0118 the per-beat clock is ACTIVE in golden replay (master on there), so the gate is deliberately the per-conversation clock, which golden pins OFF ⇒ every beat keeps the flat +3h ⇒ recorded stream unchanged, **no re-record**; calibration (master off) skips the block ⇒ byte-identical. Pure presentation — draws no rng, never moves the seeded winner/eviction stream (`juryReach` band unchanged). Engine + BDD `0119-per-event-felt-durations.feature` + `perEventFeltDurations.test.ts`. Builds on 0117 + 0118 + 0066/#1125; preserves 0108. |
| 0118 | [Ceremonies as timed, telegraphed interrupts](./0118-ceremonies-as-timed-interrupts.md) | BDD + FE | 🏗️ **Built (2026-07-12)** · **Phase 2 of the in-game-time pivot** (owner directive 2026-07-11; interrupt-must-be-telegraphed confirmed 2026-07-12): the day has a KNOWN SHAPE — each ceremony is scheduled for an in-game phase (`DAY_SCHEDULE`, comps afternoon / evictions evening), the player is TOLD ahead of time ("the comp is this afternoon"), the narrator is PRIMED during the run-up (`renderGameContext` "COMING UP … this afternoon"), and when the clock reaches the time the ceremony fires + gathers the whole house (the 0106 set-piece; "IT IS TIME … production calls everyone to the living room"). Two caps, owner-confirmed: **SOFT bedtime** (player owns `turnIn`, unchanged — no forced curfew) + **HARD, telegraphed ceremony interrupt**. Engine: `src/engine/daySchedule.ts` (`DAY_SCHEDULE`/`MILESTONE_LABEL`/`nextMilestone`/`milestoneDue` — pure, Vault-free reads), `GameStateView.daySchedule?{next,phase,due}` + `GameSessionAdapter.milestoneDue()`, `renderGameContext` priming — all gated on `perConversationClockLive()`. FE: the day-schedule `due` flag cached at framing (`chat_helpers._LAST_MILESTONE_DUE`, keyed the single-tenant-safe `default`-fallback way) drives a time-aware branch in `agent_loop._want_advance` so the forced-advance fires the ceremony at its scheduled hour EVEN during engaging play (still one advance/turn, never on a progressed turn, yields to the social runway). **Byte-identical / golden-safe:** everything gates on the per-conversation clock, which the seeded spine (time-of-day off) + golden replay (per-conv off) both pin off ⇒ no `daySchedule`, no priming, no due-signal, no re-record. Engine + BDD `0118-ceremonies-as-timed-interrupts.feature` + `dayScheduleTimedInterrupts.test.ts` + FE `test_0118_timed_ceremony_nudge.py`. Builds on 0117 + 0106 + 0066/ADR 0006 + the L39b nudge; preserves 0108. |
| 0117 | [The house lives in in-game time](./0117-house-lives-in-in-game-time.md) | BDD | 🏗️ **Built (2026-07-12)** · **Phase 1 of the in-game-time pivot** (owner directive 2026-07-11): make in-game time the spine so the off-screen house keeps scheming AS THE CLOCK PASSES during the player's between-ceremony social play, not only in a burst when a ceremony beat resolves. Root cause: `orchestrator.maybeTurnDrivenTick`'s `if (!progressed && auxTicksNever) return` silenced the house on every social (aux) turn in production. Fix: when in-game time is genuinely flowing (`perConversationClockLive()` — master + per-conversation clock on, day started), a social turn now advances the per-conversation clock **and** fires an off-screen society tick — **debounced on ELAPSED IN-GAME HOURS** (`SOCIETY_TICK_HOURS = 1.5`), not once per tool call, so the society lives "with the clock" (~every couple of social turns; a beat's +3h jump always clears it). **In-game time ONLY — never the wall clock** (no watcher revived; the house still doesn't live while the player is away). **Byte-identical guardrails hold by gating on the live clock:** time-of-day OFF (the seeded calibration spine — `juryReach` band + `perConversationClockNeutral`/`nightGateNeutral` green) and golden replay (per-conversation clock off) both take the old early-return, so no new tick fires. The off-screen society is pure engine sim (no LLM/token cost). Two new Vault-free getters (`perConversationClockLive`, `inGameHour`); `SOCIETY_TICK_HOURS` in `sleepConstants.ts`. Engine + BDD `0117-house-lives-in-in-game-time.feature` (in `cucumber.cjs`) + `houseLivesInInGameTime.test.ts`. Builds on 0066/ADR 0006 + 0038 + 0078/0085/0086/0087/0089; preserves 0108. |


Each row is shipped as its own auto-merged PR. 0001–0008 are the priority invariants; 0009 is
the M5 integration seam (the engine's outward tool API for the front-end / agent), 0010 is the
MVP-1 one-liner deploy/update, and 0011+ are the gameplay loop & player experience. 0015 is the
single human-authored profile (the player); 0016 is the second walled surface (God Mode, walled
from the Vault even for the admin); 0017 promotes [decision 0002](../decisions/0002-relationship-model.md)
(the organic relationship model) into an executable spec. 0018–0019 make **Orwell *is* the game**
playable in the main chat: 0018 is the engine-owned per-moment narrator framing (the model speaks
as the house, never a generic assistant); 0019 is the agent turn-loop that drives play by calling
engine tools while the engine decides every outcome. 0021 makes it **multi-tenant**: one active
game per physical-world user, a sandbox per user, unlimited concurrent games across users — adding
**cross-user isolation** as a new guarantee alongside the Vault Wall.

## Amendments to shipped specs (implementer: pick these up)

Changes made to **already-built** features after they shipped. They are small, additive, and keep
every existing guarantee — but the implementer must fold them into the built code.

| Spec | Amendment | Why |
|---|---|---|
| **0004** (Done) | **(satisfied — implemented)** `CharacterFactory` also generates **public appearance/identity** fields (appearance, age, presentation/style) into the static `Character` (`character.md`); seed-stable, no aptitude/hidden data. Built in `src/engine/characterFactory.ts` (`generateAppearance`). See [0004 §8](./0004-replayability-and-naming.md#8-amendment--public-appearance-fields-for-0020-portraits). | Feeds the **Vault-free portrait descriptor** so houseguests look like who they are ([0020](./0020-player-experience.md) §5). |
| **0023** (Done) | **(satisfied — implemented by 0030)** The **persist → recall** half was **not durable** in the live game — state was memory-only, so an engine restart wiped every game. [0030](./0030-durable-game-persistence-survive-restart.md) (Done) wired the **disk-backed** `FileSaveStore` + load-on-resume/save-on-mutation into the live registry. | A started game must survive a restart; otherwise the onboarding overlay re-fires every load (user-reported). |
| **0018** (Done) | **B61 cast voices**: the woven game context now carries each ACTIVE houseguest's **curated public persona facets** (archetype, strategy style, background, age, appearance, presentation) as the narrator's per-person voice anchor, and `HouseguestCard` projects the same facets (+ a `jury` seat distinct from pre-jury `evicted`). The 0018 scenario wording drops "archetypes" from the banned list — stats, souls, and `hiddenElements` remain walled (the B42 live sentinel sweep now tags `hiddenElements`, not `background`, as the hidden string field). | The narrator was voicing 15 houseguests from a name + status word — structurally voiceless NPCs (experience audit N1/N2); the facets were already Vault-free for portraits (0004/0020). |
| **0025** (Done) | **B53 live firing**: the sealed reserve twists now fire in the LIVE game — `createCharacter` loads + seals the schedule (loop state + a Vault `reserved-twist` audit copy, both persisted: the snapshot now carries the Vault, audit I7), and a sealed **double eviction** runs the same night as a compressed second cycle (HOH → noms → veto → vote, the 0005 hard rules verbatim; same week number, two eviction ceremonies, both count for jury order; the outgoing HOH cannot take the second crown). New `twist-reveal` beat + moment prompt; three live scenarios appended to the .feature. | The twists were computed but never fired live (audit D6 + B8) — the pure module was proven, the game never used it. |
| **0009** (Done) | **Audit E20/E21 (2026-06-10)**: the caller-supplied-stats `resolveCompetition` tool is **removed from the player channel** (registry + dispatch + the `EngineCommands` port) — `runCompetition` over the LIVE house is the single outcome authority (B37); the pure domain `resolveCompetition` stays for tests/engine. `recordInteraction` now **requires the player in the witness set** (the player initiating counts; their seat is made explicit) and budgets hidden folds **per beat per directed edge** on top of the per-call cap. The `.feature` scenarios are amended accordingly (absence + refusal; the end-to-end loop expects the refusal). | E20: a seed-shoppable second resolver — caller stats + caller seed, nothing recorded, no folds — sat one call away on the player channel. E21: a witness set excluding the player minted off-screen "ground truth", and identical repeated calls could pump a hidden edge without bound. |
| **0004** (Done) | **(satisfied — implemented, PR #217)** **E38 / product ruling #1 (2026-06-10):** NPC names are **seeded samples from vendored real-name corpora** (`src/engine/data/givenNames.ts` + `surnames.ts`), replacing phoneme synthesis. The do-not is amended to "**no fixed cast**": corpora are raw material; no full-name+persona pairing is hard-coded (BDD-proven); the legacy Bible's names stay banned (excluded from the corpora); cross-seed carryover is measured on **identity**, not bare first names; the inverse realism gate (every part corpus-membered) is in the BDD. | The generated names read as gibberish on the game's first screen — the product ruling requires names "determined based on what could be a real name." |
| **0047** (Done) | **(satisfied — implemented, PR #217)** **E12+T2:** eviction votes are **secret ballots** — the staged reveal reads anonymized ballots ("a vote to evict …"), `EvictionView` drops voter attribution, and the per-week ballots unseal only in the 0048 retrospective (`RetrospectiveView.evictionVotes`). **E34:** a surviving player records their **own goodbye message** via a new `goodbye-message` pending (tone = the player's choice, folded via `goodbyeMannerFor` exactly as NPC tones; the engine never authors a player goodbye). Two scenarios appended. | Real-BB ballot secrecy (rogue votes, paranoia) was impossible, and the engine spoke for the player on jury management's signature lever. |
| **0037** (Done) | **(satisfied — implemented, PR #217)** **E37:** the player-JUROR asks their own finale question via a new scoreless `juror-question` pending (free text; the finalist's answer remains engine-chosen). Scenario appended. **E35 (with 0005/0042):** the veto chip draw is now a witnessed `veto-draw` beat preceding any winner, and ANY player in the drawn field — chip-drawn included — declares the canonical comp intent; **E36:** the Final-4 veto pending carries the honest (possibly empty) legal save set and refuses a save it cannot honor. | The juror's seat auto-answered the player's own question; the chip draw was invisible; the F4 "use" choice was accepted then silently inverted. |
| **0032** (Done) | **`web_search` moves from the drop-set to the keep-set** (ruling 2026-06-09): the router mounts under the game build, the agent tool surface includes it, and the Settings `search` tab stays as **admin-only global config**. The agent uses it **in-fiction** — real-world references are looked up silently and synthesized in the houseguest's voice; search informs flavor/real-world knowledge **only**, never a game fact or outcome (engine tools remain the sole game truth). Implementation prompt: queue **C32**. | The model must handle real-world references the player makes mid-conversation (e.g. a new movie) without breaking character — critical to "the conversation is the game" (ADR 0003). |
| **0039** (Done) | **(satisfied — implemented, audit E42/E43/E46/T1)** Deals are **horizon-aware**: `safety`/`vote` bind through *their week's* eviction (the vote endpoint or `expireWeekScoped` at the rollover); `final-two`/`target-other` bind until broken. **Honoring pays**: each honoring binding action applies the bounded `DEAL_IMPACTS.honored` fold (trust/alignment + the E54 `reliability` evidence signal); `BindingAction.alternatives` scopes honor to actions where the partner was a real option. **Every binding actor reconciles** — NPC eviction votes, HOH tie-breaks, and the Final-3 eviction flow through `bindingActionsFor` in the live commit path, not only the player's actions. **NPC↔NPC deals exist**: the tightest unbound NPC pair occasionally seals a Vault-held pact at nominations (`DECISION.npcDeal`), binding through the same ledger. Live gate: `tests/integration/liveDealReconciliation.test.ts`. | The audit found deals self-extinguishing after one ceremony, honoring building no trust, NPC votes never reconciled, and `npcOnly()` dead — three shipped systems were flavor, not consequence (Theme 6). |
| **0040** (Done) | **(satisfied — implemented, audit E55/C12)** Confessionals are **structured** (`ConfessionalContext`: trigger + soul-derived mood + seeded phrasing — content names its trigger and varies across a season), fire at **nominations, the veto ceremony, AND eviction night**, and finally reach the confessor's own soul (`recordConfessionalToSoul` is called live, mirrored into `soul.memory` so recall survives a restart). | One canned line all season (also the 0048 unsealing payoff), noms-only firing, and the recall half unwired (zero `recordConfessionalToSoul` callers). |
| **0041** (Done) | **(satisfied — implemented, audit E50/E51/E52)** Off-screen scene emotions are **per-role** (`offscreenEmotion(type, role)`: a betrayal's initiator *schemes*, the partner is *betrayed*; `recordOffscreenScene` evolves both). The dead arc vocabulary fires live: `survived-vote` for the surviving nominee at every eviction; `comp-loss` for contested losers (the veto field; the final-3 crown). `evolveEmotion` carries ADR 0001's bounded per-moment temperature roll (`emotional.swingTemperatureWeight`, seeded side rng) and **delegates to the canonical 0028 `emotionalModifier`** (one swing formula, soul-specific baseline). | The betrayer was given the victim's emotion while the victim's soul never moved; half the 0041 vocabulary never fired; the live swing skipped the ADR 0001 roll beside a dead parallel formula. |
| **0026/0017** (Done) | **(satisfied — implemented, audit E47/E48/E54 + E44)** `EdgeSignals` gains ADR 0002's **`reliability`** evidence signal (fed by honored deals, veto saves, protective votes; torn down by betrayal-shock; consumed by `bondStrength` via `RELIABILITY_WEIGHT`; never decays; lossless round-trip with legacy-save defaults). `CEREMONY_IMPACTS` holds **named impact objects**: `comp-won` is `{threat:+0.14}` only (the house keeps liking its winner) and the eviction fold scales by recorded **manner** (`EVICTION_MANNER_SCALE`). Gossip **receipt folds** (`GOSSIP_HEARD`, confidence-scaled, never the player's own edges) let a rumor genuinely move third-party reads. | A comp win soured the whole house on its winner; a fully-expected eviction folded full betrayal-shock; trust was pure sentiment with no evidence channel; a rumor never changed any mind (Theme 6). |
| **0028** (Done) | **(satisfied — implemented, audit E52/E53)** `variableWeights` is pruned to the fields with REAL consumers: `initiative` (the `rankApproaches` ordering-variance band) and `allianceShift` (the `chooseStrongestBond` near-tie wobble); the four consumer-less fields are deleted. `EmotionalConstants` gains `swingTemperatureWeight`; `emotionalModifier` takes an optional per-soul `baseline` and is the ONE live swing formula (0041 delegates to it). | The "temperature is per-moment, per-variable" config was decorative (zero consumers) while subsystems rolled ad-hoc variance — a config that drives nothing is a lie. |
| **0036** (Done) | **Audit E60 (2026-06-10)**: `socialInitiatives` no longer ships an engine-authored pretext line ("wants a word with you"). The projection carries a **coarse categorical motive** — `bond` (the tie drives the approach) or `probe` (the threat read does) — computed from the same `rankApproaches` agenda; the GM voices it in its own words and the FE chip maps it to short UI copy. Never a relationship number. | ADR 0003 principle 2 names the canned pretext as the smell: hand the model the FACT/drive, let it write the line. The drive was computed and then discarded. |
| **0050** (Done) | **0065 — photo-first OOBE (2026-06-20):** the casting interview now **opens on the cast photo** ("producers ask what you look like before anything else"). New scalar `castPhoto` on `UpdateCastingReq` (`"uploaded"` \| `"skipped"`; any non-empty string marks it handled) is the **FIRST entry of `CASTING_COVERAGE`**, so a fresh interview's `next`/`missing[0]` is the cast photo before the name. Engine-driven and **OPTIONAL** — it does **NOT** gate casting `ready` (name-only), so `createCharacter` finalizes whether the photo was uploaded or skipped. Flows through the existing intake plumbing (ordinary scalar; merge/overwrite/capture/status iterate the coverage list) and the Vault-free `updateCasting` boundary untouched. The `character-creation` moment prompt opens the producer on the 📷 panel accordingly. See [0050 Amendment (0065)](./0050-casting-interview.md#amendment-0065--the-cast-photo-is-the-first-casting-step-photo-first-oobe). | The cast photo was an afterthought ("get one before you wrap"); making it casting step #1 matches "what do you look like?" being the first thing real casting asks — while staying skippable so it never blocks a player who doesn't want a photo. |
| **0023/0055** (Done) | **[ADR 0005](../decisions/0005-split-authority-by-openness.md) — generative-consequence path + expressive-non-collapse gate (PR #355, 2026-06-20):** `recordInteraction` / the 0023 `ConsequenceEngine` accept an optional, Vault-free `consequence` descriptor (per-edge `{ toward, direction, emphasis? }` + `rationale`). The model proposes the *shape* (which edge moves, which direction, relative `emphasis`); the engine still owns the bounded, seeded *magnitude* (`direction`→base impact, `emphasis`→a clamped 0.6/1.0/1.4 multiplier — **no raw number crosses**). `kind` stays the floor: no descriptor ⇒ byte-identical fold. The MCP boundary shape-guards a malformed descriptor (clean 400, not a 500); the FE schema/client forward it and the 0055 `_auto_record_scene` can propose one. New permanent gate: `tests/unit/expressiveNonCollapse.test.ts` + `frontend/tests/test_expressive_non_collapse.py` (the WINNER/NEW-HOH desync branches were phase-scoped so creative prose can't trip a board-outcome rail-correction). | A growing LLM↔engine sync spine risked flattening creative play by forcing the open set (the meaning/texture of social play) through a closed 7-way `kind` enum; ADR 0005 splits authority by **openness**, not layer, and makes non-collapse a regression gate. |
| **0006** (Done) | **Staged-rounds evolution (owner decision):** a competition is now an **endurance-style sequence of visible ELIMINATION ROUNDS** over the still-in field — each round the player picks their approach (compete / throw / play-safe) for THAT round, seeing who remains, and the lowest score DROPS until one stands. Reuses the 0006/0028 math (`resolveElimination` reads the same scores to pick the lowest); the staged sub-loop is `CompetitionProgress` / `advanceCompetition` in `src/engine/liveSeason.ts`, surfacing a per-round `comp-round` pending (with the **still-in field**) through the existing decision seam (the FE per-round prompt replaces the single binding popup). **Anti-sycophancy holds per round**: each round's approach is committed BEFORE it resolves (structured selection only — never prose), LOCKED after, adaptation forward-only. Favorite-win calibration holds (≈73%, inside the 65–80% band) — `tests/unit/stagedCompetition.test.ts`. See [0006 §8](./0006-outcomes-by-stats-and-temperature.md#8-staged-rounds-evolution-the-per-round-approach). | The single up-front, irrevocable approach was too rigid — the player committed blind to how the field would narrow. Strategy should adapt to the field as it thins (everyone left an ally → throw; a threat still in → keep competing), while the per-round commit-before-result keeps a loss from being re-labeled a "throw." |
| **0063** (Done) | **AI-driven identity half (issue #544, the deferred second half of the 2026-06-23 ruling):** the FE producer-LLM now **proposes** each houseguest's **descriptive** identity facets (heritage/ethnicity, gender presentation, orientation, disclosure, age) to U.S.-population rates and writes them back via the new `recordCastIdentity` write-back (the four-place FE-driven pattern); the engine **validates + repairs** the whole-cast proposal through `repairDiversityLayer` (the SAME floors/caps/weighted-expectation as the seeded floor — even a monochrome/biased proposal is repaired to a realistic cast), **re-grounds `skinTone` from the FINAL heritage** (the PR #527 hinge), folds the PUBLIC facets onto the byte-stable cast, and re-seals each PRIVATE orientation into the Vault. The **hard boundary**: only descriptive identity is accepted — NEVER a hidden game weight (the seeded Day-1 read / competition leans stay engine-owned; anti-sycophancy #3 + juryReach). **Calibration-neutral** (the layer rides the SAME isolated descriptive sub-streams — the #338 golden test stays the proof; an empty proposal is byte-identical to the floor). FE driver `frontend/src/orwell_cast_identity.py` runs BEFORE deep authoring (pipeline: identity → author → shoot); with no model the deterministic weighted floor (PR #527) simply stands. Gates: `tests/unit/castIdentity.test.ts` (repair-holds + boundary), `frontend/tests/test_544_cast_identity.py`. | PR #527 shipped the fix+retune half (US-population-weighted floor); this adds the LLM-driven layer the owner asked for ("I need the LLM to seed the characters… I want AI to drive the game engine") while the engine keeps the realism guarantee. |
| **0064** (Done) | **Instant cross-window HUD parity from the chat-turn path (ship-gate F5, status/gadget half).** The 0064 `game-updated` server-push fired from only THREE `orwell_routes.py` endpoints (decision + the two self-eviction routes) — **never** the chat-turn path. So a chat turn whose agent-loop tools (or the 0055 `_auto_record_scene` belt) mutate engine state refreshed **only the SENDER's** HUD (chat.js → the g15 dispatcher, client-side); peer windows got no push and stayed stale until their 20–30s poll (observed live: window A "1 of 15 met" / window B "0 of 15"). The chat DONE seam now calls `publish_game_updated_after_turn(user, beatSeqBefore, toolsCalled)` (`routes/chat_helpers.py`) — gated on an **actual mutation** (a `GAME_ENGINE_WRITE_TOOLS` call ran, or the user's `beatSeq` advanced — catching the FE error-correction belts) — and chains a second push onto the E22/0055 `recordInteraction` fallback task (which mutates after settle). The FE reaction is **unchanged** (the `game-updated` SSE still routes through the ONE g15 dispatcher via `sessionSync.js notifyGameUpdated → window.orwellGameChanged('sync:game-updated')`). Vault-free (a bare "refetch" signal — no state body; each window re-fetches its own Vault-free projection). See [0064 §B/D amendment](./0064-live-multi-device-game-sync.md#amendment-2026-06-28--instant-hud-parity-from-the-chat-turn-path-f5-statusgadget-half). Gates: the harness HUD-parity gate `mirror_hud_parity.mjs` (via `run_mirror_gate.sh MIRROR_HUD=1` — key-free, RED→GREEN) + `frontend/tests/test_1130_hud_parity_instant.py`. | A streaming game turn is the dominant mutation path, and it was the ONE mutation seam that never pushed — so two windows on one game silently diverged on every turn until the slow poll. Closes F5's status/gadget half (the complement to the R2/ADR 0015 chat-render half). |
| **0006** (Done) | **PO review 2026-06-28 — competition intent is asked ONCE, up front.** The staged per-round approach (the amendment above) was **cosmetic**: the single up-front roll already fixed the outcome, so later-round approaches were non-binding flavor (`binding: false`). The PO ruled to drop them — the player declares compete/throw/play-safe **once** (round 1, the binding intent the roll honors), then the elimination rounds play out as drama without re-asking. `advanceCompetition` no longer surfaces the per-round `comp-round` pending; the round-1 binding prompt in `resolveHohBeat`/`resolveVetoComp` is unchanged. **Calibration-neutral** — no rng/outcome change, byte-identical (`stagedTrajectoryNeutral` + full BDD green). Gates: `compIntent.test.ts` + `stagedCompetition.test.ts`. *(Single-roll model kept — the "Path B" per-round re-resolution rebuild was declined.)* | The per-round prompt asked the player to re-choose an approach that could not change the already-decided result — clicks with no stakes. |
| **0006** (Done) | **PO review 2026-06-28 — upset-band tune + NPC competition intent.** (a) **Upsets a tad more common:** `temperatureConstants.ts` `outcome.temperature` 0.36 → 0.40, so a clear stat favorite drops from ~64% to ~59% average across field sizes (raw comp stats less dominant now that emotions/sleep add depth). A DELIBERATE calibration retune — juryReach EARNED-WINS re-verified green + full unit suite (1957) green; band gate widened in `stagedCompetition.test.ts` to ~50–70%. (b) **Every NPC carries a comp intent** (compete/throw/play-safe), not just the player — `deriveNpcCompIntent` (pure, in `liveSeason.ts`) + `SeasonCtx.compIntentOf` applied in `roundIntents`, wired live via `GameSessionAdapter` behind **opt-in `ORWELL_COMP_INTENT`** (byte-identical when off ⇒ the calibration spine is untouched). A nominee fights; a lay-low houseguest with a strong ally in the field throws; a cautious houseguest a rival reads as a threat plays safe. Single-roll model kept (Path B declined). Gate: `npcCompIntent.test.ts`. | Comp stats were too dominant relative to the deepened emotion/sleep systems; and only the player had a competition intent — NPCs always blindly competed. |
| **0031/0007** (Done) | **PERSIST-8 (2026-07-05, #1186):** the 0031 fail-closed checkpoint's `toGameState` projection predated features 0058–0107 and never verified ~a dozen newer, genuinely-accumulating `SessionCore` dimensions (deals, deep profiles, confidences, secrets-as-power, nomination history, texture overrides, seeded-tie surfacing…). Adds `sessionCoreCounts`/`sessionCoreCountsNonDecreasing`/`sessionCoreIsSuperset` (`src/engine/sessionSnapshot.ts`) — the engine-layer companion to `saveState.ts`'s counts/isSuperset — wired into `Orchestrator.checkpoint` beside the existing 0007 check. Gate: `tests/unit/sessionCoreCheckpointCoverage.test.ts`. | A dozen newer accumulating dimensions could silently thin across a save/restore without tripping the non-degradation guard — exactly where mandate #4 is supposed to be strongest. |
| **ADR 0004 / 0024** (Done) | **PERSIST-1/PERSIST-4 + PERSIST-7 (2026-07-05, #1190 + #1204):** a fail-safe dimension guard + a live provider pin for the embedding/vector layer (guards a silent dimension mismatch from a provider/model swap mid-season; pins the provider actually used per save so a later provider change can't misinterpret old vectors) — `src/ports/VectorIndex.ts`, `src/adapters/{engine/SoulStore,inmemory/InMemoryVectorIndex,sqlite/SqliteVectorIndex}.ts`, `src/composition/engineRoot.ts` (gate: `tests/unit/embeddingPinAndDimGuard.test.ts`) — **plus** the `ORWELL_STORE=sqlite` opt-in tier's vector recall index is now persisted + reloaded across an engine restart (was silently resetting semantic recall to cold on every reboot). Gate: `tests/unit/sqliteVectorIndex.test.ts`. | A provider/model swap or a sqlite-tier restart could silently corrupt or cold-reset semantic recall with zero operator signal — a mandate-4 violation on the one persistence tier meant for a real multi-user deploy. |
| **0023/0055** (Done) | **Offense P1 — third-party consequence pitches (2026-07-05, #1188):** extends the ADR 0005 generative-consequence descriptor (the model proposes shape, the engine keeps the bounded seeded magnitude) to **edges between two NPCs**, not just the player's own — a first playable-offense lever (turning the house against a rival by proxy). `src/engine/consequence.ts`, `src/engine/relationshipConstants.ts`, `src/ports/EngineCommands.ts`, `src/adapters/engine/EngineCommandsAdapter.ts`, `src/adapters/mcp/McpServer.ts`, `src/surfaces/tools/registry.ts`, `frontend/src/agent_loop.py`, `frontend/src/tool_schemas.py`. Gates: `tests/unit/thirdPartyConsequence.test.ts`, `frontend/tests/test_0005_generative_consequence.py`. | The consequence descriptor only ever moved the player's own edges — the player had no offense lever that could tilt the house against a rival without being the direct target of the retaliation. |
| **0002/0038** (Done) | **Gossip genuinely distorts (SG-6, 2026-07-05, #1189):** diffusion previously only decayed *confidence* on each retelling; the belief's *content* never actually mutated, so a third-hand rumor read suspiciously as accurate as the original. `deriveTrajectory`-adjacent diffusion in `src/engine/gossip.ts` now genuinely distorts the propagated belief, not just its confidence. Gate: `tests/unit/gossipDistortion.test.ts`. | Undermined the "distorted belief with a source + confidence" model the event/visibility architecture is built around — a rumor never actually became a rumor. |
| **0013** (Done) | **Diary-Room producer invite activated (§5, 2026-07-05, #1202):** the built-but-dead producer invite trigger into the Diary Room now fires. `frontend/routes/chat_helpers.py`, `src/engine/diaryRoom.ts`, `src/adapters/engine/GameSessionAdapter.ts`, `src/ports/GameSession.ts`. Gates: `frontend/tests/test_diary_room_producer_invite.py`, `tests/unit/diaryRoom.test.ts`. | A wired mechanic (0013 §5) sat dormant because nothing ever called it. |
| **0084/0090** (Done) | **Per-NPC voice fingerprint reaches the roster (I6 distinct-voices, 2026-07-05, #1205):** the built voice fingerprint wasn't being threaded into the per-turn roster the narrator actually reads, so voices sounded flatter in practice than the underlying system intended. `src/adapters/engine/GameSessionAdapter.ts`, `src/engine/momentPrompts.ts`, `src/engine/voice.ts`. Gate: `tests/unit/rosterVoiceFingerprint.test.ts`. | The voice system existed but its output wasn't reaching the prompt that actually narrates each turn. |

New cross-cutting expectations introduced by drafts (not yet built, listed so the implementer
plans for them): **0020** adds `gameStatus` (a Vault-free public-state projection for the status
panel) and `portraitDescriptorFor` (a Vault-free descriptor over 0004's public appearance facets);
**0018/0019** formalize `getMomentPrompt` + the `pendingDecision`/`executeDecision` decision seam
(as built: `submitDecision`, with `advanceGame` driving the loop — the engine surfaces the legal
option set; binding choices execute only through validation).

## Ship-gate F5 — realtime two-window live-mirror parity (FE; executable backing) — ✅ CLOSED (2026-07-09, #1276)

> **STATUS: CLOSED + CI-gated (2026-07-09, PR #1276 `fa60f70c`).** The former #1 release blocker is
> fixed and the harness gate is now **wired into CI as a required check** (via `ci-gate`) — no longer
> "run manually." A late 2026-07-09 re-verification (#1269 / #1274) had briefly re-flagged the harness
> RED (a render-race regression on `bUsesIncrementalRenderer`); #1276 fixed the race AND wired the gate,
> so F5 is now proven green on every PR rather than by manual harness run. The prose below is retained
> for the design history; the "one owed follow-on" (CI wiring) it names is **done**.

The launch-acceptance bar's **F5** (realtime two-window mirror parity — historically the #1 release
blocker; `docs/audits/2026-06-27-ship-gate.md`) is FE-side and has **executable BDD backing outside the
Cucumber lane** (that lane is the TS engine and cannot run an FE-render scenario). Per
[ADR 0015](../decisions/0015-collapse-duplicated-live-render-paths.md) (the render-layer half of
[ADR 0012](../decisions/0012-two-window-lockstep-mirror.md) / refactor-roadmap **R2**), message
*delivery* is sound — the defect is a render-layer duplication, and the fix collapses the observer
onto the sender's one incremental renderer. Its acceptance is the Given/When/Then in
[ADR 0015 §Testability](../decisions/0015-collapse-duplicated-live-render-paths.md#testability--acceptance):
*two windows on one started game → A streams a turn → B renders it live, through the same incremental
renderer, within a bounded lag → reasoning never reaches the public bubble.* Executable gate:
`docs/audits/playtest-harness/mirror_live_parity.mjs` (run via `run_mirror_gate.sh`, key-free against
a deterministic fake streamed model — harness README §10) + the `frontend/tests/test_0012_mirror.py`
tripwire. **R2 landed 2026-06-27** (`bb2c3076`/`14d071c1`: `resumeStream` now feeds the shared
`createStreamRenderer` incremental path via `_renderLiveStream`, the same machinery the sender uses)
and the fast tripwire is **green** — the `xfail` marker has been removed from
`test_chat_client_mirror_does_not_full_repaint_per_delta` (it source-pins that `resumeStream` no
longer full-`innerHTML`-repaints). **The harness gate is now wired into CI as a required check**
(#1276, 2026-07-09; runs in `ci-gate`) — the previously-owed follow-on is complete, and F5 is proven
on every PR rather than by manual `run_mirror_gate.sh`.

## FE UI, accessibility & transport tracks (2026-07 — GitHub-issue tracks, not feature-numbered)

Several substantial FE workstreams land as their own GitHub-issue tracks rather than numbered
`docs/features/` specs. Recorded here so the status index isn't the only place they're visible.

- **WebSocket Phase-1 transport — landed DORMANT (ADR 0017 / 0018; spec
  `docs/design/websocket-phase1-protocol.md` + test plan).** The full Phase-1 stack merged behind the
  **`ORWELL_WS_TRANSPORT`** flag (**default OFF** in Phase 1 — the route/handshake are inert until a
  client negotiates the socket): per-device layout LWW storage (#1283), client transport + chat splice
  + fallback negotiation (#1284), the server route (replay-then-tail over `agent_runs`, #1285),
  per-device layout client wiring (#1286), the decision-card confirm `decision` up-frame (#1287), and
  the server turn producer + layout leg (#1293) — plus the poll-kill batches that stand redundant HUD /
  gadget / Memory-Wall polls down under WS (#1290 / #1291 / #1294). **Genuinely OPEN:** the actual
  turn-on (flipping `ORWELL_WS_TRANSPORT` on + the live two-window verification cadence) is Phase-2 and
  not done. ADRs 0017/0018 are *Proposed*.
- **Apple-HIG conformance audit (#694) — substantially shipped.** The audit
  (`docs/audits/2026-07-09-hig-audit.md`, #1273) filed ~12 findings; all but **F-CHROME-1** shipped as
  a11y floors on the glass/kit chrome: streaming-transcript quiet + skip link (#1270), the safe P2/P3
  subset (#1271), kit titlebar 44pt hit area + title-only aria (#1278), the glass CSS a11y floors
  F-CONTRAST-1/2 + F-MOTION-1 + F-SAFE-1 + F-STATE-1 (#1280), and heading-hierarchy repair (#1279).
  **Residual: F-CHROME-1** — finish the legacy `.modal-content` → OrwellWindow-kit migration (this is
  the #660 track), tracked below.
- **#660 window-kit migration — game-build surface DONE; residual is full-workspace only.** The
  convention gate + ratchet (#1268), the `ui.js` confirm/prompt dialogs (#1281), and the **Theme window
  drag** (#1289, the F-CHROME-1 game-build residual) migrated onto the OrwellWindow kit. The game-build
  surface is on the kit; any remaining legacy `.modal-content` families are outside the game build.
- **#738 Liquid-Glass legibility & a11y batch — substantially shipped.** A large batch of glass
  legibility/a11y items merged incrementally: item #1 chat-transcript legibility floor (#1259), item #5
  menus-on-light-glass + item #23 sent-timestamp (#1260), item #4 haptics/audio a11y (#1261), item
  #2/#739 theme-tinted toggle (#1262), items #8/#13/#22 (#1263), item #19 APCA/OKLCH (#1264), item #20
  glass performance hardening (#1275), and item #9 chat scroll-edge mask + recede-on-scroll banner
  (#1292). Item-level completeness is tracked in issue **#738**; a small residual tail remains open
  there.
