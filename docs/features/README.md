# Feature specs

Drafted, stack-agnostic feature definitions for the `orwell` build. Each feature is authored
here **before** implementation and handed to an implementer (a separate Claude Code
instance). The author drafts; the implementer builds to green and refactors.

## Convention

Each feature is two files, numbered by **build priority** (lower = built first):

| File | Role |
|---|---|
| `NNNN-<slug>.md` | **Design note** — context, scope, the port/tool contracts involved, test strategy, acceptance criteria, and an implementer handoff with open questions. |
| `NNNN-<slug>.feature` | **Executable spec** — Gherkin scenarios. Failing-first; the implementer makes them pass. |

## Rules these specs must hold to

- **BDD/TDD-first.** The `.feature` file is written to fail, then implemented to green
  (`docs/CLAUDE_CODE_INSTRUCTIONS.md` §9, `docs/bb-sim-spec.md` §12).
- **Roles only, never names.** No houseguest or player names appear in scenarios — only
  roles (player, admin, NPC, HOH, nominee, evictee, veto winner). Fixtures are **generated**;
  sample-save content is format-only and is never used as seed/test data.
- **Definition of done** (per `CLAUDE_CODE_INSTRUCTIONS.md` §11): a passing name-agnostic
  feature test; Vault isolation verified on the relevant player **and** admin surfaces;
  domain logic unit-tested with a **seeded** randomness source; no Vault read reachable from
  any player- or admin-facing adapter/tool.

## Build priority order

From `docs/CLAUDE_CODE_INSTRUCTIONS.md` §9. Draft and build in this order:

1. **Vault isolation (incl. God Mode)** — `0001-vault-wall-isolation`
2. Event visibility & propagation
3. Behavioral fidelity
4. Replayability / naming
5. Competition eligibility
6. Outcomes by stats + temperature
7. Persistence non-degradation
8. Daily-event invariant

## Status legend

The **[index](#index)** below is the single source of truth for per-feature status — the prose in
`CLAUDE.md` and the audit ledgers can drift, so the index is reconciled against the actual code.

| Token | Meaning |
|---|---|
| ✅ **Built** | Shipped and gated (see the **Gate** column). |
| 🚧 **Built · follow-on owed** | Shipped, with one named remaining item (host smoke, browser render, an FE chunk…). |
| 📝 **Spec only** | Authored, not yet built. |
| ⏸ **Deferred** | Intentionally not built. |

**Gate** — how the feature is verified: `BDD` (Cucumber, listed in `cucumber.cjs`) · `unit` (Vitest) ·
`FE` (front-end pytest) · `engine` (Vitest, no `.feature`) · `scripts` (deploy) · `—` (none yet).

> **Audit (2026-06-20).** Every feature **0001–0063** was cross-checked against its source artifact
> (not its prose). **No orphaned or untracked unbuilt specs.** Not-built specs: **0022**
> (⏸ deferred). **0064** (live multi-device game sync) shipped its **stopgap** (the canonical game
> session — every device on one chat) 2026-06-20; the Messenger-style sync + window/HUD layout sync
> are the owed follow-on. **0062** (the move-in zeitgeist snapshot) **shipped 2026-06-20** —
> BDD-gated, frozen +
> seed-reproducible + flavor-only (outcome-invariant). Tracked here and in the authoritative
> [open-items ledger](../audits/2026-06-10-full-product-audit.md).

## Index

| # | Feature | Gate | Status |
|---|---|---|---|
| 0001 | [Vault Wall isolation](./0001-vault-wall-isolation.md) | BDD | ✅ Built |
| 0002 | [Event visibility & propagation](./0002-event-visibility-and-propagation.md) | BDD | ✅ Built |
| 0003 | [Behavioral fidelity](./0003-behavioral-fidelity.md) | BDD | ✅ Built |
| 0004 | [Replayability & naming](./0004-replayability-and-naming.md) | BDD | ✅ Built · amended (E38/D8: real-name corpora + entropy seed) |
| 0005 | [Competition eligibility](./0005-competition-eligibility.md) | BDD | ✅ Built |
| 0006 | [Outcomes by stats + temperature](./0006-outcomes-by-stats-and-temperature.md) | BDD | ✅ Built |
| 0007 | [Persistence non-degradation](./0007-persistence-non-degradation.md) | BDD | ✅ Built |
| 0008 | [Daily-event invariant](./0008-daily-event-invariant.md) | BDD | ✅ Built |
| 0009 | [MCP tool boundary](./0009-mcp-tool-boundary.md) | BDD | ✅ Built |
| 0010 | [One-liner deployment & update](./0010-deployment-one-liner.md) | scripts | 🚧 Built · real-Proxmox-host smoke owed |
| 0011 | [Weekly loop orchestration](./0011-weekly-loop-orchestration.md) | BDD | ✅ Built |
| 0012 | [Conversation & scene system](./0012-conversation-and-scene-system.md) | BDD | ✅ Built |
| 0013 | [The Diary Room](./0013-diary-room.md) | BDD | ✅ Built |
| 0014 | [Jury & endgame](./0014-jury-and-endgame.md) | BDD | ✅ Built |
| 0015 | [Character creation (OOBE)](./0015-character-creation-oobe.md) | BDD | ✅ Built |
| 0016 | [God Mode (admin port)](./0016-god-mode-admin.md) | BDD | ✅ Built |
| 0017 | [Relationship model](./0017-relationship-model.md) | BDD | ✅ Built |
| 0018 | [Narrative & moment orchestration](./0018-narrative-moment-orchestration.md) | BDD | ✅ Built |
| 0019 | [Agent-driven play loop](./0019-agent-driven-play-loop.md) | BDD | ✅ Built |
| 0020 | [Player experience (status, decisions, portraits)](./0020-player-experience.md) | BDD | ✅ Built |
| 0021 | [Game session & save lifecycle (per-user sandboxes)](./0021-game-session-and-save-lifecycle.md) | BDD | ✅ Built |
| 0022 | [Player experience MVP-2 (rich game UI)](./0022-player-experience-mvp2.md) | — | ⏸ Deferred |
| 0023 | [Consequence & memory (the MVP-1 backbone)](./0023-consequence-and-memory.md) | BDD | ✅ Built |
| 0024 | [Soul storage & memory recall (md + vector)](./0024-soul-storage-and-memory-recall.md) | BDD | ✅ Built |
| 0025 | [Reserve twists (Vault-sealed, engine-timed)](./0025-reserve-twists.md) | BDD | ✅ Built · amended B53 (fires live) |
| 0026 | [Relationship math](./0026-relationship-math.md) | BDD | ✅ Built |
| 0027 | [NarrativePort LLM adapter](./0027-narrative-port-llm-adapter.md) | BDD | ✅ Built |
| 0028 | [Temperature & emotional-modifier constants](./0028-temperature-and-emotional-constants.md) | BDD | ✅ Built |
| 0029 | [App administrator role & user management](./0029-app-admin-and-user-management.md) | FE | ✅ Built |
| 0030 | [Durable game persistence (survive restart)](./0030-durable-game-persistence-survive-restart.md) | BDD | ✅ Built |
| 0031 | [Game orchestrator & integrity watcher](./0031-game-orchestrator-and-integrity-watcher.md) | BDD | ✅ Built |
| 0032 | [Front-end surface reduction (the "game build")](./0032-frontend-surface-reduction-game-build.md) | FE | ✅ Built |
| 0033 | [Dynamic player tagline](./0033-dynamic-player-tagline.md) | unit | ✅ Built · recorded deviation E87a |
| 0034 | [Live weekly progression & decision seam](./0034-live-weekly-progression-and-decision-seam.md) | BDD | ✅ Built |
| 0035 | [Live off-screen life (running watcher)](./0035-live-offscreen-life-running-watcher.md) | BDD | ✅ Built |
| 0036 | [Live social surface (approaches + Diary Room)](./0036-live-social-surface-approaches-and-diary-room.md) | unit | ✅ Built · recorded deviation E87a |
| 0037 | [Live jury-vote choreography (the finale)](./0037-live-jury-vote-choreography.md) | BDD | ✅ Built · + finale UI |
| 0038 | [Live off-screen society](./0038-live-offscreen-society.md) | BDD | ✅ Built |
| 0039 | [Promise & deal tracking](./0039-promise-and-deal-tracking.md) | BDD | ✅ Built |
| 0040 | [NPC confessionals](./0040-npc-confessionals.md) | BDD | ✅ Built |
| 0041 | [Character evolution & arc (the linchpin)](./0041-character-evolution-and-arc.md) | BDD | ✅ Built |
| 0042 | [Competition library](./0042-competition-library.md) | BDD | ✅ Built |
| 0043 | [Emergent bloc behavior](./0043-emergent-bloc-behavior.md) | BDD | ✅ Built |
| 0044 | [Strategic nomination & vote refinements](./0044-strategic-nomination-and-vote-refinements.md) | BDD | ✅ Built |
| 0045 | [Endgame structure (Final 5 → Final 2)](./0045-endgame-structure.md) | BDD | ✅ Built |
| 0046 | [Player eviction & the juror's seat](./0046-player-eviction-and-jury.md) | BDD | ✅ Built |
| 0047 | [Eviction night live (reveal + goodbyes)](./0047-eviction-night-live.md) | BDD | ✅ Built |
| 0048 | [Season retrospective & the Vault unsealing](./0048-season-retrospective-and-unsealing.md) | BDD | ✅ Built |
| 0049 | [House presence & lingering play](./0049-house-presence-and-lingering.md) | BDD | ✅ Built |
| 0050 | [The casting interview](./0050-casting-interview.md) | BDD | ✅ Built |
| 0051 | [In-character images](./0051-in-character-images.md) | unit + FE | ✅ Built · browser-smoke render landed (`browser_smoke.py:1166` asserts a real sized portrait `<img>` + zero placeholders) |
| 0052 | House themes (five seasons) | FE | ✅ Built · shipped FE-side, no standalone spec file |
| 0053 | [Admin transcript retrieval](./0053-admin-transcripts.md) | FE | ✅ Built |
| 0054 | [Control-room gadget rail](./0054-gadget-rail.md) | FE | ✅ Built · Phase 1 (HUD rail) + Phase 2 (dock windows) |
| 0055 | [Social play moves the weights](./0055-social-play-weights.md) | FE | ✅ Built · the `_auto_record_scene` guarantee |
| 0056 | [Season-to-season character continuity](./0056-season-character-continuity.md) | unit + FE | ✅ Built |
| 0057 | [Seasons as levels](./0057-seasons-as-levels.md) | FE | ✅ Built · live browser validation landed (`browser_smoke.py:1197` validates the season-progress bar + "Season N" chip + new-season window) |
| 0058 | [Deep character profiles](./0058-deep-character-profiles.md) | BDD | ✅ Built · Phase 1 + Phase 2 |
| 0059 | [Hidden seeded relationships](./0059-hidden-seeded-relationships.md) | engine | ✅ Built · orientation-aware via 0063 · §5 organic stage-advancement surfacing scheduler now built (issue #605): showmance arc spark→bond→visible (`advanceShowmances`) + the pre-game-TIE discovery scheduler (`advanceSeededTies`, opt-in `ORWELL_SEEDED_TIE_SURFACING`, default OFF) — pathway-only (0002) + the 0023 discovery fold, sealed off player AND admin, monotonic/persistent. Calibration-neutral when off (proved byte-identical vs. main; `tests/unit/seededTieSurfacing.test.ts`) |
| 0060 | [Story-thread scheduler (0058 Phase 2)](./0060-story-thread-scheduler.md) | BDD | ✅ Built |
| 0061 | [Player self-eviction](./0061-player-self-eviction.md) | BDD | ✅ Built |
| 0062 | [Move-in zeitgeist snapshot](./0062-world-snapshot-zeitgeist.md) | BDD + FE | ✅ Built · `src/engine/zeitgeist.ts` (frozen + seed-reproducible + flavor-only) **+ the FE `web_search` capture lane** (`frontend/src/orwell_zeitgeist.py`) writing the real move-in zeitgeist back via `recordWorldSnapshot` |
| 0063 | [Casting diversity floor](./0063-casting-diversity-floor.md) | BDD | ✅ Built |
| 0064 | [Live multi-device game sync](./0064-live-multi-device-game-sync.md) | FE | ✅ Built · canonical session (A) + Messenger serialization (C) + window/HUD layout sync (F) + `game-updated`/cast-photo/once-only (B/D) |
| 0065 | [Cast pre-warm: deep-author before portraits](./0065-cast-prewarm.md) | BDD + FE | ✅ Built · wired the `recordCastProfile` write-back onto the player channel (was rejected at the MCP boundary) + `preSeedCast` pre-game cast + `createCharacter` adoption + FE author-warm-early / portrait-warm-gated |
| 0066 | [In-game time of day & the nightly sleep economy](./0066-in-game-time-and-sleep.md) | unit + integration | ✅ Built (ADR 0006) · the five-phase clock + character-driven bedtimes + the diegetic bound + the player's `turnIn` bedtime lever + a hidden bounded sleep→comp penalty + the HUD clock/rest cue. **Opt-in** (`ORWELL_TIME_OF_DAY`, default off) so the seeded calibration spine is byte-identical. Gate: `tests/unit/timeOfDay*.test.ts` + `sleepCompetition.test.ts` (recorded deviation, like 0033/0036/0055) |
| 0067 | [Public internet exposure & internet-grade hardening (hiorwell.com)](./0067-public-internet-exposure.md) | FE + scripts | 🚧 Built · follow-on owed (host smoke) (ADR 0007) · the hardening floor — FE `ORWELL_BIND_HOST` loopback default + `--proxy-headers`, the fail-closed public-profile boot guard, `TrustedHostMiddleware`, the real-client-IP login-throttle fix behind the tunnel — + a `deploy/expose/` kit (**Cloudflare Tunnel + Access** chosen; Pangolin / Caddy documented) + the INSTALL public-deployment runbook. Engine **unchanged**; Vault Wall + 0021 isolation untouched. Gate: `frontend/tests/test_public_profile_guard.py` · `test_trusted_host.py` · `test_login_throttle.py` · `test_public_deploy_config.py`. Owed: real-host smoke (folds into 0010) |
| 0068 | [Admin "Public deployment / Connect to the internet" (UI-driven exposure)](./0068-admin-public-deployment.md) | FE + scripts | 🚧 Built · follow-on owed (host smoke) (ADR 0007; drives 0067) · an admin Settings→System panel + a token-paste **Connect** wizard using a Cloudflare **remotely-managed** tunnel, applied through the existing privileged **ops** mechanism (flag→root oneshot→`orwell-ops-public-deployment.sh`: `.env` upsert + cloudflared install/run + FE restart + token shred) with live tunnel/URL status. Routes/UI/deploy built; request-time fail-closed validation reuses `assert_public_profile_safe`; token write-only. Gate: `test_public_deployment_{routes,ui,ops}.py`. Engine **unchanged**; Cloudflare-account steps stay dashboard-side. Owed: real-host smoke (folds into 0010) |
| 0069 | [Token economy: metered LLM boundary, reasoning budget & non-degrading context tiering](./0069-token-economy-and-context-budget.md) | FE | ✅ Built (ADR 0010) · A: meter the one LLM boundary (full usage envelope incl. cached/reasoning/cost + context-%, Vault-free, admin-only `/api/admin/token-economy` + soft spend-alert) · B: token **policy per call class**, `reasoning` budget now SENT provider-aware (admin-editable `reasoning_budget`) · C: per-session stickiness + opt-in high-token provider-pin · D: opt-in non-degrading context tiering (escalate before lossy compaction). FE/adapter-only; engine/closed set untouched; player never sees a number. Gate: `frontend/tests/test_adr0010_*.py`, full FE suite green |
| 0070 | [Off-screen society texture enrichment](./0070-offscreen-texture-enrichment.md) | Engine + FE | ✅ Built (#507 engine seam + #511 FE live-wiring) · enrich the **prose** of already-recorded **hidden** off-screen scenes with model-voiced texture via a new FE-driven write-back `recordOffscreenSceneTexture`, fanned out in parallel (the hermes `delegate_tool.py` subagent **pattern**, MIT © Nous Research — pattern only). Enrichment lands *downstream* of the Vault boundary (event already hidden); reaches the player only via existing overhear/gossip pathways. Engine keeps the entire closed set (ADR 0005); no texture ⇒ byte-identical fold (`expressiveNonCollapse`). Mandate #1 fidelity win. Gates: `tests/unit/offscreenTextureBoundary.test.ts` (engine four-place) + `frontend/tests/test_offscreen_texture_live.py` (FE live-fire) + BDD `0070-offscreen-texture-enrichment.feature` |
| 0071 | [Defensive hardening: log redaction & URL/path guards](./0071-defensive-hardening-redaction-url-guards.md) | FE | ✅ Built (#505) · adapt `agent/redact.py` (broader secret masking on the FE log path) + the gateway SSRF / path-traversal URL guards (fail-closed). Mandate-neutral defense-in-depth; no Vault surface. Attribution: Nous Research MIT. Gates: `frontend/tests/test_secret_redaction.py` + `test_url_safety.py` + BDD `0071-defensive-hardening-redaction-url-guards.feature` |
| 0072 | [Multi-platform gateway: play your game from messaging platforms](./0072-multi-platform-gateway.md) | FE | ✅ Built (#504) · a new non-Vault player adapter (`frontend/gateway/`) over the same Vault-free MCP player channel: per-platform adapter+registry + `pairing.py` identity binding (one human ⇒ one game across platforms, 0021) + a **server-side reasoning/operator-aside scrub chokepoint** (the accordion-less-transport safety) + decision-card text degradation + `ORWELL_GAME_BUILD` tool-gating. Adapts hermes `gateway/` (MIT © Nous Research). Coordinates with ADR 0007 / 0064. Gates: `frontend/tests/test_gateway_{pairing,scrub,isolation,build_gate}.py` (63 tests) + BDD `0072-multi-platform-gateway.feature` |
| 0073 | [Structural anti-sycophancy wall: game-build boundary is a CI gate](./0073-structural-anti-sycophancy-wall.md) | FE tests + CI | ✅ Built (#506; BDD compose-fix #510) · makes the `ORWELL_GAME_BUILD` game-build wall **structurally proven in CI**, closing the asymmetry with the Vault Wall (compile-time via dependency-cruiser). A new `frontend/tests/test_game_build_wall.py` gate — env-pinned (forces game build on internally), covering set purity, context-injection zeroing, JS-strip completeness, and HTTP-level 404 for every drop-set route. Wired into the CI frontend job as a required step. Gates: `frontend/tests/test_game_build_wall.py` + BDD `0073-structural-anti-sycophancy-wall.feature` |
| 0074 | [Local & tunable HTTPS (trusted on the LAN, with or without a domain)](./0074-local-and-tunable-https.md) | FE + scripts | ✅ Built (2026-06-22; ADR 0014) · a Caddy TLS terminator in front of the FE, driven by tunable `ORWELL_TLS_*` vars from the `orwell https` control-panel action **and** the in-app Admin "Local HTTPS" card (the 0068 ops-flag seam: `orwell-ops-tls.*` → `orwell-https.sh`). Layered trust — offline internal-CA floor (`/orwell-local-ca.crt` download → no warning after one install) + optional publicly-trusted DNS-01 upgrade (zero per-device install). Engine never named. Gates `frontend/tests/test_{tls_routes,local_tls_guard,local_ca_download}.py` + `tests/unit/tlsArtifacts.test.ts` |
| 0075 | [Trust-gated confidences](./0075-trust-gated-confidences.md) | BDD + unit | 🟢 Built (2026-06-24; BDD-wired + passive lie-catch 2026-06-27) · the player-driven `confide` lever ships end-to-end — a houseguest confides a real secret only when they have a *reason* (bond/favor/debt), with a graduated disclosure ladder, and a manipulative houseguest may plant a false confidence (capped) — **plus the passive lie-catch** (a later truthful pathway flips the player's belief to the truth + folds a betrayal-grade blow on the player→liar edge). Vault Wall held structurally (the engine decides + records every disclosure; the model only voices). Gate: `confidence.test.ts` + `confideBoundary.test.ts` (MCP-boundary + Vault-wall + lie-catch) + BDD `0075-trust-gated-confidences.feature` (in `cucumber.cjs`, `confidence.steps.ts`). Opt-in by construction ⇒ calibration byte-identical. Fast-follow: the FE confide-under-call guardrail. |
| 0076 | [Presence grounding & motivated movement](./0076-presence-grounding-and-motivated-movement.md) | unit + FE | 🚧 Built (PR #515) · the narrator stops inventing the room: engine "present company holds the scene" churn fix (`PRESENCE.companionMoveProb`, calibration-safe on the weighted player-facing pass only), the per-turn occupancy feed, FE narrated-departures cue, and the presence/identity desync guard. Gates: `tests/unit/presence.test.ts` + `frontend/tests/test_0076_presence_{movement,desync}.py`. BDD wiring deferred until the 0077 floor-plan lands. |
| 0077 | [House map, privacy & eyeshot](./0077-house-map-privacy-and-eyeshot.md) | unit + BDD + FE | 🟢 **Built (phased).** Phase 1 (the recent-BB 13-room floor plan + movement adjacency) **merged** (PR #772). Phase 2a (the **SIGHTLINE graph** — eyeshot < adjacency; `HOUSE_SIGHTLINE`/`areVisible`/`isPrivateRoom`) **merged** (PR #786). Phase 2b **built 2026-06-25**: `whereabouts()` returns `{present, nearby(=visible), tracked}` — the **tracked** closed-door belief layer (acquired by watching a doorway; pathway + confidence + age-derived `stale`; persisted 0007), the **conspicuousness** read ("two alone too long"; derived per-read; who/where/how-long, never content; diffuses NPC-to-NPC), **sub-zone** earshot (`ROOM_ZONES`; co-presence witnessing zone-scoped, eyeshot room-wide) + an opt-in closed-door overhear muffle. Every new behavior is opt-in/read-side ⇒ the `juryReach`/gradient spine is byte-identical (`privacyEyeshot0077.test.ts` draw-count guard). **NPC-side increment (2026-06-25):** the house now **whispers** — a conspicuous NPC pairing in a private room starts a Vault-free position rumor (`src/engine/houseSuspicion.ts`) that diffuses NPC-to-NPC and can reach the player, on a **dedicated rng with no relationship fold** ⇒ still calibration byte-identical (gated by `houseSuspicion.test.ts` + the green calibration sims). Gates: `sightline0077`/`privacyEyeshot0077`/`houseSuspicion` + BDD `0077-…feature` (wired into `cucumber.cjs`) + the L26 FE gadget. Sibling of 0076. |
| 0078 | [Motivated off-screen society & intentional movement](./0078-motivated-society-and-intentional-movement.md) | unit + calibration + BDD | 🟢 **Built 2026-06-25 (Phase 1 + Phase 2 — feature complete).** **Phase 1 (intentional movement):** co-presence is **goal-driven** — NPCs move toward what their motivation points at (`MOVEMENT_INTENT` + `tanh`-bounded room steering + unsatisfied-pursuit move-gate in `assignRooms`, wired off the relationship model), so "who's in a room together" reflects agenda not luck. **Phase 2 (nature clarity):** the scene's nature sets its fold — `natureFoldImpact` folds a **friendly** scene (`{bonding, showmance}`) **affinity-only** (no strategic move, no vote-affecting change) and a **game** scene byte-identically (the spine; same jitter draws). Intent draws no rng of its own but is DELIBERATELY calibration-load-bearing (owner ruling: location must affect play); combined **calibration re-pass** re-tuned `moveIntentStrength 0.2 → 0.15` (the friendly fold reduced society trust), at which **`juryReach` + gradient hold green** (active ≥ passive). Gate: `intentionalMovement0078.test.ts` + `natureClarity0078.test.ts` + the calibration sims + BDD `0078-…feature` (8 scenarios, wired into `cucumber.cjs`; the anti-sycophancy band is enforced authoritatively in the heavy sims, proved wired here per the 0073 structural pattern). Sibling of 0077. |
| 0079 | [Runtime loop overseer & diagnostic log](./0079-runtime-overseer-and-diagnostic-log.md) | FE | ✅ Built (PRs #717, #741) · the OVERSEER ring + admin "Overseer (live)" source + gap-repair/pacing belt wiring; `OverseerPort` + `DeterministicOverseer` + `LlmOverseer` + the sparse `should_assess` gate; the live hook (opt-in, default OFF, fail-soft) drives the **reasoning tier** (utility-model diagnosis → deterministic fallback); admin Settings toggle. Diagnose-and-LOG only (the inline guardrails are the hands). Gate: `frontend/tests/test_0079_overseer*.py`. |
| 0080 | [Active overseer (reasoning tier as the primary actor)](./0080-active-overseer.md) | spec-only | 📝 Spec · **scoping; owner rulings open** · follow-on to 0079 — lets the overseer **act on its verdict** (verdict-driven corrections routed through the existing tuned action paths) with the deterministic guardrails demoted to the **fail-soft floor**. A live-game behavior change: 3-state mode (off/shadow/active), inline relocation, **trigger-only** (the engine still owns every outcome), **live-only** (seeded lanes unchanged, no re-baseline). Explicit cost/risk + 5 owner rulings (D1–D5) in the note. Design note + provisional `.feature`; not in `cucumber.cjs`. |
| 0081 | [Narration-faithfulness gate (the overseer's 2nd role)](./0081-narration-faithfulness-gate.md) | FE | ✅ Built (PR #778, P1–P6) · checks the narration is faithful to engine truth (board / persona / leak / omission). **Hybrid** detection: the deterministic 0065 guard flags **claim-bearing turns**; an LLM `FaithfulnessJudge` runs only on those, post-stream. Correction **split on ADR 0005** — an **open-set** slip is *adopted* canonical (via the 0055 record machinery, tagged O3), a **closed-set** slip is *reframed* / re-grounded next turn (the outcome is **never** bent; the wall is `adopt ⇒ open-set`, enforced at parse **and** integration); un-reframable ⇒ configurable `{reground,log-only,visible}` (default reground). Covers the in-game loop **and** the casting/premiere/preview junctions (subsumes 0082 — premiere/preview ride the in-game hook, casting has its own seam + context-aware judge). Its **own** off/shadow/active dial (`faithfulness_mode`), independent of the 0080 pacing dial; live-only, fail-soft, default **OFF**. Owner-ruled 2026-06-24 (boundary held — `adopt` open / `reframe` closed, never bend a consequential outcome). Gates: `frontend/tests/test_0081_*.py` (58); FE pytest, not in `cucumber.cjs`. |
| 0084 | [Character voice & grounded mood](./0084-character-voice-and-mood.md) | BDD | ✅ Built (2026-06-25) · sixteen mouths, not one — a byte-stable PUBLIC `voice` fingerprint on `CHARACTER` (register/rhythm/energy/directness/humor/stress-tell + a prose signature), generated per NPC and voiced every turn, plus a Vault-safe `mood` derived from the live soul (`emotionalState`/`volatility`) so affect is **queried from truth, not invented** (fixes the "invent moods freely" anti-sycophancy soft-leak). Voice is **static identity** (never drifts); mood carries the dynamism on **two timescales** — the acute spike *and* a **marinated baseline** house dynamics shift over the ~3-month season (0041). Mandates: behavioral fidelity #1, non-degradation (voice never drifts), anti-sycophancy (grounded mood), Vault-safe (observable affect only, never the cause/number). Design note + `.feature`; not yet in `cucumber.cjs`. Sibling of 0085. |
| 0085 | [NPC campaigns & the scramble](./0085-npc-campaigns-and-the-scramble.md) | BDD | ✅ Built (2026-06-25, phased A/B1/B2/C) · strategy executed over time — a hidden, persistent `Campaign` (goal/target/plan/progress/horizon/status) per NPC or bloc, advancing one motivated move per off-screen tick (lobby/plant/deal/position/throw), **adapting** when the board changes, and tilting the **seeded** noms/veto/votes (0044) so campaigning actually changes who goes home (engine tallies; narration voices — anti-sycophancy). The tilt is **character-mediated** — `persuasiveness(owner) × susceptibility(listener) × trust`, new static CHARACTER aptitudes — not a flat knob. Vault-sealed with **symmetric perspective** — a campaign is its owner's private knowledge; every OTHER houseguest *and* the player learns of one only through the same pathway (lobbying, gossip, conspicuousness 0077, contradictions), and NPCs are never omniscient about each other's plans (they act on their own belief, the engine reads the master ledger only to tally). The **scramble** is campaigns colliding (hidden true count vs. stated). Player-run campaigns are player-knowledge, never mind-control. Persists + accumulates (non-degradation). Design note + `.feature`; not yet in `cucumber.cjs`. Sibling of 0084. |
| 0086 | [Houseguest drives](./0086-houseguest-drives.md) | BDD | ✅ Built (2026-06-25) · **everyone always plays, intensity varies** — every houseguest always carries a hidden `Drive` (motivation `self-preserve`/`target`/`build`/`lay-low`/`win-power` + an intensity scaled by the board + archetype aggression + soul). `deriveDrive` is **sticky** (holds a target across a stable board, re-aims on a real board change) and **perspective-bound** (from the owner's own reads). Three tiers of one spectrum: a **low `target` lean** adds a small bounded term to the **owner's OWN ballot only** (`ownBallotLean`, never the electorate); the **mid/high** tiers are the 0085 `Campaign` lobbying/tilt — exactly one tier, never additive. Only `target` drives touch the vote (`self-preserve`/`lay-low`/`build`/`win-power` are behavioral). Behind `ORWELL_CAMPAIGNS` (default off) on the dedicated rng ⇒ `juryReach`/UAT byte-identical when off; persisted in `sessionSnapshot.drives`; never a number to the player. Anchors the diagnostic's thin mood/suspicion layers (a drive gives a houseguest something to be on-edge *about* and someone to suspect). Engine + BDD `0086-houseguest-drives.feature` (wired in `cucumber.cjs`). Sibling of 0085. |
| 0087 | [Relationship trajectories (warming/cooling arcs)](./0087-relationship-trajectories.md) | BDD | ✅ Built (2026-06-27) · each directed relationship carries a hidden, engine-owned **momentum** (`{phase: warming/cooling/souring/steady, momentum}`) derived from its recent folded history (`deriveTrajectory`, pure/no-rng; a betrayal spikes souring, an unfed arc `decayTrajectory`s toward steady at the 0026 neglect cadence). `tiltNatureWeights` RE-WEIGHTS the existing off-screen nature `weightedPick` toward continuing the arc — bounded by a tunable cap so off-arc beats stay possible and the **0078 betrayal gate is never bypassed** (a gated-to-zero nature stays zero). A friendship visibly **curdles** over weeks instead of bond→clash→bond whiplash. Dedicated `ORWELL_TRAJECTORIES` flag (default OFF) ⇒ the tilt is never wired, `natureWeights` is the identity, and `juryReach`/UAT are **byte-identical**; ON, the tilt adds **zero rng** (re-weights the SAME single draw) so the off-screen stream's draw count/order is unchanged and the seeded competition/vote spine reading it stays in phase (`trajectoryOutcomeNeutral` is the gate). Vault-class hidden state — `phase`/`momentum` appear on NO player- or admin-facing projection (proven by byte-identity of every projection on vs off), reaching the player only as the KINDS of scenes the arc produces. Perspective-bound (directed `A→B` from A's own folds) and persisted in `sessionSnapshot.trajectories`/`trajectoryFolds` (resumes mid-curdle; a pre-0087 save loads at steady). `src/engine/trajectory.ts` + `trajectoryConstants.ts`; engine + BDD `0087-relationship-trajectories.feature` (wired in `cucumber.cjs`). Sibling of 0086. |
| 0088 | [Living "current read of you"](./0088-living-current-read.md) | ✅ built | BDD (Vitest + cucumber + arch) · a living per-NPC current read of the player derived from the already-evolving hidden NPC→player edge, distinct from the frozen `dayOnePerception`; surfaced behavior-only as a Vault-safe carriage cue (sibling of 0084 `mood`), never a number. Pure read (no rng) ⇒ calibration byte-identical; absent field back-compatible. Design note + `.feature` (in `cucumber.cjs`). |
| 0089 | [Reactive confessionals](./0089-reactive-confessionals.md) | BDD | ✅ Built (2026-06-27) · confessionals react to concrete recent events the confessor witnessed ("after the veto ceremony…") instead of static target/ally readouts — the engine selects the facts (from the confessor's own witness set), the model only voices them (anti-sycophancy). Additive & byte-identical to 0040 when no events supplied; stays Vault-only NPC content. Engine + BDD `0089-reactive-confessionals.feature` (wired in `cucumber.cjs`). |
| 0090 | [Per-archetype voice (0084 extension)](./0090-per-archetype-voice.md) | unit | ✅ Built (2026-06-27; Tracks #868) · per-archetype `signature`/`lexicon` pools + harder dial anchoring (`BIAS_STRENGTH` 0.55→0.8) in `src/engine/voice.ts` make `generateVoice` genuinely distinctive per archetype, and the shipped 0084 `VoiceProfile` is THREADED through the engine-authored confessional prose (`src/engine/confessionals.ts` voice-keyed CURT/EXPANSIVE/MEASURED parallel target/ally pools + a deterministic habitual-filler weave for warm voices, selected by the public dials; wired in `GameSessionAdapter` `ctxFor`). Voice changes phrasing, never a fact (same engine-grounded target/ally) — closing the sameness the audit found (coordinates #850/#854). Vault-safe (voice is public, encodes no hidden state); deterministic from seed; ABSENT voice ⇒ byte-identical to the 0040 templated fallback ⇒ `juryReach`/UAT calibration byte-identical (voice rides the isolated side rng, the phrasing consumes no extra draw). `momentPrompts` voicing lever tightened (diction + cadence). Gate: `tests/unit/voice0090.test.ts`. |
| 0091 | [Trigger secrets → house events](./0091-trigger-secrets-house-events.md) | BDD | ✅ Built (2026-06-27; Tracks #869) · a volatile sealed hidden element gains a **`trigger`** kind carrying an engine-only `volatility` + eruption `kind` (`armTriggers` reclassifies the existing volatile B50 wordings under a per-NPC cap + population sparseness, on a dedicated `:trigger:` side-rng ⇒ the B50 + main house streams stay byte-stable). Under accumulated **strain** (live soul, 0041) × a fresh **precipitant** (a conflict/betrayal scene this tick) × a seeded **fireRate** roll, `runTriggerEruptions` (in `GameSessionAdapter`, ridden by the orchestrator's bounded off-screen tick AFTER the society/confessional folds) emits an emergent, Vault-safe **public `house-event`** the player witnesses (a generic eruption-pool line — a blow-up / showmance-detonation / mask-slip / meltdown), + folds the erupter's soul (0041) and co-present witnesses' reads (0026). The sealed `detail`/`volatility`/`kind` **never cross** to player OR admin, before or after a fire (proven by a sentinel sweep over every projection + the assembled prompt + the recorded event). **No cold open** (no precipitant ⇒ no fire); **foreseeable in hindsight** (a calm soul with the same trigger never fires — strain is the tell); per-season **cap** + per-kind one-shot/re-arm policy, all persisted (`eruptionCount`/`triggerTickCount` + the per-trigger `fired`/`lastFiredWeek`, monotonic). Dedicated session side-rng + `ORWELL_TRIGGERS` flag (default OFF) ⇒ ZERO draws when off ⇒ `juryReach`/UAT byte-identical (`triggerOutcomeNeutral` is the gate). `src/engine/triggers.ts` + `triggerConstants.ts`; engine + BDD `0091-trigger-secrets-house-events.feature` (wired in `cucumber.cjs`). Sibling of 0075. |
| 0092 | [Secret-pacing drip](./0092-secret-pacing-drip.md) | BDD | ✅ Built (2026-06-27; Tracks #861) · a thin pacing/eligibility layer over the existing **0060 story-thread scheduler** — a sealed `ripeness` (proximity + tension + recency − already-told, `src/engine/secretPacing.ts`/`secretPacingConstants.ts`) + a ~1-2/week drip budget shapes the scarce reveals into a paced cadence so dormant secrets actually reach the player (the "I knew it" payoff). The pass (`GameSessionAdapter.pacingDrip`, run before 0060's surface decision in the bounded tick) ranks ripe player-bound threads, checks the weekly budget + 0060's season cap, and routes the top eligible candidate into 0060's EXISTING anchored surface (`surfaceThread` `force`: proximity-ripe ⇒ the 0075 confidant slip, tension-ripe ⇒ the gossip chain) — **no new pathway**. Crossings still go through real pathways; what crosses is a belief/paraphrase, never the premise, never a number. Opt-in behind a dedicated `ORWELL_SECRET_PACING` flag on a DEDICATED side-rng ⇒ **zero pacing draws + byte-identical seeded comp/vote/jury spine when off** (the load-bearing guarantee; `tests/unit/secretPacingNeutral.test.ts` is the recording-rng byte-identity gate). Vault-safe on player AND admin, before and after a drip (`secretPacing.test.ts` sentinel). Per-week drip counter + per-thread `lastDrippedWeek` persisted (the cadence resumes across a restart). Engine + BDD `0092-secret-pacing-drip.feature` (wired in `cucumber.cjs`). Builds on 0060 + 0075. |
| 0093 | [Secrets as strategic levers](./0093-secrets-as-levers.md) | spec-only | 📝 Spec · **PO review** (Producer's Vault audit; Tracks #862) · a *learned* secret unlocks a concrete option — leverage in a deal (0039) or a first-class `exposeSecret` (seeded standing hit + betrayal backlash); secrets are spent/capped. The player can only wield a `factId` they already legitimately hold (no Vault read unlocks a lever); outing reaches the house only via `surfaceInformationTo`. PO rulings R1–R4 enumerated (closed-set ceiling, expose-as-action, pathway-only, deals composition). Builds on 0039 + 0075. Design note + `.feature` (not in `cucumber.cjs`). |
| 0094 | [Distorted gossip has consequences](./0094-distorted-gossip-consequences.md) | spec-only | 📝 Spec (Producer's Vault audit; Tracks #865) · makes the distortion/confidence the **0002** belief model already tracks *matter*: act on a low-confidence/distorted belief → get burned (a closed-set move lands against engine-owned reality), then learn the truth later via the same `factId` lineage (dramatic irony). Uncertainty voiced behaviorally, never a number; the engine never reveals which beliefs are wrong. Consequence/surfacing shaping on top of 0002, not a new belief system. Opt-in/isolated rng ⇒ byte-identical. Design note + `.feature` (not in `cucumber.cjs`). |
| 0095 | [Pre-show ties → time-bombs](./0095-pre-show-ties-reveal.md) | spec-only | 📝 Spec (Producer's Vault audit; Tracks #867) · turns **0059**'s seeded `[Hidden tie]`s into time-bombs — a single monotonic `exposure` state + three reveal pathways (overhear/slip diffusing via 0077, and a player-reachable `accuseTie` lever, the 0075 `confide` sibling). On exposure the engine folds a bounded, seeded, betrayal-grade blow on the deceived edges + a wider threat bump. Sealed off player AND admin until pathway-surfaced; dedicated rng + opt-in ⇒ byte-identical. Builds on 0059 + 0077 + 0075. Design note + `.feature` (not in `cucumber.cjs`). |
| 0096 | [Emergent nemesis](./0096-emergent-nemesis.md) | spec-only | 📝 Spec (Producer's Vault audit; Tracks #881) · elevates the NPC with the highest *sustained* threat-toward-player into a felt recurring antagonist — the sustained, narrated elevation of an existing **0086** `target` drive + **0085** campaign (selection + bounded escalation + a Vault-safe `rivalry` tone hint), NOT a new targeting system. Behavior-only (no identity/number/tier on any projection), organic (can shift/hand off), opt-in behind `ORWELL_CAMPAIGNS` ⇒ byte-identical, no extra vote magnitude. Builds on 0085 + 0086. Design note + `.feature` (not in `cucumber.cjs`). |
| 0097 | [Suspicion ledger](./0097-suspicion-ledger.md) | spec-only | 📝 Spec · **PO review** (Producer's Vault audit; Tracks #878) · a player-PRIVATE ledger of hunches the game later marks `called-it`/`wrong` against the truth — a detective scorecard. Scoring compares the stated hunch to truth ONLY at a sanctioned reveal (an 0002 pathway or the 0048 unseal), never a live Vault read; inherits 0013's `NO_NPC_PATHWAY` (player-knowledge, never drives NPCs). PO rulings R1–R3 (recommend a DR-style OOC surface, not a HUD). Builds on 0013. Design note + `.feature` (not in `cucumber.cjs`). |
| 0098 | [Confidence-calibrated reads](./0098-confidence-calibrated-reads.md) | spec-only | 📝 Spec · **PO review** (Producer's Vault audit; Tracks #879) · acting on a low-confidence belief widens a **symmetric, mean-preserving** seeded-outcome band (asymmetry would protect/punish — forbidden); conviction read from the 0002 belief model. Opt-in/default-off ⇒ `juryReach`/gradient/UAT byte-identical; on ⇒ heavy sims must run on AND off, a mean shift blocks ship. PO rulings R1–R4. Builds on 0002 + competition outcome. Design note + `.feature` (not in `cucumber.cjs`). |
| 0099 | [Secrets as currency](./0099-secrets-as-currency.md) | spec-only | 📝 Spec · **PO review** (Producer's Vault audit; Tracks #880) · the *trade* use of a held secret (value is the recipient's — info becomes liquid) + off-screen NPC barter; the player only trades a `factId` in `knownTo(player)`. R2 **strongly recommends 0093 + 0099 ship as ONE build spec** (leverage/trade/expose = three uses of a held fact, not three systems). Builds on 0039 + 0093 + 0002 diffusion. Design note + `.feature` (not in `cucumber.cjs`). |
| 0100 | [Jury grudge book](./0100-jury-grudge-book.md) | BDD | ✅ Built (2026-06-27; Tracks #882) · the sequestered jury house is now a hidden second society — `src/engine/juryHouse.ts` (`runJuryHouseStretch`) reuses 0038 `richOffscreenStretch` + 0002 `diffuseGossip` over the **juror set** (player a SUBJECT only, never a graph terminus) so a grievance one juror carried out diffuses to others, and the adapter `juryHouseTick` folds a **bounded, saturated, monotonic** per-(juror, finalist) grudge into the persisted `live.juryGrudge`. `edgeAsJuryRel` consumes it ALONGSIDE the recorded manner — the ONLY change to the vote path (a richer hidden lean *input*; tally/tie-break/reveal reused verbatim, anonymity unchanged). Sized UNDER `MANNER_LEAN` (`JURY_HOUSE.adjustmentCap`) so it deepens, never swamps, jury management. Runs on a DEDICATED isolated rng behind opt-in `ORWELL_JURY_HOUSE` ⇒ byte-identical when off (SHA256 draw-stream gate); a fresh `juryReach` measurement with it on keeps the EARNED-wins guard. Vault-sealed over player AND admin (hidden events, witnessSet = jurors). Builds on 0014/0037/0045 + 0038/0002. Engine + unit (`juryHouse.test.ts` / `juryHouseAdapter.test.ts`) + BDD `0100-jury-grudge-book.feature` (wired in `cucumber.cjs`). |
| 0101 | [NPC myth-making](./0101-npc-myth-making.md) | spec-only | 📝 Spec (Producer's Vault audit; Tracks #883) · the player's notable acts seed gossip **legends** about themselves that spread + distort and circle back ("people are saying you…") — a new *origin* for the existing 0002 diffusion (reuses `factId`/decay/distortion/player-terminal surfacing), the outward complement to 0088's inward read. Dedicated rng + never folds the player's own edges ⇒ byte-identical; Vault-safe (public gloss only, pathway-surfaced). Builds on 0002 + 0088. Design note + `.feature` (not in `cucumber.cjs`). |
| 0102 | [Weekly recap + cliffhanger](./0102-weekly-recap-cliffhanger.md) | spec-only | 📝 Spec · **PO review** (Producer's Vault audit; Tracks #884) · the *weekly* analog of the shipped 0048 `seasonRecap()` — a short Vault-safe digest stitched from witnessed events + already-surfaced gossip, ending on a cliffhanger, giving each HOH-week the shape of an episode. Reads only Vault-free projections (no `VaultStore` import); the hook lives entirely in the open set (never commits an outcome). PO rulings R1–R2 (an in-fiction recap beat, not a dashboard; Vault-safe non-committing teaser). Builds on 0048 + the event store + narration. Design note + `.feature` (not in `cucumber.cjs`). |
| 0103 | [Edit-bay foreshadowing](./0103-edit-bay-foreshadowing.md) | spec-only | 📝 Spec · **PO review** (Producer's Vault audit; Tracks #885) · a Vault-safe production-edit hint (a lingering camera, a clipped aside) that nods at a pathway ALREADY in motion (an 0085/0086 campaign, an 0060 `active` thread, an 0092-ripe secret) so a later payoff lands as "I knew it." Surfaces only a public observation, never sealed content; opt-in `ORWELL_FORESHADOW` on a side-rng, zero fold / outcome-commitment ⇒ byte-identical. PO rulings R1–R3 (production voice / cadence / coordinate with 0092 + 0102). Builds on 0092 + 0049 + 0060. Design note + `.feature` (not in `cucumber.cjs`). |
| 0104 | [Season-over-season notoriety](./0104-season-notoriety.md) | spec-only | 📝 Spec · **PO review** (Producer's Vault audit; Tracks #886) · a bounded hidden `NotorietySummary` (from the finished season's open-set record, never raw Vault) carried across the single restart door, biasing the next cast's day-one reads via the existing `seedFirstImpressions`/`dayOnePerception` fold (one bounded seeded term — the engine keeps outcome magnitude). Cross-user-isolated (own prior games only); opt-in ⇒ `seedFirstImpressions` byte-identical, the `juryReach` floor unmoved. PO rulings R1–R4. Builds on the single restart door + 0058/0063 + 0021. Design note + `.feature` (not in `cucumber.cjs`). |
| 0105 | [Drive-anchored suspicion](./0105-suspicion-and-mood-anchoring.md) | BDD | ✅ Built (2026-06-25) · **the wary read behind the sealed plan** — consumes 0086's drive to fill the diagnostic's empty `suspects` layer and anchor 0084's mood. A drive's PLAN (intensity, vote tilt, campaign) stays Vault-sealed, but the **threat-READ that motivates it** is the houseguest's own subjective hunch, so `driveSuspicion` surfaces it on `npcVoice.suspects` as a **behavioral hunch naming the subject** (`target` ⇒ "keeping a close eye on <name>"/"sizing up <name>"; `self-preserve` ⇒ "wary feeling about <name>"/"watching their back around <name>"; `build`/`lay-low`/`win-power` ⇒ none). The narrator now has, per houseguest, a `mood` (0084) **and** a specific person it's about — connected in behavior, never named as a motive (a mood-consumer lever note). Pure leaf (no number, no Vault handle); present only for an active houseguest with a live drive ⇒ **byte-identical projection when `ORWELL_CAMPAIGNS` is off**. Engine + BDD `0105-suspicion-and-mood-anchoring.feature` (wired in `cucumber.cjs`). Consumes 0086. (Renumbered from 0087 — the parallel Producer's-Vault spec batch claimed 0087–0104.) |
| 0106 | [Whole-house events are exclusive set-pieces](./0106-exclusive-house-events.md) | engine | ✅ Built (2026-06-26) · **a comp/ceremony is the ONLY thing happening** — from playtest feedback (a comp ran during a side conversation, then the scene resumed as if uninterrupted). `houseEventInSession()` detects a live competition (comp-intent/round/staging) OR ceremony (nominations / veto / eviction pending); while one runs, `whereabouts` reports the **whole house gathered** (a `houseEvent` block — for a comp the `competing`/`spectating` split with the **outgoing HOH a spectator** + `youAreCompeting`; a ceremony just attends), `nearby` is empty, and the player **can't wander off** (`movePlayer` no-ops). A `momentPrompts` section makes the event exclusive (no side scenes), allows the card to **interrupt mid-scene**, and forbids resuming the prior scene **as if uninterrupted** (acknowledge the gathering, move forward). Pure read-projection + a player-action gate — the seeded sim is untouched ⇒ `juryReach`/UAT **byte-identical**. Spectator *seeded* opinion-folds deferred. Engine-gated (`exclusiveComps0106` + `leverManifest`). |
| 0107 | [Named alliances](./0107-named-alliances.md) | engine | ✅ Built Phase A+B (2026-06-26) · **naming cements an alliance "in reality," real or not** — the explicit, NAMED, multi-party layer between emergent blocs (0043) and 2-party deals (0039). `formAlliance({name, members})` (a player tool, 4-place MCP) is **bond-gated**: members not close enough **decline** (anti-watering-down). Naming then (1) **cements** — a bounded, saturation-diluted tie boost into `detectBlocs` so a borderline cluster forms + tightens (the bloc bends noms/votes via `blocTerm`), capped so a *hollow* alliance never fabricates a bloc; (2) banks **favor** — bounded goodwill toward co-allies (feeds 0075 confidences). Vault-safe projection (`gameStatus.alliances` = name + members, never a number); persists. **No hard cap** — saturation dilution is the only limit (owner ruling). Every effect is 0/identity when no alliance is shared ⇒ `juryReach` **byte-identical** (re-verified). **Phase B:** NPCs **name alliances off-screen** (`formNpcAlliances`, gated `campaignTick`, build-drive founder over real bonds, player never auto-enrolled) and **pitch the player** (`gameStatus.alliancePitches` + the `joinAlliance` seam); **betraying a named ally cuts deeper** (`reconcileAllianceBetrayals` — threat ▲, a witnessed betrayal, the alliance fractures). Engine-gated (`alliances0107` + `alliancesNpc0107b` + MCP boundary). |
| 0108 | [Real-model golden-path gate](./0108-real-model-golden-path-gate.md) | — | 📝 Spec only (not yet built) · **make the real-model golden path a deterministic CI gate.** Every gate stubs the LLM, so the whole model↔engine seam (tool under-calls, narration desyncs, cast-authoring truncation, an inert desync guard) ships green and is only caught by ~30 min of hand-driving a real model (SOUL lessons 17 & 19). Records ONE real deepseek-v4-pro run of casting → premiere → Week 1 HOH → noms → veto → eviction → week-roll as a committed, **Vault-free transcript fixture** (every FE→model request keyed by a stable request hash to its response), then a **replay narrator** feeds it back deterministically while driving the **real engine + real FE** — no API key, a cache-miss is a hard failure surfacing prompt drift. The wrap lives at the FE's single model chokepoint (`stream_llm_with_fallback` / `llm_call_async` in `llm_core.py`, beside the existing `llm_trace.py` tap); **no engine change**. Asserts (one Gherkin scenario each): casting finalizes & the season starts, the producers' opener fires without a player prompt (#967), the post-photo resume fires without a manual continue (#969), cast authoring lands ≥13/15 deep profiles (the #1007 mass-fallback signature absent), the game advances through every beat (no stuck phase), the eviction reveal is batched (~4–8 rounds) and never narrates a result the engine hasn't committed (the desync/pre-emission guard holds), every player-facing body is clean (no Vault / raw `npc:<id>` / operator aside / reasoning token), and the week rolls. CI: a PR-time deterministic **replay** job wired into `ci-gate` (key-free, must not flake) + a scheduled, secret-gated, `continue-on-error` **nightly** that re-records against the live model and diffs drift. Builds on 0107 (the always-on trace tap) + 0065 (`beatSeq`/`phase`) + 0055 + 0051; ADR 0003. Design note + `.feature` (not in `cucumber.cjs`). |


Each row is shipped as its own auto-merged PR. 0001–0008 are the priority invariants; 0009 is
the M5 integration seam (the engine's outward tool API for the front-end / agent), 0010 is the
MVP-1 one-liner deploy/update, and 0011+ are the gameplay loop & player experience. 0015 is the
single human-authored profile (the player); 0016 is the second walled surface (God Mode, walled
from the Vault even for the admin); 0017 promotes [decision 0002](../decisions/0002-relationship-model.md)
(the organic relationship model) into an executable spec. 0018–0019 make **Orwell *is* the game**
playable in the main chat: 0018 is the engine-owned per-moment narrator framing (the model speaks
as the house, never a generic assistant); 0019 is the agent turn-loop that drives play by calling
engine tools while the engine decides every outcome. 0021 makes it **multi-tenant**: one active
game per physical-world user, a sandbox per user, unlimited concurrent games across users — adding
**cross-user isolation** as a new guarantee alongside the Vault Wall.

## Amendments to shipped specs (implementer: pick these up)

Changes made to **already-built** features after they shipped. They are small, additive, and keep
every existing guarantee — but the implementer must fold them into the built code.

| Spec | Amendment | Why |
|---|---|---|
| **0004** (Done) | **(satisfied — implemented)** `CharacterFactory` also generates **public appearance/identity** fields (appearance, age, presentation/style) into the static `Character` (`character.md`); seed-stable, no aptitude/hidden data. Built in `src/engine/characterFactory.ts` (`generateAppearance`). See [0004 §8](./0004-replayability-and-naming.md#8-amendment--public-appearance-fields-for-0020-portraits). | Feeds the **Vault-free portrait descriptor** so houseguests look like who they are ([0020](./0020-player-experience.md) §5). |
| **0023** (Done) | **(satisfied — implemented by 0030)** The **persist → recall** half was **not durable** in the live game — state was memory-only, so an engine restart wiped every game. [0030](./0030-durable-game-persistence-survive-restart.md) (Done) wired the **disk-backed** `FileSaveStore` + load-on-resume/save-on-mutation into the live registry. | A started game must survive a restart; otherwise the onboarding overlay re-fires every load (user-reported). |
| **0018** (Done) | **B61 cast voices**: the woven game context now carries each ACTIVE houseguest's **curated public persona facets** (archetype, strategy style, background, age, appearance, presentation) as the narrator's per-person voice anchor, and `HouseguestCard` projects the same facets (+ a `jury` seat distinct from pre-jury `evicted`). The 0018 scenario wording drops "archetypes" from the banned list — stats, souls, and `hiddenElements` remain walled (the B42 live sentinel sweep now tags `hiddenElements`, not `background`, as the hidden string field). | The narrator was voicing 15 houseguests from a name + status word — structurally voiceless NPCs (experience audit N1/N2); the facets were already Vault-free for portraits (0004/0020). |
| **0025** (Done) | **B53 live firing**: the sealed reserve twists now fire in the LIVE game — `createCharacter` loads + seals the schedule (loop state + a Vault `reserved-twist` audit copy, both persisted: the snapshot now carries the Vault, audit I7), and a sealed **double eviction** runs the same night as a compressed second cycle (HOH → noms → veto → vote, the 0005 hard rules verbatim; same week number, two eviction ceremonies, both count for jury order; the outgoing HOH cannot take the second crown). New `twist-reveal` beat + moment prompt; three live scenarios appended to the .feature. | The twists were computed but never fired live (audit D6 + B8) — the pure module was proven, the game never used it. |
| **0009** (Done) | **Audit E20/E21 (2026-06-10)**: the caller-supplied-stats `resolveCompetition` tool is **removed from the player channel** (registry + dispatch + the `EngineCommands` port) — `runCompetition` over the LIVE house is the single outcome authority (B37); the pure domain `resolveCompetition` stays for tests/engine. `recordInteraction` now **requires the player in the witness set** (the player initiating counts; their seat is made explicit) and budgets hidden folds **per beat per directed edge** on top of the per-call cap. The `.feature` scenarios are amended accordingly (absence + refusal; the end-to-end loop expects the refusal). | E20: a seed-shoppable second resolver — caller stats + caller seed, nothing recorded, no folds — sat one call away on the player channel. E21: a witness set excluding the player minted off-screen "ground truth", and identical repeated calls could pump a hidden edge without bound. |
| **0004** (Done) | **(satisfied — implemented, PR #217)** **E38 / product ruling #1 (2026-06-10):** NPC names are **seeded samples from vendored real-name corpora** (`src/engine/data/givenNames.ts` + `surnames.ts`), replacing phoneme synthesis. The do-not is amended to "**no fixed cast**": corpora are raw material; no full-name+persona pairing is hard-coded (BDD-proven); the legacy Bible's names stay banned (excluded from the corpora); cross-seed carryover is measured on **identity**, not bare first names; the inverse realism gate (every part corpus-membered) is in the BDD. | The generated names read as gibberish on the game's first screen — the product ruling requires names "determined based on what could be a real name." |
| **0047** (Done) | **(satisfied — implemented, PR #217)** **E12+T2:** eviction votes are **secret ballots** — the staged reveal reads anonymized ballots ("a vote to evict …"), `EvictionView` drops voter attribution, and the per-week ballots unseal only in the 0048 retrospective (`RetrospectiveView.evictionVotes`). **E34:** a surviving player records their **own goodbye message** via a new `goodbye-message` pending (tone = the player's choice, folded via `goodbyeMannerFor` exactly as NPC tones; the engine never authors a player goodbye). Two scenarios appended. | Real-BB ballot secrecy (rogue votes, paranoia) was impossible, and the engine spoke for the player on jury management's signature lever. |
| **0037** (Done) | **(satisfied — implemented, PR #217)** **E37:** the player-JUROR asks their own finale question via a new scoreless `juror-question` pending (free text; the finalist's answer remains engine-chosen). Scenario appended. **E35 (with 0005/0042):** the veto chip draw is now a witnessed `veto-draw` beat preceding any winner, and ANY player in the drawn field — chip-drawn included — declares the canonical comp intent; **E36:** the Final-4 veto pending carries the honest (possibly empty) legal save set and refuses a save it cannot honor. | The juror's seat auto-answered the player's own question; the chip draw was invisible; the F4 "use" choice was accepted then silently inverted. |
| **0032** (Done) | **`web_search` moves from the drop-set to the keep-set** (ruling 2026-06-09): the router mounts under the game build, the agent tool surface includes it, and the Settings `search` tab stays as **admin-only global config**. The agent uses it **in-fiction** — real-world references are looked up silently and synthesized in the houseguest's voice; search informs flavor/real-world knowledge **only**, never a game fact or outcome (engine tools remain the sole game truth). Implementation prompt: queue **C32**. | The model must handle real-world references the player makes mid-conversation (e.g. a new movie) without breaking character — critical to "the conversation is the game" (ADR 0003). |
| **0039** (Done) | **(satisfied — implemented, audit E42/E43/E46/T1)** Deals are **horizon-aware**: `safety`/`vote` bind through *their week's* eviction (the vote endpoint or `expireWeekScoped` at the rollover); `final-two`/`target-other` bind until broken. **Honoring pays**: each honoring binding action applies the bounded `DEAL_IMPACTS.honored` fold (trust/alignment + the E54 `reliability` evidence signal); `BindingAction.alternatives` scopes honor to actions where the partner was a real option. **Every binding actor reconciles** — NPC eviction votes, HOH tie-breaks, and the Final-3 eviction flow through `bindingActionsFor` in the live commit path, not only the player's actions. **NPC↔NPC deals exist**: the tightest unbound NPC pair occasionally seals a Vault-held pact at nominations (`DECISION.npcDeal`), binding through the same ledger. Live gate: `tests/integration/liveDealReconciliation.test.ts`. | The audit found deals self-extinguishing after one ceremony, honoring building no trust, NPC votes never reconciled, and `npcOnly()` dead — three shipped systems were flavor, not consequence (Theme 6). |
| **0040** (Done) | **(satisfied — implemented, audit E55/C12)** Confessionals are **structured** (`ConfessionalContext`: trigger + soul-derived mood + seeded phrasing — content names its trigger and varies across a season), fire at **nominations, the veto ceremony, AND eviction night**, and finally reach the confessor's own soul (`recordConfessionalToSoul` is called live, mirrored into `soul.memory` so recall survives a restart). | One canned line all season (also the 0048 unsealing payoff), noms-only firing, and the recall half unwired (zero `recordConfessionalToSoul` callers). |
| **0041** (Done) | **(satisfied — implemented, audit E50/E51/E52)** Off-screen scene emotions are **per-role** (`offscreenEmotion(type, role)`: a betrayal's initiator *schemes*, the partner is *betrayed*; `recordOffscreenScene` evolves both). The dead arc vocabulary fires live: `survived-vote` for the surviving nominee at every eviction; `comp-loss` for contested losers (the veto field; the final-3 crown). `evolveEmotion` carries ADR 0001's bounded per-moment temperature roll (`emotional.swingTemperatureWeight`, seeded side rng) and **delegates to the canonical 0028 `emotionalModifier`** (one swing formula, soul-specific baseline). | The betrayer was given the victim's emotion while the victim's soul never moved; half the 0041 vocabulary never fired; the live swing skipped the ADR 0001 roll beside a dead parallel formula. |
| **0026/0017** (Done) | **(satisfied — implemented, audit E47/E48/E54 + E44)** `EdgeSignals` gains ADR 0002's **`reliability`** evidence signal (fed by honored deals, veto saves, protective votes; torn down by betrayal-shock; consumed by `bondStrength` via `RELIABILITY_WEIGHT`; never decays; lossless round-trip with legacy-save defaults). `CEREMONY_IMPACTS` holds **named impact objects**: `comp-won` is `{threat:+0.14}` only (the house keeps liking its winner) and the eviction fold scales by recorded **manner** (`EVICTION_MANNER_SCALE`). Gossip **receipt folds** (`GOSSIP_HEARD`, confidence-scaled, never the player's own edges) let a rumor genuinely move third-party reads. | A comp win soured the whole house on its winner; a fully-expected eviction folded full betrayal-shock; trust was pure sentiment with no evidence channel; a rumor never changed any mind (Theme 6). |
| **0028** (Done) | **(satisfied — implemented, audit E52/E53)** `variableWeights` is pruned to the fields with REAL consumers: `initiative` (the `rankApproaches` ordering-variance band) and `allianceShift` (the `chooseStrongestBond` near-tie wobble); the four consumer-less fields are deleted. `EmotionalConstants` gains `swingTemperatureWeight`; `emotionalModifier` takes an optional per-soul `baseline` and is the ONE live swing formula (0041 delegates to it). | The "temperature is per-moment, per-variable" config was decorative (zero consumers) while subsystems rolled ad-hoc variance — a config that drives nothing is a lie. |
| **0036** (Done) | **Audit E60 (2026-06-10)**: `socialInitiatives` no longer ships an engine-authored pretext line ("wants a word with you"). The projection carries a **coarse categorical motive** — `bond` (the tie drives the approach) or `probe` (the threat read does) — computed from the same `rankApproaches` agenda; the GM voices it in its own words and the FE chip maps it to short UI copy. Never a relationship number. | ADR 0003 principle 2 names the canned pretext as the smell: hand the model the FACT/drive, let it write the line. The drive was computed and then discarded. |
| **0050** (Done) | **0065 — photo-first OOBE (2026-06-20):** the casting interview now **opens on the cast photo** ("producers ask what you look like before anything else"). New scalar `castPhoto` on `UpdateCastingReq` (`"uploaded"` \| `"skipped"`; any non-empty string marks it handled) is the **FIRST entry of `CASTING_COVERAGE`**, so a fresh interview's `next`/`missing[0]` is the cast photo before the name. Engine-driven and **OPTIONAL** — it does **NOT** gate casting `ready` (name-only), so `createCharacter` finalizes whether the photo was uploaded or skipped. Flows through the existing intake plumbing (ordinary scalar; merge/overwrite/capture/status iterate the coverage list) and the Vault-free `updateCasting` boundary untouched. The `character-creation` moment prompt opens the producer on the 📷 panel accordingly. See [0050 Amendment (0065)](./0050-casting-interview.md#amendment-0065--the-cast-photo-is-the-first-casting-step-photo-first-oobe). | The cast photo was an afterthought ("get one before you wrap"); making it casting step #1 matches "what do you look like?" being the first thing real casting asks — while staying skippable so it never blocks a player who doesn't want a photo. |
| **0023/0055** (Done) | **[ADR 0005](../decisions/0005-split-authority-by-openness.md) — generative-consequence path + expressive-non-collapse gate (PR #355, 2026-06-20):** `recordInteraction` / the 0023 `ConsequenceEngine` accept an optional, Vault-free `consequence` descriptor (per-edge `{ toward, direction, emphasis? }` + `rationale`). The model proposes the *shape* (which edge moves, which direction, relative `emphasis`); the engine still owns the bounded, seeded *magnitude* (`direction`→base impact, `emphasis`→a clamped 0.6/1.0/1.4 multiplier — **no raw number crosses**). `kind` stays the floor: no descriptor ⇒ byte-identical fold. The MCP boundary shape-guards a malformed descriptor (clean 400, not a 500); the FE schema/client forward it and the 0055 `_auto_record_scene` can propose one. New permanent gate: `tests/unit/expressiveNonCollapse.test.ts` + `frontend/tests/test_expressive_non_collapse.py` (the WINNER/NEW-HOH desync branches were phase-scoped so creative prose can't trip a board-outcome rail-correction). | A growing LLM↔engine sync spine risked flattening creative play by forcing the open set (the meaning/texture of social play) through a closed 7-way `kind` enum; ADR 0005 splits authority by **openness**, not layer, and makes non-collapse a regression gate. |
| **0006** (Done) | **Staged-rounds evolution (owner decision):** a competition is now an **endurance-style sequence of visible ELIMINATION ROUNDS** over the still-in field — each round the player picks their approach (compete / throw / play-safe) for THAT round, seeing who remains, and the lowest score DROPS until one stands. Reuses the 0006/0028 math (`resolveElimination` reads the same scores to pick the lowest); the staged sub-loop is `CompetitionProgress` / `advanceCompetition` in `src/engine/liveSeason.ts`, surfacing a per-round `comp-round` pending (with the **still-in field**) through the existing decision seam (the FE per-round prompt replaces the single binding popup). **Anti-sycophancy holds per round**: each round's approach is committed BEFORE it resolves (structured selection only — never prose), LOCKED after, adaptation forward-only. Favorite-win calibration holds (≈73%, inside the 65–80% band) — `tests/unit/stagedCompetition.test.ts`. See [0006 §8](./0006-outcomes-by-stats-and-temperature.md#8-staged-rounds-evolution-the-per-round-approach). | The single up-front, irrevocable approach was too rigid — the player committed blind to how the field would narrow. Strategy should adapt to the field as it thins (everyone left an ally → throw; a threat still in → keep competing), while the per-round commit-before-result keeps a loss from being re-labeled a "throw." |
| **0063** (Done) | **AI-driven identity half (issue #544, the deferred second half of the 2026-06-23 ruling):** the FE producer-LLM now **proposes** each houseguest's **descriptive** identity facets (heritage/ethnicity, gender presentation, orientation, disclosure, age) to U.S.-population rates and writes them back via the new `recordCastIdentity` write-back (the four-place FE-driven pattern); the engine **validates + repairs** the whole-cast proposal through `repairDiversityLayer` (the SAME floors/caps/weighted-expectation as the seeded floor — even a monochrome/biased proposal is repaired to a realistic cast), **re-grounds `skinTone` from the FINAL heritage** (the PR #527 hinge), folds the PUBLIC facets onto the byte-stable cast, and re-seals each PRIVATE orientation into the Vault. The **hard boundary**: only descriptive identity is accepted — NEVER a hidden game weight (the seeded Day-1 read / competition leans stay engine-owned; anti-sycophancy #3 + juryReach). **Calibration-neutral** (the layer rides the SAME isolated descriptive sub-streams — the #338 golden test stays the proof; an empty proposal is byte-identical to the floor). FE driver `frontend/src/orwell_cast_identity.py` runs BEFORE deep authoring (pipeline: identity → author → shoot); with no model the deterministic weighted floor (PR #527) simply stands. Gates: `tests/unit/castIdentity.test.ts` (repair-holds + boundary), `frontend/tests/test_544_cast_identity.py`. | PR #527 shipped the fix+retune half (US-population-weighted floor); this adds the LLM-driven layer the owner asked for ("I need the LLM to seed the characters… I want AI to drive the game engine") while the engine keeps the realism guarantee. |
| **0064** (Done) | **Instant cross-window HUD parity from the chat-turn path (ship-gate F5, status/gadget half).** The 0064 `game-updated` server-push fired from only THREE `orwell_routes.py` endpoints (decision + the two self-eviction routes) — **never** the chat-turn path. So a chat turn whose agent-loop tools (or the 0055 `_auto_record_scene` belt) mutate engine state refreshed **only the SENDER's** HUD (chat.js → the g15 dispatcher, client-side); peer windows got no push and stayed stale until their 20–30s poll (observed live: window A "1 of 15 met" / window B "0 of 15"). The chat DONE seam now calls `publish_game_updated_after_turn(user, beatSeqBefore, toolsCalled)` (`routes/chat_helpers.py`) — gated on an **actual mutation** (a `GAME_ENGINE_WRITE_TOOLS` call ran, or the user's `beatSeq` advanced — catching the FE error-correction belts) — and chains a second push onto the E22/0055 `recordInteraction` fallback task (which mutates after settle). The FE reaction is **unchanged** (the `game-updated` SSE still routes through the ONE g15 dispatcher via `sessionSync.js notifyGameUpdated → window.orwellGameChanged('sync:game-updated')`). Vault-free (a bare "refetch" signal — no state body; each window re-fetches its own Vault-free projection). See [0064 §B/D amendment](./0064-live-multi-device-game-sync.md#amendment-2026-06-28--instant-hud-parity-from-the-chat-turn-path-f5-statusgadget-half). Gates: the harness HUD-parity gate `mirror_hud_parity.mjs` (via `run_mirror_gate.sh MIRROR_HUD=1` — key-free, RED→GREEN) + `frontend/tests/test_1130_hud_parity_instant.py`. | A streaming game turn is the dominant mutation path, and it was the ONE mutation seam that never pushed — so two windows on one game silently diverged on every turn until the slow poll. Closes F5's status/gadget half (the complement to the R2/ADR 0015 chat-render half). |

New cross-cutting expectations introduced by drafts (not yet built, listed so the implementer
plans for them): **0020** adds `gameStatus` (a Vault-free public-state projection for the status
panel) and `portraitDescriptorFor` (a Vault-free descriptor over 0004's public appearance facets);
**0018/0019** formalize `getMomentPrompt` + the `pendingDecision`/`executeDecision` decision seam
(as built: `submitDecision`, with `advanceGame` driving the loop — the engine surfaces the legal
option set; binding choices execute only through validation).

## Ship-gate F5 — realtime two-window live-mirror parity (FE; executable backing)

The launch-acceptance bar's **F5** (realtime two-window mirror parity — the #1 release blocker;
`docs/audits/2026-06-27-ship-gate.md`) is FE-side and has **executable BDD backing outside the
Cucumber lane** (that lane is the TS engine and cannot run an FE-render scenario). Per
[ADR 0015](../decisions/0015-collapse-duplicated-live-render-paths.md) (the render-layer half of
[ADR 0012](../decisions/0012-two-window-lockstep-mirror.md) / refactor-roadmap **R2**), message
*delivery* is sound — the defect is a render-layer duplication, and the fix collapses the observer
onto the sender's one incremental renderer. Its acceptance is the Given/When/Then in
[ADR 0015 §Testability](../decisions/0015-collapse-duplicated-live-render-paths.md#testability--acceptance):
*two windows on one started game → A streams a turn → B renders it live, through the same incremental
renderer, within a bounded lag → reasoning never reaches the public bubble.* Executable gate:
`docs/audits/playtest-harness/mirror_live_parity.mjs` (run via `run_mirror_gate.sh`, key-free against
a deterministic fake streamed model — harness README §10) + the `frontend/tests/test_0012_mirror.py`
`xfail` tripwire. Currently **RED** on `main` (B sits blank, then a late `softReloadHistory` reconcile
through the non-incremental path); flips green when **R2** unifies the live render path, at which point
the harness becomes a required CI gate and the tripwire `xfail` is removed.
