# PO Review Ledger

The single source of truth for **product-owner sign-off** on every feature. We comb through the
feature set one at a time; the PO marks each **Approved / Expand / Delete / Defer**, and the
decision is recorded here with the date and any notes.

This ledger tracks **PO sign-off** — it is distinct from the **build status** in
[`README.md`](./README.md) (which tracks *built vs. spec-only vs. deferred*). A feature can be
built but not yet PO-reviewed, or PO-approved but not yet built.

## Decision legend

| Mark | Meaning |
|---|---|
| ✅ **Approved** | PO reviewed the scenarios and signs off as-is. |
| ✏️ **Expand** | PO wants added/changed scenarios; new ones written BDD-first, then it becomes Approved. |
| 🗑️ **Delete** | PO wants the feature removed. |
| ⏸️ **Defer** | PO parks it intentionally (keep the spec, don't build now). |
| ⬜ **Pending** | Not yet reviewed. |

## Sign-off table

| # | Feature | Build status | PO Decision | Date | Notes |
|---|---|---|---|---|---|
| 0001 | Vault Wall isolation | ✅ Built | ✅ Approved | 2026-06-28 | — |
| 0002 | Event visibility & propagation | ✅ Built | ✅ Approved | 2026-06-28 | — |
| 0003 | Behavioral fidelity | ✅ Built | ✅ Approved | 2026-06-28 | — |
| 0004 | Replayability & naming | ✅ Built | ✅ Approved (expanded) | 2026-06-28 | DONE+GREEN: wired the parked "public appearance fields for portraits" scenario into the live BDD suite (567 scenarios pass) |
| 0005 | Competition eligibility | ✅ Built | ✅ Approved (expansion queued) | 2026-06-28 | Houseguest's Choice (veto draw) must pick strongest **strategic** choice, not merely strongest bond. Test written as PENDING-BUILD block in the .feature; build is opt-in + calibration-neutral (changes the veto field → seeded outcomes). See build queue below |
| 0006 | Outcomes by stats + temperature | ✅ Built | ✅ Approved (expanded) | 2026-06-28 | **ALL THREE PARTS BUILT & GREEN.** (1) intent asked ONCE up front (removed cosmetic per-round prompts); (2) upset-band tuned down — temperature 0.36→0.40, clear favorite ~64%→~59% avg, juryReach EARNED-WINS re-verified; (3) every NPC carries a derived comp intent (compete/throw/play-safe), opt-in `ORWELL_COMP_INTENT`, byte-identical off. Single-roll model kept (Path B declined). Gates: `stagedCompetition` + `compIntent` + `npcCompIntent`. Full unit suite (1957) + juryReach green |
| 0007 | Persistence non-degradation | ✅ Built | ✅ Approved | 2026-06-28 | Reviewed full BDD (.feature + step defs). Toothed non-degradation (deep-equality round-trip, soul grows >50 entries, character baseline frozen, Vault/Journal co-versioned). Crash-safety lives in 0031 |
| 0008 | Daily-event invariant | ✅ Built | ✅ Approved | 2026-06-28 | Reviewed full BDD (.feature + step defs). No empty days across a 16-week season (seeds 1/2/3); week = HOH reign (5–6 days, HOH-comp→eviction); ≤1 social day/week, minority of weeks, still carries a house event |
| 0009 | MCP tool boundary | ✅ Built | ✅ Approved | 2026-06-28 | Plumbing/enforcement feature — the locked door enforcing the Vault Wall (#2) + anti-sycophancy (#3) at the app's tool API. No new gameplay decision; upholds rules already approved in 0001. Trust-the-engineering approve |
| 0010 | One-liner deployment & update | ✅ Built | ✅ Approved | 2026-06-28 | Plumbing feature (install/update one-liners; saves survive updates; no secrets in repo). **PO confirms it has been run on a real server** — the long-standing "real-Proxmox-host smoke owed" deferral is satisfied per PO |
| 0011 | Weekly loop orchestration | ✅ Built | ✅ Approved (expanded + cleanup) | 2026-06-28 | Core gameplay spine — faithful, deterministic, rule-enforcing. Light-expand: cross-reference note that this tests the BASELINE nomination read; the live game refines it via 0044/0043/0085/0086/0107/0039. **Cleanup:** removed the redundant test-only `chooseNominationsWithMood` (duplicated `nominationStrategy`'s mood path), retargeted its 2 tests + dropped a dead `void` ref. Green. **PR: deferred — combine with next edited feature** |
| 0012 | Conversation & scene system | ✅ Built | ✅ Approved | 2026-06-28 | "The conversation is the game" (ADR 0003) with the anti-sycophancy guardrail proven byte-identical: free-text lobbying CANNOT move a binding decision; NPCs never voice a fact they have no pathway to; reads hint but never name off-screen events; all surfaces Vault-swept |
| 0013 | The Diary Room | ✅ Built | ✅ Approved (expanded + cleanup) | 2026-06-28 | Two walls solid (player-DR→no-NPC; NPC-confessional→Vault-only). **Clarified purpose:** the DR is an EXPRESSIVE channel (journal + duplicity enabler + retrospective payoff), no live mechanical effect by design (anti-sycophancy). **Cleanup:** removed the dead/unwired `playerStrategyRead` identity-stub + its 2 tests; unified the magic-number NPC sweeps into a documented `SEEDED_NPCS` (real guarantee is the structural `deriveNpcKnowledge` strip). A purposeful engine read → future feature FB-1. Green. **PR: deferred — combine with next** |
| 0014 | Jury & endgame | ✅ Built | ✅ Approved | 2026-06-28 | Faithful endgame — jury management has real teeth (blindside/disrespect lowers a juror's vote), relationships dominate while the finale sways close jurors, 18-Q&A ceremony, engine decides the vote (narration can't move it). Mechanics solid. **PO flags: the finale *feel/logistics in practice* to expand later → backlog FB-2 (see 0037)** |
| 0015 | Character creation (OOBE) | ✅ Built | ✅ Approved | 2026-06-28 | Nails "author WHO you are, not HOW the game treats you" — no stat max-out, bounded like NPCs, player can lose, private strategy walled, OOC. This is the engine primitive; the player-facing layer is the casting interview (0050). **PO raised: how the interview determines player stats → DEFERRED to the 0050 review (options A/B/C; player currently gets a FLAT archetype bias while NPCs get seeded jitter)** |
| 0016 | God Mode (admin port) | ✅ Built | ✅ Approved | 2026-06-28 | PO considered DELETING it (feels pointless — invisible to players, dated "dev cheat" framing). Investigated: **load-bearing, kept.** It's the operational surface behind the live admin panel — `setTimeOfDay` (the Settings clock toggle), `sandboxHealth` (status page), `manageSandbox` (reset/save/load), `configure`, `advanceToFinale` (debug), all live-wired in `frontend/src/orwell_engine.py`. Also the 2nd surface the Vault Wall is proven against (0001/0009) + tied to the admin-token security (E27/SEC5) that 0029/0053/0068/0074 depend on. Deleting would break the time-of-day switch, health page, sandbox ops. Optional scope-clarification expand declined |
| 0017 | Relationship model | ✅ Built | ✅ Approved | 2026-06-28 | The calculated heart (decision 0002) — graded directed signals (trust/affinity/threat), never stored ally/enemy flags; labels derived on the spot & disposition-colored (same history → "enemy" for paranoid, "ally" for trusting); asymmetric; betrayal-shock + neglect-decay; earns confidence. NOTE: the "Houseguest's Choice = strongest bond" scenario evolves with the queued 0005 strategic-pick expand |
| 0018 | Narrative & moment orchestration | ✅ Built | ✅ Approved | 2026-06-28 | The narrator framing (ADR 0003) + anti-sycophancy/Vault Wall at the prompt layer: model always framed as host/"voice of the house" (never a generic assistant); building the prompt is a PURE READ (can't advance the game — proven byte-identical on a live nomination); told the game decides + "never invent"; lever-manifest drift-gated; every moment's prompt swept sentinel-free; woven context is names + public facets only (no stats/souls/hidden) |
| 0019 | Agent-driven play loop | ✅ Built | ✅ Approved | 2026-06-28 | The "referee" turn-loop tying together 0011/0012/0018: agent reads state → narrates → surfaces ONLY legal options → validated execute → engine decides. Free-text never binds, illegal choices refused (state unchanged), agent can't fabricate a winner, every tool result Vault-free |
| 0020 | Player experience (status, decisions, portraits) | ✅ Built | ✅ Approved (as-is) | 2026-06-28 | Live scenarios solid (public-only status, legal-option decisions, validated path, Vault-free portraits). Has a "PENDING" block (no numbers/badges · public-only options · confirm-before-binding) — reviewed: the *behavior* exists (confirm-before-binding is built + FE-tested C20; numberless is a core mandate), just not wired into THIS feature's BDD. **PO chose NOT to expand (no UI work here); left as-is deliberately** |
| 0021 | Game session & save lifecycle | ✅ Built | ✅ Approved | 2026-06-28 | Multi-tenancy backbone — cross-user isolation STRUCTURAL (distinct object graphs, no cross-sandbox handle): nothing of user A's game (secret OR visible) ever reaches B; one game/user (new game resets only yours); 8 concurrent distinct; resume byte-identical; Vault Wall holds per-sandbox; admin can't browse across users. First-class guarantee beside the Vault Wall |
| 0022 | Player experience MVP-2 (rich game UI) | 🗑️ Removed | 🗑️ Deleted | 2026-06-28 | PO cut it. The one deferred spec — a full dashboard UI that ADR 0003 ("chat IS the UI") deliberately never took, and whose goals were already delivered chat-forward (0020 status panel, 0051 portraits, 0054 gadget rail). Deleted both files + fixed all live references (README index, root README, CLAUDE.md, 0020.md); historical audit logs left as point-in-time record |
| 0023 | Consequence & memory (the backbone) | ✅ Built | ✅ Approved | 2026-06-28 | "The point of the game" — act→hidden fold→persist→recall, invisible-but-consequential, deterministic, lossless (survives a process restart), drives later behavior. The reviewed scenario is fine (voter's OWN shift). PO flagged the secret-vote→jury-grudge issue (a DIFFERENT fold, 0037/0047): **ruled option B**, realized as the new **feature 0110 (vote deduction / process of elimination)** — spec written 2026-06-28 |
| 0024 | Soul storage & memory recall | ✅ Built | 🔧 Approved + Expanded | 2026-07-06 | The house's *intelligent* memory — recall surfaces the RELEVANT past beat (an old betrayal stays salient when it matters), not just the recent one; feeds BOTH the relationship read (mechanics) and the narrator's voice (a specific past beat); hidden memory shapes behavior but never leaks (SOULSENTINEL swept); deepens monotonically while the static Character stays byte-stable; engine-only vector index (dependency-cruiser); deterministic. Strong coverage. **PO ruled the open `soul.md`-structure knob → Option B (lightly-sectioned): the inner diary now renders under Memories / Leanings / Feelings headings, auto-classified from content, authoritative record unchanged. Built & gated** (`tests/unit/soul.test.ts` +6, new `.feature` scenario) |
| 0025 | Reserve twists | ✅ Built | 🔧 Approved + Expanded (rebuilt) | 2026-07-06 | PO found the original shipped only 1 of 3 pooled twists (double eviction), pre-scheduled at setup. **Redesigned + rebuilt to spec:** all three twists (double eviction, secret veto, battle-back) are live in a **standing reactive pool** — each fires when the live house EARNS its trigger, on **per-season dynamic thresholds** (no cross-playthrough predictability), with a **hold-back** roll (some seasons quiet), **all three may fire in one season** (each ≤ once, cooldown-spaced). **Player plays their own** (secret-veto decision; battle-back competes). Secret veto = one-time safety off the block (still one eviction); battle-back = a juror re-enters yet the jury still closes at 9. Sealed from player+admin, format-preserving, deterministic, durable. Gated behind `ORWELL_REACTIVE_TWISTS` (off ⇒ byte-identical legacy path; calibration baseline untouched). Gates: `reserveTwistsReactive.test.ts`, `reactiveTwistsLive.test.ts`, reactive `.feature` (17 scenarios) |
| 0026 | Relationship math | ✅ Built | ✅ Approved | 2026-07-06 | The numeric engine under 0017 — the per-interaction update rule, **betrayal-shock (sharp one-step drop)**, slow decay (**grudges stick by default** = realistic/competitive), confidence growth, all modulated by **disposition × temperature** so each cast feels different yet is seed-reproducible. Player never sees a number (0020). Exceptionally well-tested (incl. a "zero the constant ⇒ betrayal is a literal no-op" proof that no number is hard-coded outside the one tunable module). **PO note:** added a **§14 "Downstream effects" cross-reference** — a dozen+ built features now layer on this base (0028/0041 feel · 0087/0024 arcs · 0043/0107/0059/0095 graph structures · 0075/0098 trust-gated · 0039/0109/0093/0099 deals+leverage · 0038/0094 beliefs · 0100/0110 endgame grudges · 0023 the fold). Stickiness baseline is THE balance lever if ever retuned |
| 0027 | NarrativePort LLM adapter | ✅ Built | ✅ Approved | 2026-07-06 | The narrator contract — "the narrator voices, the engine decides." Handed ONLY the Vault-free context (sentinel-clean, exactly the 6 allowed keys); async + streaming (stream concatenates to full); resilient (provider error ⇒ safe fallback, game state untouched); **a hallucinated result changes NO game state** (proven byte-identical against a LIVE game); provider/model/key from env, no secret in code. **PO asked how a hallucination is corrected & what the player sees** — answered: game STATE is never wrong (the hard guarantee); the FE runs a 2-layer desync spine (`chat_helpers.py`) — closed-set board claims corrected pre-emission, a slipped outcome re-grounded next turn — while creative prose is never normalized (ADR 0005). Defense-in-depth: stray hallucinations are transient/self-healing, the board is always right. Note offered, PO declined (approve-and-move-on). **Architecture:** the DEPLOYED narrator is the FE's LLM over MCP; this engine-side `LlmNarrativePort` is the same contract for tests/headless — both honor Vault-free-in + engine-decides |
| 0028 | Temperature & emotional constants | ✅ Built | 🔧 Approved + Expanded | 2026-07-06 | The game's "swing" numbers in one tunable module (sibling to 0026): bounded temperature, per-variable weights, the soul-sourced emotional modifier (mean-reverts), rare hidden-element surfacing. Well-tested — favorite unprotected (player odds == NPC odds), bounded/rule-safe, mean-reversion, ≤15% surfacing, and a "zero the temp weight ⇒ favorite wins 100%" proof of no hidden numbers. **PO caught stale calibration:** the doc headlined **~72%** but the **0006 retune** (temp 0.36→0.40) had lowered the measured 0.9-vs-0.5 favorite to **~66%**, sitting right on the test's 0.65 floor. **Fixed:** spec re-centred on ~66% + a 0006 cross-reference note, and the calibration gate floor **widened 0.65→0.60** for headroom (0.80 ceiling + ≥10%-upset + no-protection invariants unchanged). Gate green (623 BDD) |
| 0029 | App admin & user management | ✅ Built | ✅ Approved | 2026-07-06 | The **account/login tier** (NOT the game's God Mode 0016) — first-account-is-admin, propagable promote/demote, an admin-only Users manager, self password change + admin reset-without-current (revokes the target's sessions), LLM-settings gated to the `manage_llm_settings` entitlement. **Every admin power re-checked server-side** (proven: non-admin hits `require_admin`/`require_entitlement` → 403; unknown entitlement fails closed). **Last-admin lockout guard** solid both ways (can't demote/delete the sole admin; can once a 2nd exists). B57/H4 amendment: account deletion **wipes the user's game sandbox** (typed-username confirm, no self/last-admin delete). Load-bearing for the admin-token boundary (0053/0068/0074). The draft's §8 "gaps" are all now built. Validated by `fe-unit` (`test_app_admin.py` + `test_fe_user_delete.py`), not the BDD gate |
| 0030 | Durable game persistence (survive restart) | ✅ Built | ✅ Approved | 2026-07-06 | Fixes the "Welcome to the house" overlay re-firing — the live game is now **saved-on-mutation, recalled-on-return** to a disk-backed `FileSaveStore`, so an engine restart RESUMES the season instead of wiping it. Rigorous tests (real temp-dir disk; "restart" = a fresh registry over the SAME dir — the true new-process analogue): started game survives with same week/phase/roster; no-save user still onboards; **per-user isolation holds across restart**; **non-degradation** (superset + counts non-decreasing + static Character byte-stable); **Vault Wall holds on reload** (sentinel off-screen scheme + hidden attr never surface across 5 swept surfaces). The persist→recall half of 0023 made durable. Amendments honored: **H8** — resume re-enters IN-FICTION (fresh morning scene, never an OOC recap dump, ADR 0003); **H4** — save/load respects the 0007 ratchet, account deletion wipes the sandbox (0029). Load-bearing backbone |
| 0031 | Game orchestrator & integrity checkpoint | ✅ Built | ✅ Approved (+ **real-time purge**) | 2026-07-10 | The runtime backbone: **one `advance()` spine** = a committed turn (snapshot → run off-screen life + progression + consequence fold → verify → keep-or-revert). **Fail-closed integrity checkpoint** enforces the 4 mandates AT RUNTIME (non-degradation, Vault Wall on player+admin, eligibility legality, cross-user isolation) — a leaky/degrading advance is **rolled back, not saved**, prior save intact, fault logged (proven with a real leaking apply). Deterministic (same seed+turns ⇒ byte-identical); God-Mode health is Vault-free metadata only (player has no access). **PO ruling 2026-07-10 (executed): remove the real-world clock entirely.** The house must live **only** on the player's play-clock — one bounded off-screen tick per committed turn — and must **never** scheme in the background. So the wall-clock `GameWatcher`/`SystemClock`/`ORWELL_WATCHER_*` were **deleted, not disabled** (no "hardcore mode" in any future iteration); the runtime `Clock` is now a pure **`LogicalClock`** (fixed epoch, advanced one step per committed mutation, never `Date.now()`) — which also makes 0108's golden record/replay deterministic for free, and makes off-screen ids/confessional seeds run-to-run reproducible. Feature **0035 removed**. Advance ≠ ceremony beat: beats are sparse plot milestones (~1/day), most turns are lingering that advance **in-game time-of-day (0066)** + hidden life without moving the beat. Spec (§8), feature file, BDD steps, and CLAUDE.md all reconciled to turn-driven-only; full gate green (typecheck/build/arch/unit/BDD/byte-identity/heavy calibration sims) |
| 0032 | Front-end surface reduction ("game build") | ✅ Built | ✅ Approved | 2026-07-06 | Strips the inherited general-purpose workspace FE down to **just the game** — one `ORWELL_GAME_BUILD` switch (default on) drops email/calendar/contacts/editor/gallery/cookbook/notes/tasks/shell/websearch/webhooks + the FE's own memory/RAG/skills; keeps chat/LLM/MCP-agent/onboarding/status/portraits/accounts/theme (voice kept, off by default). **Security + architecture, not just cleanup:** (1) the live arbitrary-**shell** endpoint (was only CSS-hidden!) now **404s even for an authenticated admin**, no command executes (+ structural guard on `app.py`); (2) **no rival FE memory/RAG/skills** feeds chat outside the engine's Vault discipline — the engine moment prompt is the only injected framing. All **3 tiers** done: server-side 404, JS not shipped, **94 modules deleted** with a headless-browser gate proving zero broken imports. FE-only, engine untouched. Foundation later hardened into the CI anti-sycophancy wall (0073). Validated by `frontend/tests/test_game_build.py` (15) + boot/browser smoke |
| 0033 | Dynamic player tagline | ✅ Built | ✅ Approved | 2026-07-06 | Small cosmetic hero line — the engine writes a snarky, state-aware BB one-liner from the player's PUBLIC standing (replaces "Yours for the voyage."). Vault-free by construction (via NarrativePort 0027); **anti-sycophantic** (weak standing ribbed, never flattered into false safety); changes as the game advances; cached per moment; **fails open** to a static themed line on narrator error OR an Echo context-dump. Well-tested (`playerTagline.test.ts`). Recorded deviation: unit-gated + FE-render-tested, not in `cucumber.cjs` (E87a). **PO raised a bigger UX question here → backlog FB-3 (avoid an infinite chat; day-chunk the experience).** |
| 0034 | Live weekly progression & decision seam | ✅ Built | ✅ Approved | 2026-07-06 | The live weekly-loop spine + the guarded binding-decision doorway (codifies already-built `liveSeason`/`advanceGame`/`submitDecision`, driven through the real MCP player channel). `advanceGame` auto-resolves NPC beats and **halts** at the player's turn with only legal options (idempotent — advancing again doesn't skip); binding choices apply **only** through the validated `submitDecision` — **never parsed from prose** (mandate #3 in action) — and an **illegal** choice is refused with the decision still standing. Beats recorded as player-witnessed (non-hidden); survives restart (exact week/phase); outputs Vault-free (planted hidden sentinel never leaks). **Strong determinism proof**: two same-seed runs take a byte-identical 60-advance trail AND identical committed event records. All Wave-2 pending kinds (comp-intent/tie-break/final-eviction/houseguests-choice) go through the same contract |
| 0035 | Live off-screen life (the running watcher) | 🗑️ **Removed** | 🗑️ Deleted | 2026-07-10 | **Deleted per PO ruling (real-time purge).** This feature was the wall-clock background watcher that advanced idle games on real time — exactly what the PO ruled must never exist ("I don't want the characters scheming in the background at all… only in between turns"; "no future iteration" of a real-time/hardcore mode). The `.feature` + `.md` are deleted, removed from `cucumber.cjs`, and its step definitions purged. The valuable invariant it protected ("the game advances only when the player takes a turn") is now the **default** and is covered by 0031's turn-driven spine. See the 0031 row |
| 0036 | Live social surface (approaches + Diary Room) | ✅ Built | ✅ Approved + **Expanded (0115)** | 2026-07-10 | Reviewed full spec + unit gates (`liveSocialSurface`/`diaryRoom` tests) + the live Vault-sentinel sweep. Two built-but-unwired capabilities made live: **NPC-initiated approaches** (`socialInitiatives` — bidirectional scenes, names + coarse "bond"/"probe" motive, no hidden numbers, gated off the cold open per ruling #5) and the **Diary Room** (`diaryRoom` — walled OOC channel, witness=player, `NO_NPC_PATHWAY`, stripped from NPC derivation — proven). Coverage solid; the wall genuinely tested. **PO Q&A → EXPAND:** the DR was expressive but had **no mechanical teeth** ("how is it different from OOC chat?"). PO asked for a combo of narration-grounding + weighted producer prompts + a retrospective through-line, with the hard rule that DR **never reaches NPCs**. Shipped as **new spec 0115** (see its row) — the GM now narrates the player's REAL strategy (anti-sycophancy pointed at the player's own lies) while the house stays structurally deceived. (Noted the E87a BDD deviation on 0036 itself; 0115 is BDD-gated properly.) |
| 0037 | Live jury-vote choreography (the finale) | ✅ Built | ✅ Approved | 2026-07-10 | Reviewed full spec + 10 BDD scenarios (`jury_choreography.steps.ts`) + FE UI (B26/C11). The interactive Final-2 finale: opening statements → all 9 jurors question BOTH finalists (18 Q&A) → one-at-a-time vote reveal → crown; player answers every juror when a finalist, casts their own vote when a juror. **Anti-sycophancy is airtight** and PO-verified against the code: the winner is engine-decided from FIVE weighted factors — **relationship 1.0, eviction manner 0.8, game-respect/résumé 0.7, reliability 0.4** (≈90% of the vote is the whole SEASON) and **finale performance only 0.3** (≈10%, and it only tips already-CLOSE jurors — a great finale can't overturn a clear lead, proven >85% dominance while making the WEAKEST appeal). The finale sway is a **structured appeal enum** (own-game/mend/connect/discredit) the engine scores — the LLM never grades prose. Jury management cuts BOTH ways (A5: a juror the PLAYER blindsided holds it against them). Opening statement + player-juror question are deliberately **scoreless flavor** (PO comfortable — prose must not buy a win). Vault-free (sentinel-clean, no lean/tally/manner leaks pre-reveal), restart-safe, seed-deterministic, last-evicted-juror tie-break. Reuses 0014 jury math verbatim |
| 0038 | Live off-screen society | ✅ Built | ✏️ Approved + Expanded | 2026-07-12 | Reviewed full spec + 6 BDD scenarios (`offscreen_society.steps.ts`). The mandate's #1 feature — the house lives a **varied hidden life** between turns (7 interaction types), **gossip diffuses NPC→NPC** with drift + decaying confidence + provenance and reaches the player only by a terminating pathway (never a number), scenes **deepen the souls** (recall-able, monotonic), all **bounded** (one tick/committed turn), **seed-deterministic**, **Vault-walled**, and **per-user isolated** — every claim proven by a scenario. **Doc cleanup:** the design note's stale "partially implemented / remaining B27b" header was corrected (gossip→player + soul deepening are long-since built + gated). **Now central to the in-game-time pivot:** 0117 un-silences this society into the player's between-ceremony social play (paced to the in-game clock). **PO expansion → new feature 0120:** vary the scheming CADENCE by each houseguest's strategic drive (a sharper player schemes a touch more often — the PO's "slight variance to personality/strategic intelligence" note), calibration-safe. |
| 0039 | Promise & deal tracking | ✅ Built | ✏️ Approved + Expanded | 2026-07-12 | Reviewed full spec + 6 BDD scenarios (`deal_tracking.steps.ts`). A first-class, **engine-adjudicated** promise — the ENGINE (never the narrator) decides kept/broken from the structured binding action; a broken deal deterministically folds betrayal-shock (trust↓/threat↑, sticky) + a jury demerit + a witnessed reveal, and reaches the player as a **rumor by pathway** for NPC↔NPC deals, never a number. Prose-independent (a flowery deal gets the same verdict as a plain one). One of the cleanest anti-sycophancy levers in the build. **PO Q&A → EXPAND (new feature 0121):** (a) keeping was under-rewarded vs. breaking — the reward (a hidden non-decaying reliability signal → jury 0.4 + nom-safety) is real but subtle/back-loaded, so add a **mix of 3 felt rewards** (a "keeps their word" reputation that spreads as positive gossip → more deals/trust; a reliable ally who actively protects you; loyalty streaks that compound the bond); (b) add **two ACTIVE-obligation deal kinds** — `comp-throw` ("throw a comp for you") + `veto-save` ("use the veto on you") — where a break is a FAILURE TO ACT (the first four kinds are all defensive "don't move against me"). PO's "why deal types?" answered: types are the engine's *grip* that turns a free-form chat promise into something enforceable — they amplify creativity, not limit it. **0121 Part 1 built** (the active-kind adjudication + flag, calibration-safe); Part 2 (live wiring + the 3 rewards + FE) to follow. All flag-gated (`ORWELL_DEAL_DEPTH`, default off ⇒ byte-identical). |
| 0040 | NPC confessionals | ✅ Built | ✏️ Approved + Expanded | 2026-07-12 | Reviewed full spec + 6 BDD scenarios (`npc_confessionals.steps.ts`) + the live wiring (`GameSessionAdapter.recordCeremonyConfessionals` + `orchestrator.defaultApply`). The **inverse of the player DR (0013)**: an NPC's private, engine-grounded read (top-threat target + strongest bond), recorded **Vault-only** (`hidden`, `witnessSet:[npc]`) — proven sealed from **both** the player AND admin/God Mode — folded into the soul (recall-able, monotonic), seed-deterministic. One of the strongest anti-sycophancy/Vault-Wall features in the build (the NPC's "real feelings" are *queried*, never improvised — the flip-test proves it). **Traced the whole lineage** (0089 reactive openers, 0090 voice, E55 mood/trigger) so the expansion doesn't duplicate. **PO Q&A → EXPAND (new feature 0122):** confessionals today are (a) **thin** — only biggest-threat + trust — and (b) **rare** — only the 3–5 ceremony-standers confess, plus one random off-screen confessor per tick. PO asked for **deeper** content (**plan, standing/safe-vs-exposed, a grudge distinct from the target, the aftermath of a big conversation, and reaction to adjacent moves** — "my ally won HOH", "my showmance was nominated") — **triggered, NOT a checklist** (PO clarification 2026-07-12: each facet fires only when the moment earns it, so the **HOH / nominees** get the **deepest** confessionals and a coasting houseguest gets a short one) — and **daily** frequency (**most** houseguests confess once per in-game day, **unless their game is bare** — PO approved the bare-game definition). All grounded in engine truth, Vault-only, calibration-safe (default-off flag + dedicated rng, on the 0117–0119 in-game clock ⇒ no golden re-record). **Doc cleanup:** 0040.md header said "fired at the live nomination ceremony" — corrected to noms + veto + eviction + the off-screen tick. |
| 0041 | Character evolution & arc (the linchpin) | ✅ Built | ✏️ Approved + Expanded | 2026-07-12 | Reviewed full spec + 6 BDD scenarios (`character_evolution.steps.ts`) + the live wiring (`emotionalArc.evolveEmotion`, the soul in `buildEngineCore`, driven from the consequence fold + off-screen tick). **The linchpin** — a season now *changes* a houseguest: consequential events move their hidden soul, that bends their competitions + nominations, it accumulates into a recall-able arc, the static CHARACTER stays byte-stable, and the player sees no number (all four mandates at once). Rigorously tested (mood isolated as the only variable → measurable behavior delta with the hard rule still held; 200-beat game proves CHARACTER byte-stable while a soul drifts; bounded/mean-reverting/deterministic; sentinel-clean). **PO Q&A → EXPAND (new feature 0124):** found three ways it's still thinner than a real houseguest — (1) affect is **one dial** (can't be confident AND on-edge at once); (2) only *mood* drifts, never *strategy*; (3) **the PO's catch** — an NPC's starting reactivity is a **random draw**, NOT their personality (the player's is disposition-derived via `VOL_OF`, NPCs get `rng.next()`), so a "temperamental" archetype is only reactive by luck. 0124 fixes all three (multi-axis affect · temperament drift · disposition-tuned reactivity), one default-off flag `ORWELL_SOUL_DEPTH`, calibration-safe. Spec written: `0124-deeper-character-evolution.{md,feature}` (7 scenarios). |
| 0042 | Competition library | ✅ Built | ✏️ Approved + Expanded | 2026-07-12 | Reviewed full spec + 6 BDD scenarios (`competition_library.steps.ts`) + the source (`competitionLibrary.ts` — 12 mechanic defs, `drawCompetition` seeded no-immediate-repeat; resolution routed through the unchanged 0006/0028 math; the Vault-free result carries name/format/narrative). Strong core: **real variety** (no back-to-back repeat, reproducible), **right stat governs** (a mental comp favors the mental houseguest), **engine still decides** (favorite strong-majority-but-loses-upsets, player unprotected), **flavor crosses but numbers never** (name+format+premise on a player surface, hard-scanned for leaked stats/scores/ranking/sentinel), **eligibility holds** (exactly 6 play any drawn veto). **PO → EXPAND (new feature 0125):** ~12 mechanics over an ~11-week season means each comp is seen ~twice and a repeated mechanic reads as the same comp. PO wants **"upwards of 100" options / a repeat-free season**, floated either a bigger hand-list or **a pool of variables mixed** + theming. Chose the calibration-safe half: a large seeded **THEME/skin pool** (`mechanic × theme` ⇒ ~240 combos) as a pure Vault-free **projection** (dedicated hash, never the beat rng ⇒ the seeded winner is byte-identical on-vs-off, proven by a live eviction-order guard), default-ON, golden driver pinned off (no re-record). Theme = engine-seeded structure, narrator dresses it (ADR 0003); #1400 model-authored fiction still overrides. **Deliberately NOT growing the mechanic pool** (that re-rolls calibration → fixed-seed BDD breakage — a separate follow-on if reskinned variety still feels samey). Spec + built: `0125-competition-theme-variety.{md,feature}` (5 scenarios, in `cucumber.cjs`) + `competitionThemes*.test.ts`. |
| 0043 | Emergent bloc behavior | ✅ Built | ⬜ Pending | — | — |
| 0044 | Strategic nomination & vote refinements | ✅ Built | ⬜ Pending | — | — |
| 0045 | Endgame structure (Final 5 → Final 2) | ✅ Built | ⬜ Pending | — | — |
| 0046 | Player eviction & the juror's seat | ✅ Built | ⬜ Pending | — | — |
| 0047 | Eviction night live (reveal + goodbyes) | ✅ Built | ⬜ Pending | — | — |
| 0048 | Season retrospective & Vault unsealing | ✅ Built | ⬜ Pending | — | — |
| 0049 | House presence & lingering | ✅ Built | ⬜ Pending | — | — |
| 0050 | The casting interview | ✅ Built | ⬜ Pending | — | ⚠️ PENDING PO EXPAND (raised at 0015 review): pin/enrich **how the interview determines the player's stats**. Today = coarse (interview → 1 of 12 archetypes → that archetype's FLAT bias; NPCs get seeded jitter, player does not; no-archetype → neutral). Options: (A) document+test current, (B) give the player NPC-style jitter, (C) LLM proposes stat lean from self-description, engine validates/bounds (0063 pattern). Anti-sycophancy floor must hold (no max-out, bounded, engine owns magnitude, no numbers shown). PO leaned C, B as fallback |
| 0051 | In-character images (portraits/headshots) | ✅ Built | ⬜ Pending | — | — |
| 0052 | House themes (five seasons) | ✅ Built | ⬜ Pending | — | — |
| 0053 | Admin transcript retrieval | ✅ Built | ⬜ Pending | — | — |
| 0054 | Control-room gadget rail | ✅ Built | ⬜ Pending | — | — |
| 0055 | Social play moves the weights | ✅ Built | ⬜ Pending | — | — |
| 0056 | Season-to-season character continuity | ✅ Built | ⬜ Pending | — | — |
| 0057 | Seasons as levels | ✅ Built | ⬜ Pending | — | — |
| 0058 | Deep character profiles | ✅ Built | ⬜ Pending | — | — |
| 0059 | Hidden seeded relationships | ✅ Built | ⬜ Pending | — | — |
| 0060 | Story-thread scheduler | ✅ Built | ⬜ Pending | — | — |
| 0061 | Player self-eviction | ✅ Built | ⬜ Pending | — | — |
| 0062 | Move-in zeitgeist snapshot | ✅ Built | ⬜ Pending | — | — |
| 0063 | Casting diversity floor | ✅ Built | ⬜ Pending | — | — |
| 0064 | Live multi-device game sync | ✅ Built | ⬜ Pending | — | — |
| 0065 | Cast pre-warm (deep-author before portraits) | ✅ Built | ⬜ Pending | — | — |
| 0066 | In-game time of day & sleep economy | ✅ Built | ⬜ Pending | — | — |
| 0067 | Public internet exposure | 🚧 Built (host smoke owed) | ⬜ Pending | — | — |
| 0068 | Admin "Connect to internet" panel | 🚧 Built (host smoke owed) | ⬜ Pending | — | — |
| 0069 | Token economy & context budget | ✅ Built | ⬜ Pending | — | — |
| 0070 | Off-screen texture enrichment | ✅ Built | ⬜ Pending | — | — |
| 0071 | Defensive hardening (redaction, URL guards) | ✅ Built | ⬜ Pending | — | — |
| 0072 | Multi-platform gateway | ✅ Built | ⬜ Pending | — | — |
| 0073 | Structural anti-sycophancy wall (CI gate) | ✅ Built | ⬜ Pending | — | — |
| 0074 | Local & tunable HTTPS | ✅ Built | ⬜ Pending | — | — |
| 0075 | Trust-gated confidences | ✅ Built | ⬜ Pending | — | — |
| 0076 | Presence grounding & motivated movement | ✅ Built | ⬜ Pending | — | — |
| 0077 | House map, privacy & eyeshot | ✅ Built | ⬜ Pending | — | — |
| 0078 | Motivated society & intentional movement | ✅ Built | ⬜ Pending | — | — |
| 0079 | Runtime overseer & diagnostic log | ✅ Built | ⬜ Pending | — | — |
| 0080 | Active overseer (acts on its verdict) | 📝 Spec only | ⬜ Pending | — | — |
| 0081 | Narration-faithfulness gate | ✅ Built | ⬜ Pending | — | — |
| 0084 | Character voice & grounded mood | ✅ Built | ⬜ Pending | — | — |
| 0085 | NPC campaigns & the scramble | ✅ Built | ⬜ Pending | — | — |
| 0086 | Houseguest drives | ✅ Built | ⬜ Pending | — | — |
| 0087 | Relationship trajectories (warming/cooling) | ✅ Built | ⬜ Pending | — | — |
| 0088 | Living "current read of you" | ✅ Built | ⬜ Pending | — | — |
| 0089 | Reactive confessionals | ✅ Built | ⬜ Pending | — | — |
| 0090 | Per-archetype voice | ✅ Built | ⬜ Pending | — | — |
| 0091 | Trigger secrets → house events | ✅ Built | ⬜ Pending | — | — |
| 0092 | Secret-pacing drip | ✅ Built | ⬜ Pending | — | — |
| 0093 | Secrets as strategic levers | 🟢 Build-ready (PO resolved) | ⬜ Pending | — | Index notes PO-resolved 2026-06-27 (build w/ 0099) — confirm here |
| 0094 | Distorted gossip has consequences | 📝 Spec only | ⬜ Pending | — | — |
| 0095 | Pre-show ties → time-bombs | 📝 Spec only | ⬜ Pending | — | — |
| 0096 | Emergent nemesis | 📝 Spec only | ⬜ Pending | — | — |
| 0097 | Suspicion ledger | ❄️ Frozen/parked | ⬜ Pending | — | Index notes parked 2026-06-27 (over-tells player) — confirm Defer |
| 0098 | Confidence-calibrated reads | ❄️ Frozen/parked | ⬜ Pending | — | Index notes parked 2026-06-27 — confirm Defer |
| 0099 | Secrets as currency | 🟢 Build-ready (PO resolved) | ⬜ Pending | — | Index notes PO-resolved 2026-06-27 (build w/ 0093) — confirm here |
| 0100 | Jury grudge book | ✅ Built | ⬜ Pending | — | — |
| 0101 | NPC myth-making | 📝 Spec only | ⬜ Pending | — | — |
| 0102 | Weekly recap → daily bedtime recap | 🟢 Build-ready (PO resolved) | ⬜ Pending | — | Redesigned 2026-06-27; filename rename pending. (Collision resolved 2026-07-05: the premiere spec moved to **0111**.) |
| 0103 | Edit-bay foreshadowing | ❄️ Frozen/parked | ⬜ Pending | — | Index notes parked 2026-06-27 (spoiler-adjacent) — confirm Defer |
| 0104 | Season-over-season notoriety | ✅ Built | ⬜ Pending | — | — |
| 0105 | Drive-anchored suspicion | ✅ Built | ⬜ Pending | — | — |
| 0106 | Whole-house events are exclusive set-pieces | ✅ Built | ⬜ Pending | — | — |
| 0107 | Named alliances | ✅ Built | ⬜ Pending | — | (Collision resolved 2026-07-05: the observability draft moved to **0112**.) |
| 0108 | Real-model golden-path CI gate | 📝 Spec only | ⬜ Pending | — | — |
| 0109 | Negotiated deal duration | 🟢 Build-ready | ⬜ Pending | — | Amends 0039 |
| 0110 | Vote deduction (process of elimination) | ✅ Built | ✅ Approved (built) | 2026-06-28 | **NEW feature specced AND built this session** from the PO's process-of-elimination idea (0023 review, option B). The secret-ballot jury grudge folds on a DEDUCED belief (public count + eligible pool + the evictee's reads, `deduceEvictionVoters`), not the true vote list: deducible → grudge, undeducible → none (secrecy honored), wrong → misattribution (dramatic irony). Opt-in `ORWELL_VOTE_DEDUCTION` (byte-identical off; juryReach unmoved). Gates: `voteDeduction.test.ts` + BDD `0110` (11 scenarios, in `cucumber.cjs`). True tally still unsealed at 0048 |
| 0124 | Deeper character evolution | ✅ Built | ✅ Approved (built) | 2026-07-12 | **NEW feature specced AND built this session** from the 0041 review — three deepening axes on the character-evolution linchpin: (1) **multi-axis affect** (independent `distress` + `confidence` so a houseguest can be cocky AND on-edge at once, not one slider); (2) **strategic-temperament drift** (a repeatedly-burned houseguest hardens toward paranoia, mean-reverting toward their true baseline — the soul bends, the static CHARACTER never does); (3) **personality-tuned reactivity** (the PO's catch — wire NPC starting volatility + settle rate to their **disposition** instead of the current flat `rng.next()`, so a temperamental houseguest is genuinely more sensitive on the on-edge dial and settles slower). One default-off flag `ORWELL_SOUL_DEPTH` + dedicated/draw-preserving rng ⇒ byte-identical off (juryReach + golden untouched). Hidden layer only, CHARACTER byte-stable. `emotionalArc.ts` + `GameSessionAdapter` (flag `ORWELL_SOUL_DEPTH`); BDD `0124` (7 scenarios) + `deeperEvolution.test.ts` + `deeperEvolutionLive.test.ts`; juryReach byte-identical off; deploy opt-in. |
| 0123 | NPC-initiated deal offers | ✅ Built | ✅ Approved (built) | 2026-07-12 | **NEW feature specced AND built this session** from the PO's ask ("we need a pathway for NPCs to make deals with the player or other NPCs"). Investigation found NPC↔NPC deals already exist (`mintNpcDeal`, hidden) and player→NPC (`makeDeal`) — the real gap was **NPC→player**, now closed: a motivated houseguest **floats the player a deal at a lull**, surfaced as a `deal-offer` pending resolved via the existing `submitDecision` (**no new tool** ⇒ golden-safe; the FE generic card renders it). Grounded kind (strong bond ⇒ final-two, else safety); **accept** → a real player↔NPC deal on the same spine; **decline** → no deal + a small hidden cooling. Player-witnessed (not hidden), Vault-safe, bounded (≤1 open, ≤1/week), seeded. **Calibration-safe:** default-off `ORWELL_NPC_DEAL_OFFERS` + dedicated rng + lull-only ⇒ **juryReach byte-identical**, golden untouched. `GameSessionAdapter`/`liveSeason`/`DECISION.playerOffer` + FE `orwellDecision.js`; BDD `0123` (6 scenarios) + `npcDealOffers.test.ts`; deploy opt-in. |
| 0122 | Deeper, daily NPC confessionals | ✅ Built | ✅ Approved (built) | 2026-07-12 | **NEW feature specced AND built this session** from the 0040 review — the PO's ask that NPC confessionals be **deeper** (five **triggered** facets — **plan / standing / grudge / big-conversation aftermath / adjacent-move**, engine-grounded, on top of the existing threat+trust; NOT a checklist — HOH/nominees get the deepest, a coasting houseguest a short one) and **daily** (**most** living houseguests confess once per in-game day via a `turnIn` day-close sweep, **unless their game is bare** — no recent salient event AND no clear target/ally). Vault-only (sealed from player AND admin — BDD scenario 8 proves both surfaces clean; unchanged from 0040), folded to soul, seed-deterministic. **Calibration-safe:** default-off `ORWELL_CONFESSIONAL_DEPTH` + dedicated per-npc/day rng + gated on the live in-game clock (pinned off in the golden driver) ⇒ **juryReach byte-identical** (re-run green), no golden re-record. `confessionals.ts` + `GameSessionAdapter` (`sweepDailyConfessionals`); BDD `0122` (8 scenarios, in `cucumber.cjs`) + `deeperConfessionals.test.ts` + `dailyConfessionalSweep.test.ts`; deploy opt-in in `smoke.sh` + `orwell-install.sh`. |
| 0115 | Diary Room as a strategic confessional | ✅ Built | ✅ Approved (built) | 2026-07-10 | **NEW feature specced AND built this session** from the 0036 review — the PO's ask that the GM "know it's strategy" and not believe the lies the player tells NPCs. Anti-sycophancy pointed at the player's OWN deception: the player's DR intent grounds the GM's narration (`playerDiaryRoom` → fenced PRIVATE steer in `renderGameContext`), the producer's invitations become pointed beat-specific questions (`DRAMATIC_PROMPTS`), and the confessionals resurface in the 0048 retrospective (`playerConfessionals`). **The wall is STRUCTURAL where it protects the player:** DR never enters NPC knowledge (`deriveNpcKnowledge` unchanged) or per-NPC voicing, never drives NPC behavior — the house stays deceived; only the GM's player-facing narration is grounded (prompt-guided + a targeted no-leak test — the PO explicitly accepted this can't be a hard code wall and still do what they want). Vault-free; byte-identical heavy sims. Gates: `diaryRoomStrategic.test.ts` + BDD `0115` (5 scenarios, in `cucumber.cjs`) |

## Build queue (PO ✏️ Expand decisions awaiting engine work)

Scenarios written/agreed during review that still need implementation to go green.

| # | What to build | Notes |
|---|---|---|
| 0005 | Strategic "Houseguest's Choice" veto pick (replace pure strongest-bond) | Add a strategic-ally selection in the relationship engine; wire `liveSeason` HC pick to it; un-comment the two pending scenarios + add step defs. Ship **opt-in, default-off** with a calibration-neutrality guard (the HC pick changes the veto field → seeded competition outcomes; default-off must stay byte-identical). After a calibration re-baseline, strategic can become default. |
| ~~0006a~~ | ✅ **DONE** — favorite-win band tuned down (temperature 0.36→0.40; clear favorite ~64%→~59% avg). juryReach EARNED-WINS re-verified green; full unit suite green. Gate: `stagedCompetition.test.ts`. |
| ~~0006b~~ | ✅ **DONE** — every NPC carries a derived comp intent (compete/throw/play-safe), opt-in `ORWELL_COMP_INTENT`, byte-identical when off. Single-roll model kept (no per-round adaptivity — Path B declined). Gate: `npcCompIntent.test.ts`. |
| ~~0125~~ | ✅ **DONE (2026-07-12)** — competition theme variety built: `competitionThemes.ts` (`COMPETITION_THEMES` 24 skins, `themeForWeek` per-(seed,phase) permutation, `applyTheme`) + `GameSessionAdapter.themedScaffold` wired into `runCompetition` + `competitionStagingView` behind `ORWELL_COMP_THEMES` (default **ON**; pinned `=0` in the golden driver). Pure Vault-free projection ⇒ seeded winner/eviction byte-identical on-vs-off (live guard); juryReach re-confirmed green; #1400 fiction still overrides. BDD `0125` (5 scenarios) in `cucumber.cjs` + `competitionThemes.test.ts` + `competitionThemesLive.test.ts`. | Deliberately did NOT grow the mechanic pool (separate calibration-costed follow-on). |
| ~~0124~~ | ✅ **DONE (2026-07-12)** — deeper character evolution built: `evolveEmotion` moves independent distress/confidence axes + `temperamentDrift` under `{soulDepth}`; `composedEmotion`/`effectiveDisposition` feed the competition + nomination reads; `applyDispositionReactivity` stamps NPC volatility/settle from disposition (draw-preserving). Flag `ORWELL_SOUL_DEPTH` (default off); juryReach byte-identical; BDD `0124` (7 scenarios) + `deeperEvolution*.test.ts`; deploy opt-in. Original plan: Behind one default-off `ORWELL_SOUL_DEPTH`: **(A)** add optional soul axes `distress`/`confidence`, moved independently in `evolveEmotion` (on) while the legacy `emotionalState` scalar still evolves as 0041 (off ⇒ byte-identical); behavior reads compose confidence-lifts/distress-drags. **(B)** add a bounded signed `temperamentDrift` (0028 mean-reversion family) + `effectiveDisposition(soul, character)` (banded, hysteresis) that the disposition/nomination reads consult only when on; static CHARACTER disposition byte-stable. **(C)** in `characterFactory`, a no-main-stream-draw post-pass sets NPC `volatility = VOL_OF[disposition]` + a disposition `settleScale` (clash slower / bond faster), draw-preserving (still consume `rng.next()`); off ⇒ the random draw stands. All hidden (extend the 0001 canary), persisted (0030). juryReach + golden byte-identical off. Add `0124` to `cucumber.cjs`; deploy opt-in after green. |
| ~~0122~~ | ✅ **DONE (2026-07-12)** — deeper+daily NPC confessionals built: `confessionalFor` gained the five triggered facets (plan/standing/grudge/aftermath/adjacent) + `isBareGame`; `GameSessionAdapter.sweepDailyConfessionals` fires at `turnIn` (day-close), gated on `ORWELL_CONFESSIONAL_DEPTH` (default off) **and** `perConversationClockLive()`, on a dedicated per-npc/day rng. juryReach byte-identical (flag off); BDD `0122` (8 scenarios) in `cucumber.cjs`; deploy opt-in shipped. Gates: `deeperConfessionals.test.ts` + `dailyConfessionalSweep.test.ts`. |
| ~~0110~~ | ✅ **DONE** — vote deduction built: `src/engine/voteDeduction.ts` (`deduceEvictionVoters`) + `liveSeason.ts` `commitStagedEviction` folds the grudge on the deduced belief behind `ctx.voteDeduction`; `GameSessionAdapter` flag `ORWELL_VOTE_DEDUCTION` (default off ⇒ byte-identical; juryReach re-confirmed green). Gates: `voteDeduction.test.ts` + BDD `0110`. |

## Future feature backlog (PO-parked ideas — develop later)

Ideas surfaced during the comb-through that the PO wants to build later as their own spec/feature.
Not yet scheduled; captured here so they aren't lost.

| ID | Idea | Origin | Notes |
|---|---|---|---|
| FB-1 | **Diary Room with purpose** | 0013 review (2026-06-28) | Give the player DR a real, **Vault-safe** role: the engine *reads* the player's stated strategy to do something that never drives NPCs — e.g. shape which producer prompts / story beats fire, or feed a richer 0048 retrospective, or acknowledge the player's reflections. Must not cross the anti-sycophancy line (never puppeteer an NPC, never change an outcome). Would re-introduce a purposeful consumer where the removed `playerStrategyRead` stub used to sit. Coordinate with the frozen 0097 (suspicion ledger) — same "don't over-tell the player" tension. |
| FB-2 | **Finale experience / logistics in practice** | 0014 review (2026-06-28) | The endgame *mechanics* (0014) are solid; expand how the finale actually FEELS to play — pacing of the statement → 18-Q&A → one-at-a-time vote reveal, the drama/tension of the crowning, the player-as-finalist and player-as-juror beats. Builds on the live choreography (0037, review pending) — revisit after 0037 is reviewed so the "feel" work targets the real live seam. |
| FB-4 | **0121 deal-depth — the remaining 3 items (build + merge later)** | 0039 review (2026-07-12) | The 0121 ENGINE is done + merged-ready (both active kinds — comp-throw + veto-save — fire live; the loyalty-streak + reliable-ally rewards are in). **Three items remain, fully specced in `docs/features/0121-...md` §7:** **R1** the diffusing *"keeps their word"* reputation reward (wire the already-stubbed `ReconcileSink.reputation` hook to seed a Vault-free reliability belief that diffuses via the 0038 gossip layer + raises NPC deal-willingness — needs the `KnowledgeService` threaded into `reconcileDeals`); **R2** the FE chat-extraction of the new kinds (`tool_schemas.py`/`tool_implementations.py`/`agent_loop.py`) — **blocked by golden-fixture staling**: adding to the `makeDeal` enum changes the recorded schema, so either **flag-gate the schema on `ORWELL_DEAL_DEPTH`** (golden-safe, no key — recommended) or **re-record the golden fixture** (needs a live `OPENROUTER_API_KEY`); **R3** the deploy opt-in (`ORWELL_DEAL_DEPTH=1` in `smoke.sh` + `orwell-install.sh`), only after R1+R2, keeping the flag off in the golden driver. All stay behind the default-OFF flag ⇒ byte-identical until they land. |
| FB-3 | **Chat day-chunking & graceful re-entry (avoid an infinite chat)** | 0033 review (2026-07-06) | **PO DIRECTIVE: the experience must NOT be an infinite chat.** The single-thread model is technically sustainable (verified: the LLM is fed a bounded moment prompt + `stateDelta` + recalled memories + a token-budgeted history slice, *not* the whole transcript — the engine store is the memory, so a long season never degrades or slows). The problem is purely player-facing UX: reopening dumps you at the bottom of a season-long scroll with no orientation. **Keep the bulk in chat (ADR 0003) but structure it into DAYS:** (1) day/chapter dividers in-thread using the **0102 daily recap** as the divider content; (2) **collapse finished days** into their recap card (short scroll even late-season); (3) on reopen, **land at the live actionable beat** with a "Previously in the house…" recap card (0102) + the in-fiction morning re-entry (0030 H8), never the raw top-of-scroll; (4) optional day/week jump-navigator. Do NOT hard-split into a new chat per day (fragments the "one continuous house" feel + fights the two-window canonical-session binding 0008/0012). Building blocks already exist (0102, 0030 H8) — this is the UX assembly. Net goal: navigable, chapter-structured, graceful re-entry — **not** an endless scroll. |

## Numbering housekeeping (flagged during the comb-through)

These are bookkeeping collisions/gaps to untangle so the review queue stays clean. They are not
game bugs.

| Issue | Detail |
|---|---|
| ~~Duplicate `0102`~~ | ✅ **RESOLVED 2026-07-05** — `0102-weekly-recap-cliffhanger` keeps the number (tracked, built-ready); the premiere spec was renamed `0111-day-1-experience` (content unchanged) and now has its own README row. |
| ~~Duplicate `0107`~~ | ✅ **RESOLVED 2026-07-05** — `0107-named-alliances` keeps the number (tracked, built); the observability draft was renamed `0112-llm-call-observability` (content unchanged) and now has its own README row. |
| Unused numbers | `0082` and `0083` have no feature. |
