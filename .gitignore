node_modules/
dist/
coverage/
*.log
*.sqlite
*.sqlite3
.DS_Store
.cucumber-cache/
# Python virtualenvs (e.g. a root-level .venv created for FE test runs) — never committed.
.venv/
venv/
# Durable game saves (feature 0030) — runtime data, never committed.
.orwell-data/
/data/
.claude/worktrees/

# audit visual-telemetry sandbox (never commit)
.audit-telemetry/

# heavy-sim shard artifacts (local default *_RESULTS_DIR under tmp/ + CI's collected dirs)
tmp/
jury-results/
gradient-results/

# read-only surfacing-agent hypothesis scratch (never committed; excluded from vitest run below)
tests/scratch/

# throwaway repro/render scratch (delegate agents write repros + before/after renders here; never committed)
scratchpad/
frontend/scratchpad/
