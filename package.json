{
  "name": "bbai",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Immersive single-player Big Brother simulation — hexagonal engine with a structural Vault Wall.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test:unit": "vitest run",
    "test:watch": "vitest",
    "test:bdd": "NODE_OPTIONS='--import tsx' cucumber-js",
    "test:arch": "depcruise src --config .dependency-cruiser.cjs",
    "test": "npm run typecheck && npm run test:unit && npm run test:bdd"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^11.3.0",
    "@types/node": "^22.10.0",
    "dependency-cruiser": "^16.10.0",
    "fast-check": "^3.23.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
